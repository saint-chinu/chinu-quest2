# チヌクエスト2 開発仕様書

## 概要
カルドセプト風の3Dボードゲーム。プレイヤー同士の直接対決バトルは廃止し、モンスターとのバトルに一本化。カード収集・デッキ編成・ガチャによる成長要素を持つ。

## 技術スタック
- **描画**: Three.js（WebGL、3D表示）
- **データ同期・保存**: Firebase（Realtime Database or Firestore、Authentication、Storage、Functions）
- **配信形態**: PWA（Progressive Web App）
  - manifest.json + Service Worker
  - ホーム画面に追加してフルスクリーン起動、AppStore審査不要
- **対応画面**: スマホ横画面固定を想定

## 開発フェーズ（この順で実装）
0. ログイン／キャラメイク／モード選択ハブ
1. 盤面移動＋マス目イベント（シングルプレイ、CPU相手）
2. モンスターバトルシステム
3. デッキ編成画面
4. カード収集・ガチャ・通貨システム
5. 対戦モード（マルチプレイ）

現時点ではフェーズ0の骨組み（ログイン〜対戦(CPU)まで）とフェーズ1〜2を実装済み。動作確認しながら段階的に進める。

---

## 未実装まとめ（2026-08-10更新、実装指示待ち）
今後の実装指示のための棚卸し。詳細な仕様・背景は各項目が属するセクション本文を参照。

### 対人戦・マルチプレイ（2026-08-10実装、対戦本編まで完了）
- Firebase/Firestoreによる部屋ロビー（`src/pvp.js`）に加え、実際の対戦本編（ダイス・盤面移動・分岐選択・土地コマンド・召喚・カードドロー等）をホスト権威モデルでリレーする仕組みまで実装済み。ローカルのFirebaseエミュレータ+2ブラウザタブでロビー→デッキ選択→対戦開始→ターン交代を含む一連の流れを確認済み
- ホストだけが本物の`Game`シミュレーションを持ち、ゲストは`publicState`（盤面・プレイヤー状態、手札は含まない）と自分専用の手札ドキュメント（`private/{uid}`）を購読する薄いクライアント。ゲスト自身の盤面は`createBoard()`/`GameScene.buildBoard()`をホストと同じ引数で呼び直して再構築する（決定的なので一致する）
- `main.js`の`relayable(type, localPrompt, {broadcast})`が、game.jsの約20種の意思決定コールバック（`onLandCommand`等）をホスト自身の手番ならローカルUIへ、相手の手番ならFirestore経由でゲストへ、それぞれ振り分ける。`game.js`側のコールバックは`(payload, ..., player.id)`のように途中に追加引数を挟む型があるため、`relayable`は可変長引数を受けて「最後の引数=player.id」として分離する（`onLandCommand`/`onShopPurchase`がこれに該当）
- ゲスト発の操作（ダイスを振る／スペル使用）は逆方向の`guestAction`/`guestActionId`チャンネル（`GuestActionSender`/`HostActionListener`）で送る
- 相手の本当の手札は表示しない（vs CPUでは見せているCPUの手札と違い、対人戦では両者とも自分の手番以外は相手の手札を伏せる）
- Firestoreは値に`undefined`を許さないため、`sendGuestResponse`は`undefined`を`null`に丸めてから書き込む（`promptCardReveal`等、確認のみでresolve()に引数を渡さない型があるため）
- 本物のFirebaseプロジェクト設定（`.env`）はまだ未投入（開発中はローカルエミュレータにフォールバックしている）
- モンスター同士のバトル演出（`battle-scene-modal`）は実機で確認済み（侵略→アイテム選択→攻撃応酬→通行料決着まで一巡）。検証中に`relayable()`のバグを発見・修正: `onBattleSceneEnter`/`onBattleAttack`/`onBattleOutcome`はplayer.idを引数に持たないbroadcast型なのに、末尾引数を一律「player.id」とみなして切り落とす処理が唯一の引数（payload本体）を誤って消してしまい、ゲスト側で`undefined`を受け取って侵略した瞬間に無言で止まっていた。broadcast型は末尾切り落としをスキップするよう修正済み
- ショップマス（`onShopPurchase`）とSTART/EVENTマス（チェックポイント到着）は実機で確認済み。検証中に2件のバグを発見・修正:
  - `getTileSummary()`: 非LANDマス（START/EVENT/SHOP）はboard.js側でlevelがundefinedのまま置かれており、そのままFirestoreへ中継すると拒否されて対戦相手（チェックポイントに止まった側）の意思決定が無言で止まっていた。非LANDマスのlevel/landValue/tollをnullへ丸めて修正
  - `relayable()`: 追加引数を持つ型（`onLandCommand`/`onShopPurchase`）の中継payloadを配列でまとめていたが、引数自体が配列の場合（ショップの購入候補一覧など）Firestoreが拒否する「ネストした配列」になっていた。配列ではなく`{a0, a1, ...}`という個別プロパティのオブジェクトにまとめて修正
- 未検証のまま残っている個別フロー: スペル使用のリレー、破産・勝敗判定の対人戦での見え方 - アーキテクチャ上は他の意思決定と同じ経路を通るはずだが、実機での動作確認はまだ
- 既知のv1簡略化: ゲスト側の駒移動はステップアニメーションなし（publicState同期のたびに最終位置へスナップ）、切断検知なし（ホスト退出→ゲスト強制退出のパスのみ）、ゲストのダイス目はゲスト側で計算した値をそのまま信頼（サーバー側再検証なし）

### カード・収集まわり
- カード規模の本格拡充（想定: モンスター各属性20種・アイテム30種・スペル40種。現状は名前付き実データがごく少数＋汎用プレースホルダー中心）
- アイテムの一部復活効果（現状は全アイテムが勝敗問わず戦闘後に必ず消滅）
- 通貨・ガチャ処理のサーバー側（Firebase Functions）移行（現状は全てクライアントローカルのlocalStorage、改ざん防止なし）

### 土地コマンド・盤面まわり
- **強制停止**（指定マスに強制的に相手を止まらせる効果）: 同盟仲間は巻き込まれない、というルールは確定済みだが効果自体が未実装
- 特殊能力は「火炎瓶男」の1種類のみ（ダメージ型）。今後別タイプ（回復・移動阻害等）を増やす場合は`ability.type`の分岐をgame.js側に追加していく想定

---

## 複数デッキ（ブック）スロット（2026-08-10実装）
- `character.decks`: `{id, name, deckList}`の配列、最大`MAX_DECKS`(3)冊。旧`character.deckList`（単一）は`ensureBreedFields`が初回ログイン時に`decks[0]`へ自動移行する（フィールド自体は残るが以後どこからも読まない）
- デッキ編成画面にブック切り替えタブ（既存のブック名ボタン＋冊数未満なら「＋新規作成」）とデッキ名編集欄を追加。切り替えても未保存の編集内容はブックごとに独立（保存ボタンを押した冊だけが更新される）
- **対戦・ストーリーどちらも、盤面に入る直前に必ずデッキ選択画面を挟む**（`promptDeckSelection()`）。最大3冊を本の絵＋名前で並べて「デッキを選んでください」→ 押すと「「〇〇」にしますか？ はい/いいえ」＋カード内訳一覧（`#deck-select-breakdown`、`max-height`+`overflow-y:auto`の同一パターンでこの内訳リストだけがスクロールし、画面自体はスクロールしない）
- 対人戦（対戦本編、2026-08-10実装）でも同じ`promptDeckSelection()`をホスト・ゲスト双方の対戦開始フローに挟んで配線済み
- **副産物のバグ修正**: このセッション中盤までに保存されていたデッキの一部に、カタログに存在しないキー（壊れた保存データ）に紐づく「空オブジェクトのカード」が混入し続けていたことが判明（デッキ編成画面は所持数ベースで数えるため気づかず、今回新設した内訳表示で初めて「undefined」として露見した）。`showDeckScreen`のカウント初期化時にカタログに存在しないキーを弾くよう修正（保存し直すと自動的に取り除かれ、正しい枚数不足として表示される＝自己修復）

## ストーリーモード「再挑戦」（2026-08-11実装）
- クリア済みステージをストーリー画面でもう一度選ぶと、本編とは別のシナリオ（`STORY_STAGES`各ステージの`replay`フィールド）で再戦できる。`intro`/`outro`/`format`/`opponents`/`ally`/`extraAlly`/`heroAllianceId`/`enemyAllianceId`のうち指定したものだけ本編の値を上書きし、未指定のフィールドは本編の値をそのまま使う（`main.js`の`buildBattlePlayerConfigs`がstage本体とvariant=replayの両方を受け取りフォールバックする）
- 例: ①ヒトデ戦の再戦は農家のウサギが乱入し1vs1vs1に、②暴君マダイ＆ニュウドウカジカ戦の再戦は某不思議の国の少女が乱入し1vs1vs1vs1に、③の再戦はヒトデが加勢し3vs2に拡大（2人目の味方は`ally`1フィールドでは表現できないため`extraAlly`という専用フィールドを新設）。④ダンボール男戦（ラスボス）はintro/outroのみ差し替え、形式・登場人物は本編と同じ1vs1のまま
- 再戦の勝敗は`character.storyProgress`・クリア報酬に一切影響しない（`handleStoryReplayEnd`は勝敗に応じたセリフを表示してステージ選択に戻るだけ。本編初回クリア時の`handleStoryBattleEnd`とは別関数）。何度でも遊べるおまけ戦闘という位置づけ
- ストーリー画面のステージ行は、クリア済みの場合ラベルが「クリア済み（もう一度挑戦できます）」に変わり、クリック先が本編`playStoryStage`から再戦`playStoryReplay`に切り替わる

## カードエディタのCSV一括登録（2026-08-11実装）
- カードエディタ画面に単体登録フォームとは別に「CSVで一括登録」セクションを追加（`index.html`の`#card-editor-bulk`）。約200種規模のカードデータを1件ずつ手入力するのは非現実的という判断から、Excelではなく依存ライブラリ不要のCSVを採用
- CSVはヘッダー名ベースの列マッピング（列の並び順に依存しない）。「種類」「レアリティ」「属性」「効果」列は日本語ラベルで入力し、内部でenum値に逆変換する（`BULK_TYPE_LABELS`等、main.js）。テンプレートCSVはボタン1つでダウンロード可能
- **画像はファイル名一致で一括紐付け**: CSVの「画像ファイル名」列と、複数選択した画像`<input type=file multiple>`のファイル名が一致した場合のみ自動で紐付く（一致しなければ画像なしで登録される）。1枚ずつのアップロード操作は不要
- 行ごとに独立してバリデーション（`validateCustomCard`を流用）するため、1行の入力ミスが他の正常な行の登録をブロックしない。結果は行単位で成功/エラーを一覧表示（`renderBulkCsvResult`、エラー内容も行ごとに表示）
- パーサーは`parseCsv`（RFC4180ライクな手書き実装、ダブルクォート内カンマ・改行に対応、BOM除去込み）。CSV/画像読み込みからカタログ保存まで全てクライアント側で完結し、サーバー通信は発生しない（`saveCustomCardsBulk`が1回のlocalStorage書き込みで全件保存）

## 複数マップ + 対人戦のマップ選択（2026-08-11実装）
- `board.js`の盤面生成を単一固定レイアウトから複数マップ対応へ拡張。`MAPS`（`{id, name, rows}`の配列）で①〜④のストーリー各ステージに対応する4つの盤面レイアウトを定義し、`createBoard(mapId)`がidで引く（省略/不明なidはMAPS[0]＝①ヒトデの縄張りにフォールバックするので、CPU戦など従来どおりmapId無指定の呼び出しは今までと完全に同じ盤面になる）
- ストーリーモードは各ステージの`key`（`hitode`/`madai`/`budou`/`danball`）をそのまま`mapId`として`createBoard()`へ渡すだけで対応マップになる（`story.js`側にmapIdフィールドを別途持たせる必要はない）。本編・再挑戦（replay）どちらも同じステージの同じマップを使う
- 4マップの形状: ①外周＋十字（7×7、既存レイアウトそのまま）／②外周＋「H」字の内部通路2本（9×7）／③外周＋中央3×3の広場（7×7）／④外周＋十字を拡大（9×9、ラスボス戦向けの一番広いマップ)。全マップ、開始マス起点のBFSで全タイル到達可能なことを確認済み（孤立マスなし）。実素材の背景画像はまだ1種類（`stage1.png`）しか無いため、当面は全マップ共通のプレースホルダーとして使う
- **対人戦のマップ選択**（`main.js`）: ホストが「部屋を作る」を押すとまず`#pvp-map-select-screen`（4マップのサムネイル一覧）が出る。サムネイルは`board.js`の`createMapThumbnailCanvas()`が盤面レイアウトの配色（属性色/開始・チェックポイント・ショップ色）だけから毎回その場でcanvas描画する（実素材の画像は使わない）。マップをタップすると縮小プレビュー付きの確認ポップアップ「『（マップ名）』で遊びますか？ はい/いいえ」（`#pvp-map-confirm-modal`）が出て、はいで初めて`createPvpRoom({..., mapId})`を呼ぶ（部屋の`mapId`フィールドとしてFirestoreに保存、ゲスト側もこれを読んで同じ`createBoard(mapId)`を呼ぶので同一盤面になる）
- **対人戦ロビーのデッキ選択タイミングも変更**: 参加者（ゲスト）は部屋コードを入力して「入室する」を押した直後にその場でデッキを選び、選んだデッキリストは入室と同時に`guestDeckList`としてFirestoreへ書き込む（`joinPvpRoom`のuid/status更新と同じ1回の書き込みにまとめている - firestore.rulesの更新権限がゲストは「入室の瞬間」以外は`guestResponse`系フィールドしか書けない制約のため、デッキも入室と同時に書く必要がある）。そのためロビー画面に入った時点でゲストはもう準備完了で、あとはホストの「対戦開始」を待つだけになる。ホストは対戦開始を押した時点で`guestDeckList`を読むだけで済み、以前あった`relay.ask('pickDeck', {})`（対戦開始のたびにゲストへ質問を飛ばす往復）は不要になったので削除した
- ホストが「対戦開始」を押すと`beginPvpMatch()`が部屋の`status`を`'battling'`に更新し、ゲスト側は購読中のロビーリスナーがこの遷移を検知して`startPvpGuestBattle()`を呼ぶ（以前はpickDeck質問への回答自体が対戦開始の合図を兼ねていたが、デッキ選択を入室時点に前倒ししたため、対戦開始専用の一方向シグナルとして`status`遷移を新設した）
- **未検証のまま残っている部分**: ローカルのFirebaseエミュレータ（Firestore/Auth）がこのセッションでは起動できなかった（Java未インストール、`winget install Microsoft.OpenJDK.21`が別セッションで34時間ハングしていたのを発見しkillした経緯あり）ため、マップ選択→部屋作成→ゲスト入室→対戦開始という一連の実際のFirestore経由の往復はホスト・ゲスト2ブラウザでの実機確認ができていない。マップ選択画面・確認ポップアップ・サムネイル描画・「部屋を作成できませんでした」への失敗時フォールバックまでは実機確認済み。ストーリーモード4マップは全て実機で対戦開始・ダイス・移動まで確認済み（CPU戦のデフォルト盤面も回帰なし）

## ②マダイの岩礁マップ差し替え + ほこら/全チェックポイント制ゴール（2026-08-11実装）
- ユーザー指定のレイアウトに差し替え（`board.js`のMADAI_ROWS、15×5）。左右2つの縦長ループを、中央のG（開始マス）を通る横一列の通路が橋渡しする「めがね」型。四隅がH（ほこら）とC（チェックポイント）の対角配置（左上H・右下H、右上C・左下C）。ショップマスは無し（ユーザー指定に無いため）
- **このマップだけ「全チェックポイントを通過しないとゴールにならない」ルール**が有効（`MAPS`の`madai`エントリに`requireAllCheckpoints: true`。他の3マップはfalseのまま＝従来通り無条件でゴールボーナス）。`Game`はプレイヤーごとに`passedCheckpoints`（通過済みチェックポイントtile idの集合、`_movePlayer`のループで通過・着地どちらでも記録）を持ち、`_grantGoalBonus`が全チェックポイント通過済みの時だけ+100Gを渡してから記録をクリアする（未達成ならログだけ出してボーナス無し。次ラップでまた全部通り直す必要がある）
- **ほこらマス「マダイの福音書」**: 新タイル種別`TileType.SHRINE`。ちょうど止まると4種類の効果からランダムに1つ発生（`Game._resolveShrineTile`）:
  - 混沌を愛せ: 手札を全て捨て、デッキ（山札+捨札）を丸ごと再シャッフルしてから5枚引き直す（`Deck.resetShuffle()`を新設）
  - 力こそパワー: 盤上の配置済みモンスターから1体をランダムに選び、「倍化」という名の永続呪い（既存の呪いインフラ`applyCurse`をそのまま流用 - 呪いの`addedAtk`にその時点の基礎ATKと同じ値を入れるので実質倍になる）をかける。盤上にモンスターが1体も無ければ不発
  - 速度違反はご愛嬌: 次のプレイヤー人数分の手番だけ、サイコロフェーズ自体を見せずに強制的に10マス進ませる（`Game.forcedDiceRemaining`、`_beginTurn`が消化）。フェーズが無いのでその間はスペルも使えない
  - 右の頬をシバかれたら、左の頬をシバきなさい: 盤上の配置済みモンスターの土地全てに「強制停止」の呪い（`tile.forcedStopCursed`）をかける。呪われた土地は所有者本人と**同盟仲間**（ユーザー指定の追加ルール）以外、通過中でも足を止められ通行料を払うか戦闘するしかなくなる（`_movePlayer`のループ内で判定・強制的に移動を打ち切る）。呪いは対象の土地で実際に戦闘が起きると消える（`_runInvasion`/`_humanMoveFlow`が戦闘解決直後にクリア）
  - `getTileSummary`の`unitAtk`/`unitHp`は呪い込みの実数値を返すよう修正（以前はdef値そのままで「倍化」が数値に反映されなかった）。土地の情報表示にも「呪い: 強制停止中」の注記を追加

## ③決闘の浜辺マップ差し替え + ワープマス（2026-08-11実装）
- ユーザー指定のレイアウトに差し替え（`board.js`のBUDOU_ROWS、11×12）。メインの盤面（外周ループのみ、11×6）と、行と行の間を丸ごと空白にして物理的に隔絶したもう一つの島（4×4のシンプルな外周ループ）の2領域から成る。2領域はtileの隣接関係としては一切繋がっていない（Node上のBFSで確認済み）
- **ワープマス**: 新タイル種別`TileType.WARP`。V（メイン盤面に1マスだけ）とP（別の島の四隅、4マス）の2種類のコードがどちらもこの型になる。ちょうど止まった時だけ発動（通過しただけでは何も起きない - ほこらと同じ挙動）。VはPのうち「先頭の1つ」（`createBoard`がtile生成順=盤面走査順で最初に見つかるP、実質的に一番上・一番左の角）へ、4つのPはどれに着地してもVへ、瞬間移動する。着地時点でリンク先を計算するのではなく`createBoard`の末尾で`tile.warpTargetId`として盤面生成時に一度だけリンク付けしておく方式（`Game._resolveWarpTile`はそれを読むだけ）
- ワープは一歩ずつ歩くtween演出をせず、その場で座標をスナップする（「瞬間移動」の意図的な演出）。ワープ後は移動元の概念が無くなるので`previousTileId`をリセットする（次に分岐に差し掛かった時、全方向が候補になる）
- **検証方法についての注記**: このセッションのBrowserペインは`document.hidden === true`（実際にはコンポジット/描画されていない）状態で固定されており、`requestAnimationFrame`に依存するアニメーション（`utils.js`の`tween()` - 盤面の1マスごとの移動、`GameScene.panTo`のカメラパン）が一切発火せず、ダイスを振った後の実際の移動が画面上では永久に完了しない（`Game`側のロジック自体は正しく動いていても、視覚的な確認ができない）。そのため②③の新マップ・新メカニクス（チェックポイント制ゴール・ほこら4効果・強制停止＋同盟免除・ワープ）はNode.js上で`requestAnimationFrame`をポリフィルした上で`Game`クラスを直接動かすテストスクリプトで検証した（盤面の連結性・ワープの紐付け・各効果の実際の状態変化を全てアサーションで確認、全て成功）。ブラウザでの確認は「戦闘開始時点の初期状態が正しく組まれる」ところまで（プレイヤー構成・盤面のタイル種別・コンソールエラー無し）に留まる - ダイスを振ってからの実際の移動アニメーションは次回、Browserペインが正常にコンポジットされるセッションで改めて実機確認するのが望ましい

## ④暗転した世界マップ差し替え＋全マップ共通「全チェックポイント制ゴール」＋パネルの通過状況表示（2026-08-11実装）
- ④ダンボール男戦のマップをユーザー指定のレイアウトに差し替え（`board.js`のDANBALL_ROWS、11×11）。外周ループ＋「田」字型の十字通路、チェックポイント4つ（四隅寄り3つ＋盤面中央1つ）、右辺中央に3連続のほこら（H×3、縦一列）。開始マス起点のBFSで全85マス到達可能を確認済み
- **「全チェックポイントを通過しないとゴールにならない」ルールを全4マップ共通に変更**（以前は②マダイの岩礁だけの専用ルールだったが、ユーザー指定で全ステージに拡大）。`MAPS`の全エントリに`requireAllCheckpoints: true`を設定。ロジック自体（`Game._grantGoalBonus`/`_hasPassedAllCheckpoints`）は②実装時のまま変更なし、②以外の3マップにも同じ判定が及ぶようになっただけ
- **チェックポイント番号付け**: `createBoard()`がEVENT(チェックポイント)タイルに盤面走査順（生成順）で1から通し番号(`tile.checkpointNumber`)を振る。マップごとの実在番号一覧は`Game.checkpointNumbers`としてコンストラクタで一度だけ計算し、`_notifyState`/PvPの`_pvpSnapshot`が毎回そのまま送る
- **プレイヤーパネルに通過状況を表示**: 各プレイヤー名の下に、そのマップのチェックポイント番号を暗い丸バッジで並べる（`renderPlayerPanels`が`checkpointRowHtml()`で描画、CSSは`.checkpoint-num`/`.checkpoint-num.lit`）。該当プレイヤーがそのチェックポイントを通過済みなら明るい黄色に点灯（`player.passedCheckpointNumbers`、Gameが`player.passedCheckpoints`のtile idを`checkpointNumber`に変換して送る）。ゴールでボーナスを獲得すると全て消灯し次のラップの通過待ちに戻る。チェックポイントの無いマップは現状無い（全マップ最低2個以上）が、無い場合は何も表示しない設計にしてある
- 実機確認: ④の戦闘開始直後、両プレイヤーのパネルに「①②③④」の4バッジが全て消灯状態で正しく表示されることを確認（CSSの色・サイズも意図通り）。点灯側の動作（実際にチェックポイントを通過してからの点灯遷移）はダイス移動アニメーション自体がこのセッションのBrowserペイン制約で確認できないため、Node上のGameクラス直接テストで検証済み（`passedCheckpointNumbers`が通過したチェックポイントの番号だけを正しく含み、ゴールでクリアされることをアサーションで確認）

## 土地レベルの縁取り表示（2026-08-10実装）
- Lv2以上の土地マスに黒い縁取り（枠）を重ねて表示する。Lv2=細い一重、Lv3・Lv4と段階的に太くなり、Lv5だけ太さを戻して二重の細い枠にする（Lv1は枠なし）。`scene.js`の`GameScene.updateTileLevelBorder(tile)` - `THREE.ExtrudeGeometry`で作った平たい額縁状ジオメトリをtile.meshの子として重ねる方式（GL線の太さはブラウザ依存で制御できないため）
- 呼び出し箇所は土地Lvアップ（`_humanLevelUpFlowForTile`）と土地売却（`tile.level`を1にリセットする`_humanSellLandFlow`）の2箇所のみ - 属性・所有者・配置モンスターの変化ではtile.levelは変わらないので、それらの操作では呼ばれない

## 土地コマンド「特殊能力」（2026-08-10実装）
- 配置されたモンスターが持つ固有能力を行使する土地サブメニューの新項目。カード定義に`ability`フィールド（例: `{ type: 'damage', range: 3, power: 10 }`）を持たせるだけで有効化される汎用仕組み（`game.js`の`_humanAbilityFlow`/`_tileDistance`）。射程はマス目のグラフ距離（隣接関係でのホップ数、移動で使うのと同じ`tile.neighbors`）で判定する
- 第一号として`battleCards.js`に「火炎瓶男」（火・R・ATK20/HP25・コスト40）を追加。射程3マス以内の敵モンスター1体に10ダメージを与える
- 対象は自分と別陣営（同盟仲間は対象外）で、射程内にいる全ての敵から土地サブメニュー内の一覧UI（新モーダル`#ability-target-modal`）で選ぶ。ダメージで対象のHPが0以下になったら即座に倒れる（土地は無人化、`_handleUnitDeath`経由で不死鳥特性も通常の戦闘死と同様に効く）
- 特殊能力の使用は移動・売却と同様、実行すると自動でそのターンが終了する
- 土地サブメニューの「特殊能力」ボタンは、配置モンスターが`ability`を持つ土地でのみ表示される（`getTileSummary`の`hasAbility`フィールド）

## ストーリーモード（`chinu-quest2-story-spec.md`、2026-08-09実装）
- `src/story.js`: `STORY_STAGES`（全4ステージのタイトル・形式・登場人物・intro/outro会話・報酬定義）。立ち絵グラフィックは未着手のため`speaker`名だけのプレースホルダー表示（画像パスを持たせる構造ではなく、後日`portrait`等のフィールドを足すだけで差し替えられるようテキストのみで組んである）
- ハブの「ストーリー」タイル → `#story-screen`（ステージ選択・ロック/クリア済み表示） → `#story-dialogue-screen`（1行ずつクリックで進める会話）→ バトル本編 → outro会話 → ステージ選択に戻る、という一連の流れをmain.jsに実装
- **バトル本編は通常の対戦と同じ`Game`クラスをそのまま使う**（後述のN人対戦エンジン汎用化により、`playerConfigs`と`storyMode:true`を渡すだけで1vs1・1vs1vs1・2vs2同盟戦のいずれも成立する。ストーリー専用のバトルエンジンは別途作っていない）
- 進行状況は`character.storyProgress`（クリア済みステージ数）としてキャラクターデータに保存。既存アカウントには`ensureBreedFields`でデフォルト値0を補完
- ステージ③クリア報酬「ペーの杖」は`battleCards.js`の`ITEM_CATALOG.peeStaff`（EXレア・武器・ATK+25/HP+10・先制付与特性）として実装、クリア時に`ownedCards`へ自動付与
- 敗北時はM変換・進行度変更なしでステージ選択に戻るのみ（再挑戦前提、ペナルティなし）
- **NPCデッキサンプル（2026-08-09実装）**: 各キャラは`battleCards.js`の`buildThemedDeckList({elements, featuredMonster, featuredItem})`で組んだ固有の40枚デッキを持つ（`story.js`の各キャラ`theme`フィールド）。属性2種縛り＋任意で名前付きモンスター/アイテムを4枚ずつ混ぜることでキャラごとに個性を出している（例: ヒトデ=水/森+港区女子、暴君マダイ=火/雷+サラリーマンダー）。ラスボスのダンボール男だけは`elements: [neutral]`の1属性縛りで組んでおり、`buildCardPool`のランク上昇（同属性内での連番ランクに応じてステータスが伸びる仕様）がそのまま働くことで自然とATK/HPが他キャラより大幅に高くなる（無属性なので属性有利/不利も一切受けない）。カード規模が小さい現段階での簡易差別化であり、本格的なキャラ専用カードが増えたら置き換わる想定

## N人対戦エンジンの汎用化（`src/game.js`、2026-08-09実装）
ストーリーモードの1vs1vs1・2vs2同盟戦を成立させるため、`Game`のプレイヤー構築を汎用化した。
- コンストラクタが`playerConfigs`（任意人数のプレイヤー設定配列）を受け取れるようになった。省略時は従来通りhumanPlayer+固定CPUの2人構成にフォールバックするため、通常の対戦モードは無変更で動作する
- ターン進行・CPU行動ロジック・同盟（`allianceId`）の通行料免除／連鎖合算／総資産合算は元々プレイヤー人数に依存しない実装だったため、変更不要でそのまま3人・4人構成に対応した
- `storyMode`フラグを追加。通常モードでは従来通り「破産＝500Gを渡されゴール地点から再スタート」だが、`storyMode:true`では破産＝脱落（盤上の土地・ユニットを手放し、駒を非表示化）として扱い、`_checkStoryWinCondition`が生存陣営数を判定する（`allianceId`の集計だけで1vs1vs1のFFAも2vs2の同盟戦も同じロジックで勝敗が付く）。勝敗確定時は`onStoryBattleEnd({won, alivePlayerIds})`コールバックでUI側に通知
- プレイヤーパネル（`renderPlayerPanels`）に脱落時のグレーアウト表示（「（脱落）」ラベル＋`opacity`低下）を追加

---

## フェーズ0: ログイン・キャラメイク・モード選択ハブ（骨組み実装済み・ローカル仮認証）

### 初回起動フロー
1. **ID・パスワードで新規登録 or ログイン**（Firebase Authenticationを想定）
2. 初回ログイン時のみ**キャラメイク**: 好きなキャラ画像を選ぶ（盤面上の自キャラアイコンになる。現状の丸いプレースホルダー色アイコンに代わるもの）→ 名前を決定 → 初期デッキを2種類から選択

### 2回目以降のログイン
- ログイン後はいきなり対局に入らず、**モード選択画面**（ハブ画面）を経由する
  - **ストーリー** — 1人プレイでストーリーを進める（新規モード。シナリオ設計は今後詳細化）
  - **対戦** — CPU戦または対人戦（対人戦はフェーズ5のオンライン対戦に相当）
  - **デッキ** — カード図鑑から選んで40枚のデッキを編集する（フェーズ3のブック編成に相当。骨組み実装済み、詳細はフェーズ3参照）
  - **ショップ** — 所持Gでパックを購入。パックは複数種類あり、レアリティごとに排出率が異なる（フェーズ4のガチャに相当）
  - **ブリード** — 所持Gを使ってブリードモンスターのステータス・特殊能力をセットする（フェーズ4のブリードモンスターに相当、今回「お金を使ってステータス構成を組む」という具体的な操作方法が確定）

### 既存フェーズとの対応関係
このハブ画面は独立した新規要件ではなく、フェーズ3〜5の入口を1つの画面にまとめたもの。「対戦」の中の“CPU戦”が現在実装済みの盤面ゲームそのものにあたる。実装順としては、まずログイン〜モード選択〜対戦(CPU)の骨組みを通してから、デッキ／ショップ／ブリード／ストーリーを順次埋めていく想定。

### 実装状況（骨組みのみ）
- `src/auth.js`: ローカル仮認証（localStorageにID/パスワード/キャラ情報を保存するだけ、Firebase未接続）。`loginOrRegister(id, password)`で新規登録とログインを兼ねる、`saveCharacter(id, character)`でキャラ情報を保存
- キャラメイク画面: アイコンは実素材がまだ無いため色スウォッチ6色から選ぶプレースホルダー（盤面のコマ色になる）。名前入力（10文字まで）。初期デッキは`battleCards.js`の`STARTER_DECKS`（火・森ブック／水・雷ブックの2種、下記フェーズ3参照）から選択
- モード選択ハブ: 「対戦」「デッキ」「カード図鑑」「ショップ」「ブリード」が実際に機能する。CPU戦を選ぶと`main.js`の`startBattle(character)`が呼ばれて既存の盤面ゲームが開始する（対人戦ボタンは無効化表示）。**ストーリーのみ**「準備中です」のスタブ画面（戻るボタンのみ）が残っている
- `game.js`の`Game`コンストラクタに`humanPlayer: { name, color, deckVariant, deckList }`を渡せるようにし、盤面ゲーム側の自キャラ名・コマ色・デッキがキャラメイク（またはデッキ編集）の選択を反映するようにした

---

## フェーズ1: 盤面・移動システム

### カメラ・ビュー
- カルドセプト風の見下ろし視点（斜め上からのアングル）
- コマが1マス移動するごとに、カメラもなめらかに追従（線形補間でスライド）
- コマが停止した瞬間、カメラも静止。イベント発生時はズームイン等の演出を想定（フェーズ1では簡易でよい）

### 盤面（ステージ1・分岐あり、実装済み）
- 周回コースから分岐ありのグラフ構造に変更済み。`board.js`の`STAGE1_ROWS`にASCIIアートでマス配置を直書きし、各マスは隣接マスのid配列`neighbors`を持つ（`generateLoopGridCoords`による単純な周回生成は廃止）
- ステージ1のレイアウト: 外周ループ＋中央を貫く十字（外周4辺の中点と中央がそれぞれ3〜4方向に分岐する交差点になる）
  ```
  G火火火火火C
  水　　火　　雷
  水　　火　　雷
  水水水無雷雷雷
  水　　森　　雷
  水　　森　　雷
  C森森森森森S
  ```
  G=スタート、C=チェックポイント、S=ショップ、火/水/雷/森/無=空き地属性
- マス種類: `TileType`に`START`/`LAND`/`EVENT`/`SHOP`の4種（ショップマスを新規追加）

### ゲームループ
以下の①②③の流れで実装済み（フェーズ2の土地コマンドを先行実装）。
- ① サイコロを振る → コマが1マスずつ進むアニメーション
- ② 特殊マス（ゴール＝スタートマス／チェックポイント＝イベントマス）ならその処理。敵の土地に止まった場合はここで通行料を自動精算
- ③ 止まったマスが土地なら「土地コマンド」メニュー（フェーズ2参照）
- ターン制（プレイヤー人数分を順番に処理。フェーズ1はCPU 1体との対戦を想定）

---

## 手札・ドローシステム（フェーズ1で先行実装済み）
ターンループの土台として、ブック（デッキ）3本柱に先行してドロー・手札まわりを実装した。

- ブックは40枚固定（モンスター4属性×6枚＝24枚、武器防具8枚、スペル8枚）
- カード種類: モンスター（4属性: 火・水・風・地、カルドセプトの地形属性に準拠）、武器防具（戦闘用）、スペル
- ゲーム開始時、各プレイヤーはブックからランダムに4枚ドローして初期手札とする
- 手札上限は6枚。ターン開始時に1枚ドローし、これが7枚目になった場合はどれか1枚を選んで捨てる
- 自分の番のドロー時はカード画像を大きくアップ表示してから手札に加える（CPUのドローは非公開情報として画面には出さない）
- ブックを1周（山札を引き切る）したら、捨て札を山札にシャッフルして戻す
- 手札表示は2系統: (1) 自分の手札は画面左下に常時小さく表示、自分だけいつでもタップしてカード情報を確認できる。(2) ターンが来たプレイヤーの手札は画面中央上部（サイコロの下）に、左下の2倍サイズで表示し、サイコロを振った瞬間に消える。CPU（対人戦なら相手）のターンでも中身をそのまま表示する（伏せない）。ただし左下の常時パネルは常に自分の手札のみで、相手の手札はそこには出ない
- スペルはターン毎に1回、サイコロを振る前だけ中央の手札から使用可能。使用すると即座に手札から消費され、効果発動演出のあと自動的にサイコロを振れる状態に戻る（実際のスペル効果はフェーズ2以降のバトル設計で詳細化）
- サイコロは目押し方式: 1回目のクリックでスピン開始（1〜6の目が高速で切り替わる）、2回目のクリックで停止を予約し、そこから0.7秒後に表示されていた目で確定する（クリックの瞬間に即決まらないスキル要素）。CPUも同じ`rollDice(steps)`経路で乱数を振るが、目押し操作はプレイヤーのみ
- CPUの意思決定（サイコロを振るまでの間、土地購入判断、捨て札選択）には毎回一呼吸の待ち時間を入れ、テンポが速すぎないようにする

## フェーズ2: モンスターバトルシステム
- CPU戦の強さ調整（初心者向けの難易度からスタートし、対人戦へのステップとする）
- **土地の獲得は「購入」ではなく「召喚」**: 空き地にモンスターカードを召喚することで自分の土地にする。土地購入コストは廃止し、召喚コストのみを消費する
- **侵略**: 他プレイヤーが所有する土地にも、召喚と同じコスト・ルールでモンスターを送り込み、バトルによって奪う（＝侵略）。召喚と侵略は同一の仕組み（対象が空き地か、敵の土地かの違いのみ）

### 土地コマンド（全面刷新済み）
止まったマスが土地・スタート・チェックポイントのいずれかの場合、以下の3択メニューが表示される（ラベルにA/B/C等の記号は付けない）。
- **召喚（敵地なら侵略）** — 今止まっているマスが土地の場合のみ有効。手札の（コストが払える）モンスターカードを選択（選ばれたカードは2回点滅）→「◯◯しますか？ コスト◯G」ではい/いいえ確認 → 空き地なら召喚、敵地ならバトル発生（戦闘結果は下記バトルルール参照）、自陣（既に自分のモンスターがいる）なら入れ替え（下記参照）。実行したら自動でターン終了
- **土地** — カメラワーク画面に遷移し、**このターンの移動で通過したマス**（サイコロの出目の数だけ、着地マス含む）だけがゆっくり点滅・淡く発光する。ただしスタートまたはチェックポイントにぴったり止まった場合は例外的に**自分の所有地すべて**が対象になる（本拠地的な扱い）。発光しているマスをタップすると：
  - 自分のモンスターが配置されている土地 → 縦並びのサブメニューが表示: **入れ替え**（新しいカードの召喚コストが必要。元々配置されていたモンスターのカードは手札に戻る）／**土地Lvアップ**／**属性変更**／**移動**（下記参照）／**情報**／**もどる**
  - それ以外（空き地・敵地・自分のモンスターがいない自分の土地は存在しない — 下記の所有権モデル参照）→ タップしてもそのマスの情報がポップアップするだけで、サブメニューは出ない
  - サブメニューの「土地Lvアップ」「属性変更」「入れ替え」「移動」はいずれも実行したら自動でターン終了。「情報」はポップアップを閉じると同じサブメニューに戻る。「もどる」でこの土地ブラウズを終了しトップの3択メニューに戻る（ターンは終了しない）
- **終了** — 何もせずターン終了
- CPUは召喚/侵略ロジックのみ自動実行（土地ブラウズ相当の操作は行わない）

**移動（新規）**: サブメニューの「移動」を選ぶと、配置されているモンスターに隣接する土地（`neighbors`、空き地または敵地のみ・自分の他の所有地には移動不可）へ向けて矢印が表示される（分岐選択と同じ画面斜め方向の矢印UI）。1つ選ぶと「移動しますか？」のはい/いいえ確認。空き地なら戦闘なしでそのまま移動。敵地なら侵略と同じバトルが発生し、以下の3通りに分岐する：
  - 侵略側が生存・防衛側が死亡 → 移動先の土地を奪取（そこへ配置）。元いた土地は所有権を失う（下記モデル参照）
  - 両者生存（防衛成功） → モンスターは元の土地にとどまる（実質何も変化しない）
  - 侵略側が死亡（防衛側の生死問わず） → モンスターは消滅。元いた土地は所有権を失う。防衛側も同時に死亡していれば移動先の土地も所有権を失う
  移動（および移動先での戦闘）が発生したら、結果を問わず自動でターン終了

**土地の所有権とレベルのモデル（確定仕様）**:
- **所有権はモンスターが配置されている間だけ成立する** — 「所有しているがモンスターがいない」という中間状態は存在しない。モンスターが死亡・移動で離れた瞬間、そのマスの `owner` は必ず `null` に戻る
- **土地のレベルは所有権と無関係に保持される** — 所有権を失っても（土地を売る、という未実装の将来機能を除き）レベルは下がらない。無人になった土地を誰か（元の持ち主自身を含む）が改めて召喚/侵略/移動で獲得すると、そのマスに残っていたレベルがそのまま引き継がれる（＝「捨てられた高レベル地」を拾うのが得になる）

**通行料（先行実装から仕様変更・確定仕様）**: 敵の土地に着地した瞬間の即時徴収はやめ、その土地コマンドが終わる瞬間に一度だけ確定する。
- 侵略を試みて**勝ち、土地を奪った場合は通行料は一切かからない**（自分が奪った土地に自分で通行料を払うのはおかしいため）
- 侵略に**負けた/そもそも侵略しなかった**（別の所有地をレベルアップするだけ等）場合は、今まで通り通行料を徴収する
- 「移動」コマンドでの侵略はもともとプレイヤーの駒がその土地に立っていないため、勝敗に関わらず通行料の対象外（変更なし）

### ショップマス（先行実装済み）
- 止まると図鑑からランダムに3枚（コストのあるモンスター・アイテムのみ。スペルはコスト概念が無いため対象外）を提示。選んだカードは2回点滅→「「◯◯」を購入しますか？ コスト◯G」ではい/いいえ確認→はいで即座に手札に加わる（いいえの場合は3枚の選択画面に戻る）
- **ここで買ったカードはその対戦中だけ手札に存在する一時的なもので、`character.ownedCards`（永続所持枚数、フェーズ3のデッキ編集で使うもの）には一切影響しない**。対戦が終われば（退出しても）自然に消える。ハブの「ショップ」画面（永続所持カードの売却）とはコード上も完全に別系統
- CPUはショップマスに止まっても何もしない

### バトルルール（全面刷新済み・盤面の侵略コマンド／移動コマンドの侵略から実際に呼び出される）
- 属性の弱点サイクル: 火→水→雷→森→火（各属性は次の属性に弱い＝1.2倍ダメージ）。無属性（無色とは別概念、モンスター専用）はこのサイクルの対象外で、弱点も特攻もない
- **戦闘は同時ではなく順番制**: 先制・後攻などの特殊能力が無い限り、侵略側（攻撃側）から先に攻撃する。防御側は攻撃側の一撃で死亡した場合、反撃する機会がないまま戦闘終了（＝攻撃側は無傷）。防御側が生き残った場合のみ、続けて反撃する。この結果、通常戦闘では「相打ち（両者死亡）」は起こり得ない（将来、反撃を待たずにダメージを与えるような特殊能力が実装されれば別）
- 侵略の勝敗判定: 防衛側が死亡＆侵略側が生存 → 侵略側が土地を奪取（「〇〇の□□は土地を奪った」と表示）。それ以外（防御側が生存、または両者死亡）→ 防御側が土地を守った扱い（「〇〇の□□は土地を守った」と表示）
- 武器・防具（総称「アイテム」）は装備先モンスターのATK/HPに加算されるが、勝敗を問わず戦闘後に消滅する（一部復活するものを今後実装予定）
- モンスターにかけるスペルは「呪い」と呼び、時限的なもの以外は永続。加算ATK/加算HPとして基礎値に上乗せされ、モンスターが戦闘で死亡した時点で消滅する
- **同属性ボーナス（先行実装済み）**: 自分の土地に配置されているモンスターは、その土地の属性が自分の属性と一致していれば、土地レベル×10（最大50）のHPが加算される。所有権がある（＝配置されている）モンスターにのみ適用され、手札から召喚したばかりの侵略側にはこのボーナスは無い（移動コマンドで元の土地から侵略する場合は元の土地基準で判定される）
- **応援ボーナス（先行実装済み）**: 戦闘が発生した土地に隣接するマスに、その戦闘に参加しているモンスターと同じ所有者の別モンスターが配置されていれば、ATK+10。攻撃側・防御側どちらにも同じ判定を独立に適用する（自分自身は対象外）
- 初期実装モンスター: サラリーマンダー（火, ATK30/HP20, コスト50, 土地に生き残ったら残HP×4ゴールド獲得）、港区女子（水, ATK15/HP30, コスト45, 弱点属性からの被ダメージ1.2倍・それ以外は0.8倍, 与えたダメージと同額のゴールドを対戦相手から奪う）
- 初期実装アイテム: ナイフ（武器, コスト5, ATK+10）、なべのふた（防具, コスト5, HP+10）
- 初期実装スペル: マ〇ジャロ（対象モンスターに加算ATK+10・加算HP+20の呪いを付与、永続）
- **特殊効果（先行実装済み・`unit.def.traits`配列で保持、現状はブリードモンスターのパーツのみが付与元）**:
  - **先制**: この特性を持つ側が防御側の場合、通常と逆に防御側から先に攻撃する（攻撃側も先制を持っていれば通常通り攻撃側が先）
  - **被ダメージ半減**: この特性を持つ側への被ダメージが0.5倍になる（攻撃側が貫通を持つ場合は無効化される）
  - **貫通**: 攻撃側がこの特性を持つ場合、防御側の同属性ボーナス（土地加算HP）を0として扱い、防御側の被ダメージ半減も無効化する。アイテムによるHP加算は無効化できない
  - **不死鳥**: この特性を持つユニットが死亡すると、カードが（新しいidで）持ち主の手札に戻る。これにより手札が7枚になった場合は通常の手札上限処理と同じ「捨てるカードを選ぶ」画面に移行し、捨てたカードはもう戻ってこない
  - **強盗**: この特性を持つ側の攻撃が成功（ダメージ>0）すると、与えたダメージ×3のゴールドを相手の手持ちから奪い自分（使用者）に還元する（港区女子の同種能力は×1）

### バトルフェーズの演出（全面刷新済み）
侵略が発生すると盤面画面から専用のバトルフェーズ画面へ遷移する。
1. **暗転して移行**: 画面が暗転し、攻撃側のカードを画面左、防御側のカードを画面右に配置する
2. 各カードの左上・右上に所有者名、HP、ATKを表示。ラベルは単に「HP」「ATK」（「基礎HP」とは表記しない）。応援ボーナス（ATK）・同属性ボーナス（HP）が乗っている場合のみ、基礎数値の横に「＋◯◯」と表示（両ボーナスとも、この時点で確定しているため先に見せられる。アイテムの加算分はまだここには出ない）。**カード下部には有利/不利属性のラベルも表示**（弱点サイクル上、自分が相手に1.2倍ダメージを与えられる側なら「有利！攻撃1.2倍」、逆に相手から1.2倍を受ける側なら「不利…被ダメージ1.2倍」、どちらでもなければ非表示）
3. **アイテム選択（攻撃側→防御側の順、非公開）**: まず攻撃側の手札（アイテムのみ）が表示され、1枚選ぶか「使わない」を選択。選んだ瞬間の視覚的な演出（点滅等）は無く、防御側の画面には攻撃側が何を選んだか一切表示されない。攻撃側の選択が終わってから防御側の手札が同様に表示され、選び終えたら戦闘開始。選んだアイテムは戦闘後、勝敗を問わず手札から消費される（永続所持カードではなく、その場で使い切り）
4. **攻撃実行**: 特殊能力がある場合はダメージ計算前に発動することがある（今後の拡張用）。先制/後攻の特殊能力が無ければ攻撃側から攻撃: 使ったアイテムがあれば攻撃側カードの左上に1/4サイズで重なって表示され、ダメージ計算メッセージ（「◯◯→△△に31ダメージ（倍率1.2）」形式）を表示。**メッセージは1.5秒間表示したままにする**。この一撃で防御側が死亡した場合、防御側のカードが下から崩れ落ちるエフェクトを出す（このメッセージの1.5秒の間に発生）。防御側が生存していれば、続けて防御側の攻撃フェーズが同様に発生（アイテム重畳・メッセージ表示・1.5秒待機・特殊効果は同様、攻撃側が死亡すれば同じ崩れ落ちエフェクト）。**両者とも生存した場合（引き分け）は、決着メッセージの前に両陣営のカードをそれぞれ自分の側の画面外へ退避させる**演出を挟む
5. **決着メッセージ（1.5秒表示）**: 攻撃側が勝った（防御側死亡・攻撃側生存）場合「〇〇の□□は土地を奪った」と表示し、フェードアウトして盤面へ戻る。奪った土地は**レベル・属性をそのまま引き継ぎ**、攻撃側モンスターがそこに残る。ただし戦闘で受けたダメージ分そのまま（基礎HPが全快せず減ったまま）配置される。それ以外（防御側が勝った/引き分けた）場合は「〇〇の□□は土地を守った」と表示。通行料の扱いは上記「通行料」節を参照
- 上記5種の実データカードはキャラメイクで選んだ初期ブック（下記フェーズ3参照）にそれぞれ4枚ずつ混入済み。それ以外の汎用プレースホルダーモンスターにも仮のATK/HP/コストを付与し、どのモンスターカードも召喚可能にしてある

### 土地の属性・レベル・資産価値（先行実装済み）
- 土地の属性は画面上の4象限（斜めカメラで実際に見える上下左右）で固定: 上＝火、右＝水、下＝森、左＝雷。周回コースの各辺がちょうど1属性に収まるよう、ワールド座標の対角線（x-z, x+z）で判定している
- **無色（薄いグレー）属性**も存在する。連鎖しない（連鎖倍率は常に×1.0）特殊な属性で、周回コースの角（4象限の境界にあたるマス、スタートマスを除く）に盤面生成時から配置される
- 土地コマンドの「土地属性変更」で、所有地の属性はいつでも変更可能（上記の4属性＋無色から、現在の属性以外を選択）。コスト＝現レベル×100G（対象が無色の場合は半額）
- 土地はレベルを持ち、初期Lv1・カルドセプト本家準拠でLv5上限
- **地価・通行料はカルドセプト準拠の計算式**（本家に「土地の売却」はなく、資産価値＝将来の通行料徴収ポテンシャルとして評価される）
  - 地価 = 基本地価（全マス共通150G固定） × レベル倍率（Lv1=×1〜Lv5=×5） × 連鎖倍率（同属性の所有地数によるボーナス、カルドセプト ビギンズ系の数値表: 連鎖1=×1.0／2=×1.5／3=×2.0／4=×2.5／5=×3.0）
  - 通行料 = 地価 × 通行料倍率（Lv1=0.3／Lv2=0.3／Lv3=0.4／Lv4=0.6／Lv5=0.8）
  - レベルアップのコストは地価に連動しない固定値: Lv1→2=50G／Lv2→3=200G／Lv3→4=400G／Lv4→5=600G
  - プレイヤーの総資産Gは「所持G＋所有地の地価合計」。レベルアップや連鎖（同属性の追加所有）で地価・通行料の両方が上がる
  - 現状の盤面は1周回コース＝属性ごとに1エリアなので、「エリア」は実質「属性」と同義（同じ属性の所有地＝1連鎖として数える）。将来、分岐や複数エリアを持つマップを追加する場合はエリアIDをマスに持たせて連鎖判定を分離する必要がある

### 進行方向・分岐選択（先行実装済み・分岐盤面に合わせて全面刷新）
- 盤面がグラフ構造になったため、「ゲーム開始時に1回だけ方向を決める」旧方式は廃止。**移動中に分岐マス（隣接候補が2つ以上）へ差し掛かるたびに毎回選択を求める**方式に変更（`Game._movePlayer`/`_chooseNextTile`）。1回のサイコロ移動の途中で複数回選択が発生することもある
- 選択UIは画面中央に斜め矢印（↖↗↙↘、最大4方向）が候補の分だけ表示され、1回タップで即決定（頻繁に発生するため、以前の「2回タップで確定」方式は廃止）。同時に土地情報と同じカメラワーク（上下左右パン、戻るボタンは非表示）も使えるので、分岐先を見てから選べる
- どの分岐先がどの矢印に対応するかは、盤面のグリッド座標の差分（世界座標の±X/±Z）を、斜めカメラでの実際の画面方向に変換して決めている（+X→↘／-X→↖／+Z→↙／-Z→↗）
- CPUは分岐でランダムに1つを選ぶ
- 分岐でない（隣接候補が1つだけの）マスは従来通り自動で通過する

## プレイヤー情報UI（先行実装）
- 画面四隅にプレイヤー情報パネル（アイコン・名前・所持G・総資産G[所持金+所有土地の評価額合計]・召喚数[所有マス数]）を表示。各プレイヤーの色で縁取り
- 最大4人対戦を想定した配置ロジック: 同盟戦でない場合はターン順に左上→右上→左下（左上の下）→右下（右上の下）。同盟戦の場合は最初のターンのプレイヤーのチームが左列（リーダー左上、同盟者は左下）、もう一方のチームが右列という配置
- ターン表示（「〇〇のターン」）は左上ではなく、中央のサイコロの右に大きめの文字で表示

## 同盟戦（ルール確定・ロジックは実装済み、対戦モード自体は未実装）
`player.allianceId`が同じ2人が組む2vs2相当のチーム戦。**4人対戦を開始する画面・フローがまだ無い**ため現状は常に`allianceId: null`同士（＝実質何も変わらない）だが、`Game`側のロジックは`allianceId`を見て以下のように自動で切り替わるよう実装済みなので、同盟戦の開始フローが実装され次第そのまま機能する。
- **仲間の土地では通行料を取られない**（`_settleLandingToll`が`tile`の所有者と着地したプレイヤーの`allianceId`が一致する場合は徴収をスキップ）
- **同属性の連鎖ボーナスは仲間の所有地も合算**して数える（`_chainMultiplier`。例: 自分が火の土地を1つ、仲間が火の土地を1つ持っていれば連鎖2として計算される）。**土地の所有権自体はあくまで個人**のまま（`tile.owner`・`_ownedTiles`は仲間分を含めない）で、連鎖カウントの合算だけが仲間戦の特典
- **総資産（画面表示）は2人の合計**になる（`_totalAssetsOf`。所持G＋地価の合計を両プレイヤーの「総資産」欄に同じ値で表示）。ただし「所持G」欄自体は個人のまま別々に表示される
- **強制停止（未実装の将来効果 - 指定マスに強制的に相手を止まらせる）にも仲間は巻き込まれない**想定。効果自体がまだ無いため、実装時にこのルールを反映すること
- **手持ちがマイナスになった際、パートナーの土地を代わりに売ることはできない**想定（「土地を売る」自体が未実装のため、実装時にこの制約を反映すること。破産処理は下記参照）

## 破産処置（先行実装済み）
所持Gがマイナスになったプレイヤー（自分のターンの行動で通行料や略奪特性等により発生しうる）は、そのターンの行動が全て終わった時点（`rollDice`の最後、次のプレイヤーに手番が移る直前）で自動的に**500Gを渡され、ゴール地点（スタートマス）へ強制的に再スタート**する（`Game._checkBankruptcy`）。**デッキ・手札の中身はそのまま**（何も失わない）。現在の操作プレイヤーだけでなく、そのターン中に（侵略の略奪効果などで）マイナスになった可能性がある全プレイヤーを毎ターン終わりにチェックする。土地の所有権はこの処理だけでは失われない。

## フェーズ3: ブック編成（デッキ編成）（骨組み実装済み）
- プレイヤーは「カード図鑑」（全カード一覧）から選んで40枚の「ブック」を編集する
- 1種類のカードにつき最大4枚まで、かつ**所持枚数を超えて編成不可**。合計ちょうど40枚になるまで保存不可（`MAX_COPIES_PER_CARD`/`DECK_SIZE`, `main.js`）
- カード名をタップするとイメージ（カード自体の色付き表示を拡大したもの。個別のカードイラスト素材はまだ無い）と説明（種別・レア度・属性・ステータス）のポップアップが開く（既存の`card-detail-modal`を流用）
- 複数ブックの保存・切り替え（実装済み・2026-08-10）: 最大3冊、詳細は後述の「複数デッキ（ブック）スロット」セクション参照

### カード所持・レアリティ・ショップ（骨組み実装済み）
- **所持枚数の概念**: `character.ownedCards`（カード名→枚数）。キャラメイク時に選んだ初期ブックの40枚がそのまま初期所持カードになる。デッキ編集の「＋」は所持枚数と4枚のどちらか小さい方が上限
- **レアリティは4種**: N・S・R・EX（`cards.js`の`Rarity`）。カード左上に表示。現状の割り当ては、ネームドモンスター2種=R、ネームドアイテム2種=N、ネームドスペル1種=S、汎用プレースホルダー全部=N（EXは現状未使用、今後の強力な実装カード用に予約）
- **種類アイコン**をカード右上に表示（`TYPE_ICON`）: モンスター=👹、スペル=⚡、武器防具=⚔️
- **ショップ画面**（実装済み・Codexが担当）: 所持カードのうち「所持数－ブック編成数」の余りがあるものだけ売却可能。売却額はN=15G／S=30G／R=100G、EXは売却不可（`RARITY_SELL_PRICE`）。売却すると`character.m`（永続所持M）に加算される。**パック購入も実装済み**（`src/shopPacks.js`、属性別モンスターパック×4＋アイテム／スペル／無属性パック、各100M、最低1枚S以上を保証する排出ロジック）

### 属性・カード規模（方針確定・コンテンツは今後拡充）
- 属性は5種: 火・水・雷・森・無属性（`cards.js`の`Element`）。弱点サイクルは火→水→雷→森→火（各属性は次の属性に1.2倍ダメージを受ける）。無属性のモンスターは弱点も特攻も無い代わりに、同属性ボーナス（土地属性とモンスター属性が一致すればHP加算、先行実装済み・詳細は上記バトルルール参照）も受けられない。土地属性とモンスター属性を同じEnumに統一したのはこのメカニクスのため
- 想定カード規模: モンスターは各属性20種（無属性含む）、アイテム30種、スペル40種くらい。現状は名前付きの実データが5種（モンスター2・アイテム2・スペル1）＋汎用プレースホルダー（`buildCardPool`、既定で計40種）のみで、本格的なコンテンツ拡充は今後
- **初期ブック（スターターブック）は2種**（`STARTER_DECKS`, `battleCards.js`）: 火・森ブック（サラリーマンダー軸）／水・雷ブック（港区女子軸）。それぞれ該当2属性に寄せて汎用モンスターも生成される（`Deck`の`elements`オプション）ので、単に名前付きカードが違うだけでなく実際に構成が偏る
- `src/cardCatalog.js`の`getCardCatalog()`が図鑑の中身（名前付きカード＋汎用プレースホルダー）を返す。デッキ編集画面はカード名で名寄せ・枚数管理する（`cardKey` = `card.name`。理由: 図鑑の生カード定義には`catalogId`が無く、汎用カードの`id`は生成のたびに変わるため、両者を安定して突き合わせられるのは`name`だけ）。唯一の例外がブリードモンスターで、いつでも改名できるため`catalogId==='breedMonster'`を優先的にキーとして使う（名前が変わってもデッキ内所持カウントが迷子にならないようにするため）。デッキ編集画面は`getCardCatalog()`固定のキャッシュとは別に、キャラクターごとの最新ブリードモンスター定義（`buildBreedCardDef`）をその場で合成した一覧を見る

## フェーズ4: カード収集・ガチャ・通貨・ブリードモンスター
- CPU対戦・対人戦の勝利で通貨を獲得（対人戦は難易度が高い分、報酬を厚めに設定）
- 通貨でパックガチャを購入し、新規カードを入手（**実装済み・Codexが担当**、`src/shopPacks.js`。詳細は上記フェーズ3のショップ画面の項を参照）
- レアリティ設計は先行実装済み（N/S/R/EX、上記フェーズ3参照）
- 通貨・ガチャ処理はクライアント改ざん防止のためサーバー側（Firebase Functions）で処理する
- **ブリードモンスター（実装済み・`src/breedParts.js`）**: ガチャ入手カードとは別に、プレイヤーが自分で育てた（ブリードした）オリジナルモンスター1体を`character.breedMonster`として持つ。初期値は名前「ブリモン」・無属性・ATK15・HP15・召喚コスト30G。「ブリード」画面でいつでも**改名**でき、**画像も登録**できる（カードエディタの画像アップロードと同じ仕組み: `FileReader`でdata URL化し`breedMonster.imageDataUrl`に保存、`buildBreedCardDef`経由でカード表示に反映されるので、図鑑・デッキ編集・対戦中の手札などどこでも自作画像で表示される）。名前・画像どちらも純粋に見た目の要素で、ステータス（ATK/HP/コスト/属性/特殊効果）はパーツ構成のみで決まる
  - **レアリティEX・初期デッキに1枚**: キャラメイク時に`buildBreedCardDef()`でその時点のブリードモンスターをレアリティEXの実在カードとして生成し、汎用モンスターを1枚外して初期40枚デッキに差し替える（`character.ownedCards`にも1枚として計上）。デッキ編集画面にも常に1行として表示され（`getCardCatalog()`の静的キャッシュとは別に、その場でブリードモンスターの最新ステータスを合成した一覧を見る）、0枚か1枚かの出し入れができる
  - **パーツの入手（実装済み）**: ショップ画面「ブリードパーツ購入」セクション（Codexのパック購入・カード売却セクションの間に追加）で、`BREED_PARTS`各パーツをMで購入すると`character.ownedPartIds`に追加される。レアリティ帯ごとの価格目安はN=40M/S=60M/R=90M（`BREED_PARTS`の`price`フィールド）。購入済みパーツは「購入済み」表示で再購入不可。「ブリード」画面では所持パーツの装着／取り外しを自由に切り替え可能（装着・取り外し自体は無料・何度でも可逆、購入だけが一度きりのM消費）
  - **初期実装パーツ11種**（`BREED_PARTS`。costDeltaはブリードモンスター自身の召喚コスト(G)への加算、priceがショップでのM購入価格）:
    - 属性パッチ（S, コスト+30G, 無色を除く4属性から装着時に選択・上書き）
    - ATKアップ（N, コスト+20G, ATK+10）／HPアップ（N, コスト+20G, HP+10）
    - ダブルアップ（R, コスト+35G, ATK+10・HP+10）
    - 攻撃特化（N, コスト+20G, ATK+20・HP-10）／防御特化（N, コスト+20G, HP+20・ATK-10）
    - 先制付与（S, コスト+30G, 特性「先制」）／被ダメージ半減（R, コスト+60G, 特性「被ダメージ半減」）／貫通（R, コスト+70G, 特性「貫通」）／不死鳥（S, コスト+40G, 特性「不死鳥」）／強盗（S, コスト+30G, 特性「強盗」）
    - 特性の実際の戦闘での挙動は上記バトルルール節の「特殊効果」を参照
    - 「ブリード」画面のパーツ一覧は質素なアイコンバッジで表示（⚔️=ATK、🛡️=HP、💰=コスト(G)、🎨=属性選択、特殊効果ごとに1文字絵文字（⚡先制／💠被ダメージ半減／🏹貫通／🔥不死鳥／🥷強盗）+ラベルテキスト）。`breedPartBadges()`が{icon, text}の配列を返す
  - **装着上限**: ATK・HP・召喚コストそれぞれ独立に上限（ATK70/HP70/コスト200）を判定し、いずれか1つが上限を超える場合はそのパーツを装着できない（他のステータスがまだ余裕があっても、その特定のパーツだけがブロックされる。例: HPが上限でもコストのみ変動するパーツは装着可）。装着した結果HPまたはATKが0以下になる場合もエラーメッセージを表示して装着を拒否（`canEquipPart`が理由付きで判定、装着ボタン自体も事前に無効化される）

## アセット（実素材の反映状況）
- ステージ背景: `public/images/stage/stage1.png`（海底イラスト）を盤面の地面（ground plane）テクスチャとして適用済み（`scene.js`）
- キャラアイコン: `public/images/player/icons6.png`（3×2グリッドの魚＋うさぎ6種シート）を`src/iconSheet.js`がブラウザ側でCanvas分割し、6枚の個別アイコンとして扱う（サーバー側の画像加工は不要）。キャラメイクのアイコン選択と、盤面上の自キャラ駒（ビルボードスプライト）の両方に使用
- **既知の問題**: `icons6.png`の各アイコンの「透明に見える」市松模様部分は、実際には透過（アルファチャンネル）ではなくピクセルとして描き込まれた市松模様そのもの。そのため盤面の駒やキャラメイクのアイコンにもこの市松模様がそのまま表示される。本当に背景を透過させたい場合は、元画像を実際にアルファ抜きした状態で差し替える必要がある
- **BGM（2026-08-11〜12実装）**: `src/audio.js`は以前Web Audio APIで音声ファイル無しにプロシージャル生成していたが、そのコードは全て削除し、`HTMLAudioElement`でユーザー提供のMP3実音源（`public/audio/`）を再生する方式にした（`playBoardTheme()`/`playBattleTheme()`/`stopMusic()`/`setMuted()`等のエクスポート名は変更していない）。3曲構成:
  - `board-theme.mp3`: 通常ステージの盤面用
  - `battle-theme.mp3`: バトルシーン用（30秒ループ想定）
  - `boss-theme.mp3`: ④ダンボール男戦（ラスボス）専用の盤面BGM。`main.js`が現在の対戦の`mapId`を`currentMapId`というモジュール変数で覚えておき、盤面BGMを鳴らす箇所（バトル開始時・バトルシーン終了後の3箇所、全て`playBoardOrBossTheme()`に統一）で`mapId==='danball'`ならボス曲、それ以外なら通常の盤面曲を選ぶ。バトルシーン自体（`playBattleTheme()`）は専用のボス戦闘曲が無いため通常のまま
  - PvPで④のマップを選んだ場合もゲスト側含め同じ分岐が効く（`pvpLastRoom.mapId`から`currentMapId`を設定）
- **ストーリー名前付きNPCの立ち絵・盤面駒（2026-08-12実装）**: `src/npcArt.js`が名前文字列（story.jsの`speaker`/opponent`name`と完全一致）から実素材のURLを引く小さなルックアップ表。対応済み5キャラ: ダンボール男・暴君マダイ・ニュウドウカジカ（お肉）・紫の魔女ホフク・某不思議の国の少女（ヒトデ・農家のウサギ等は未対応でプレースホルダーのまま）
  - **立ち絵**（`public/images/npc-portraits/`）: 全身立ち絵、背景透過PNG。`#story-dialogue-screen`に追加した`#story-dialogue-portrait`が、`playDialogueLines`で1行進むたびに`speaker`名からルックアップして表示/非表示を切り替える（該当が無ければ非表示のまま、今まで通りテキストのみの表示）
  - **盤面駒**（`public/images/npc-tokens/`）: 256×256の正方形、キャラごとの縦横比を保ったまま底辺揃えで配置済み（`GameScene.createPieceFromImage`が前提とする1.6×1.6の正方形スプライトに合わせた設計）。`Game`の駒生成を汎用化 - 以前は人間プレイヤーの`humanIconImage`だけが特別扱いだったが、`player.iconImage`があれば（CPU/人間問わず）`createPieceFromImage`を使うよう統一した。`main.js`の`buildBattlePlayerConfigs`が非同期になり（`npcArt.js`の`loadNpcTokenImage`がpromise）、対応キャラのally/opponent configにだけ`iconImage`を設定する
  - 立ち絵・盤面駒とも元はAI生成の「フィギュア＋カードのモックアップ」6枚1組の合成画像から、背景（白/薄灰色）をflood-fillで透過にし、隣のキャラの絵が数px混入していた箇所を手作業で追加トリミングして作成した（`rembg`のような汎用背景除去だと枠やカード台座まで一体化していて使えなかったため、単色背景の別バージョンをユーザーに作り直してもらう方針にした）
- **カード共通プレースホルダー画像（2026-08-12実装）**: `src/cardArt.js`の`defaultCardArtUrl(card)`が、カードに`imageDataUrl`（カードエディタで個別に設定した画像）が無い場合のフォールバック画像を種別・属性から返す（`public/images/card-art/`、モンスターは属性ごとに5種、武器防具・スペルはそれぞれ1種の計7枚）。1枚ずつ個別に画像を用意するのは非現実的という判断で、当面は全カード共通のこのフォールバックを使う（`renderCardEl`一箇所に集約してあるので、カード一覧・デッキ編成・手札・カード詳細ポップアップ・バトルシーン等どの画面にも自動で反映される）。将来カードごとの専用素材ができたら、そのカードに`imageDataUrl`を持たせるだけで自然に上書きされる
- **マップごとの背景画像を切り替え可能に（2026-08-12実装）**: `board.js`の`MAPS`各エントリに`background`フィールドを追加（②マダイの岩礁だけ実素材の`stage2.jpg`、他3マップは共通の`stage1.png`のまま）。以前は`#app`のCSS背景が静的な1枚固定だったが、`main.js`の`applyMapBackground(mapId)`が対戦開始のたびに`getMapBackground()`を引いてインラインstyleで上書きするようにした（`currentMapId`を設定する2箇所＝`startBattle`/`startPvpGuestBattle`と同じタイミングで呼ぶ）
- **タイトル画面の刷新（2026-08-12実装）**: ログイン画面に実素材のロゴ（`public/images/title-logo.webp`、背景の黒をflood-fillで透過化）と背景イラスト（`public/images/title-background.jpg`、暗めのグラデーションを重ねて可読性を確保）を反映。文字だけの`<h1>`タイトルは廃止（ロゴ画像に文字が入っているため）。ゲーム紹介文を4項目（盤面パート・ストーリー＆対人戦・キャラメイク・カード収集）追加し、盤面パートには実際の対戦画面のスクリーンショット（`public/images/title-screenshot.jpg`）も添えた。ログイン画面はBGM無し（意図的）
  - **スクリーンショットの取得方法についての注記**: このセッションはBrowserペインが終始`document.hidden=true`で`requestAnimationFrame`が発火しない状態だったため、通常の方法では画面を撮影できなかった。`window.requestAnimationFrame`をブラウザのコンソールから`setTimeout`ベースの実装に一時的に上書き（Node側のポリフィルと同じ発想）することで、WebGLキャンバス自体は`document.hidden`に関係なく描画できることを利用し、`canvas.toDataURL()`で実際の盤面描画を直接キャプチャした。それをCSSの盤面背景画像とその場でCanvas 2D合成し、実際のゲーム画面に近いスクリーンショットを作成した（HUD等のDOM/CSS部分は同じ手法が使えず含まれていない）

## フェーズ5: 対戦モード
- Firebaseを使ったリアルタイムマルチプレイ
- 盤面状態・ターン進行・プレイヤー状態の同期

---

## データ構造（叩き台）

```
users/{userId}/
  ├─ currency: number
  ├─ ownedCards: [カードID配列]
  ├─ decks/
  │   ├─ deck1: { name: string, cards: [カードID配列] }
  │   └─ deck2: { name: string, cards: [カードID配列] }
  └─ matchHistory: [対戦履歴配列]

cards/{cardId}/
  ├─ name: string
  ├─ rarity: string（コモン/レア/激レア等）
  ├─ imageUrl: string
  └─ stats: {...}（攻撃力・防御力等、フェーズ2で詳細設計）
```

## アセット・容量について
- カードイラストは1枚ずつ用意（WebP形式、圧縮推奨）
- 100〜300枚規模でもFirebase Storage無料枠（5GB）内に収まる想定
- ダウンロード量対策としてブラウザキャッシュを効かせる設計にする

## 参考にしている前作からの変更点
- プレイヤー同士の直接バトルを廃止し、モンスターバトルのみに一本化
- 最初にデッキを選ぶ／自由に編成する形式を新規導入
- カード収集・ガチャによる成長ループを新規導入
- 土地の獲得を「購入」から「モンスター召喚」に変更。購入コストは廃止し召喚コストのみ。他プレイヤーの土地への侵略も同じ仕組み
- ブック（デッキ）編集にカード収集の楽しみを持たせる。あわせて、自分で育てるブリードモンスターをブックに編成できる仕組みを新規導入
