# チヌクエスト2 開発仕様書

## 概要
カルドセプト風の3Dボードゲーム。プレイヤー同士の直接対決バトルは廃止し、モンスターとのバトルに一本化。カード収集・デッキ編成・ガチャによる成長要素を持つ。

## 技術スタック
- **描画**: Three.js（WebGL、3D表示）
- **データ同期・保存**: Firebase（Realtime Database or Firestore、Authentication、Storage、Functions）
- **配信形態**: PWA（Progressive Web App）
  - manifest.json + Service Worker
  - ホーム画面に追加してフルスクリーン起動、AppStore審査不要
- **対応画面**: スマホ横画面固定を想定

## 開発フェーズ（この順で実装）
0. ログイン／キャラメイク／モード選択ハブ
1. 盤面移動＋マス目イベント（シングルプレイ、CPU相手）
2. モンスターバトルシステム
3. デッキ編成画面
4. カード収集・ガチャ・通貨システム
5. 対戦モード（マルチプレイ）

現時点ではフェーズ0の骨組み（ログイン〜対戦(CPU)まで）とフェーズ1〜2を実装済み。動作確認しながら段階的に進める。

---

## フェーズ0: ログイン・キャラメイク・モード選択ハブ（骨組み実装済み・ローカル仮認証）

### 初回起動フロー
1. **ID・パスワードで新規登録 or ログイン**（Firebase Authenticationを想定）
2. 初回ログイン時のみ**キャラメイク**: 好きなキャラ画像を選ぶ（盤面上の自キャラアイコンになる。現状の丸いプレースホルダー色アイコンに代わるもの）→ 名前を決定 → 初期デッキを2種類から選択

### 2回目以降のログイン
- ログイン後はいきなり対局に入らず、**モード選択画面**（ハブ画面）を経由する
  - **ストーリー** — 1人プレイでストーリーを進める（新規モード。シナリオ設計は今後詳細化）
  - **対戦** — CPU戦または対人戦（対人戦はフェーズ5のオンライン対戦に相当）
  - **デッキ** — カード図鑑から選んで40枚のデッキを編集する（フェーズ3のブック編成に相当。骨組み実装済み、詳細はフェーズ3参照）
  - **ショップ** — 所持Gでパックを購入。パックは複数種類あり、レアリティごとに排出率が異なる（フェーズ4のガチャに相当）
  - **ブリード** — 所持Gを使ってブリードモンスターのステータス・特殊能力をセットする（フェーズ4のブリードモンスターに相当、今回「お金を使ってステータス構成を組む」という具体的な操作方法が確定）

### 既存フェーズとの対応関係
このハブ画面は独立した新規要件ではなく、フェーズ3〜5の入口を1つの画面にまとめたもの。「対戦」の中の“CPU戦”が現在実装済みの盤面ゲームそのものにあたる。実装順としては、まずログイン〜モード選択〜対戦(CPU)の骨組みを通してから、デッキ／ショップ／ブリード／ストーリーを順次埋めていく想定。

### 実装状況（骨組みのみ）
- `src/auth.js`: ローカル仮認証（localStorageにID/パスワード/キャラ情報を保存するだけ、Firebase未接続）。`loginOrRegister(id, password)`で新規登録とログインを兼ねる、`saveCharacter(id, character)`でキャラ情報を保存
- キャラメイク画面: アイコンは実素材がまだ無いため色スウォッチ6色から選ぶプレースホルダー（盤面のコマ色になる）。名前入力（10文字まで）。初期デッキは`battleCards.js`の`STARTER_DECKS`（火・森ブック／水・雷ブックの2種、下記フェーズ3参照）から選択
- モード選択ハブ: 「対戦」と「デッキ」が実際に機能する。CPU戦を選ぶと`main.js`の`startBattle(character)`が呼ばれて既存の盤面ゲームが開始する（対人戦ボタンは無効化表示）。ストーリー／ショップ／ブリードは「準備中です」のスタブ画面（戻るボタンのみ）
- `game.js`の`Game`コンストラクタに`humanPlayer: { name, color, deckVariant, deckList }`を渡せるようにし、盤面ゲーム側の自キャラ名・コマ色・デッキがキャラメイク（またはデッキ編集）の選択を反映するようにした

---

## フェーズ1: 盤面・移動システム

### カメラ・ビュー
- カルドセプト風の見下ろし視点（斜め上からのアングル）
- コマが1マス移動するごとに、カメラもなめらかに追従（線形補間でスライド）
- コマが停止した瞬間、カメラも静止。イベント発生時はズームイン等の演出を想定（フェーズ1では簡易でよい）

### 盤面
- 最初は周回コース（分岐なしのシンプルな形）から開始
- マス数は仮に16〜20マス程度から着手し、後で調整可能な設計にする
- マスごとに種類を持たせる（例: 土地マス、イベントマス、スタートマス）。将来的な拡張（地形バリエーション、分岐路）を見据えたデータ構造にする

### ゲームループ
以下の①②③の流れで実装済み（フェーズ2の土地コマンドを先行実装）。
- ① サイコロを振る → コマが1マスずつ進むアニメーション
- ② 特殊マス（ゴール＝スタートマス／チェックポイント＝イベントマス）ならその処理。敵の土地に止まった場合はここで通行料を自動精算
- ③ 止まったマスが土地なら「土地コマンド」メニュー（フェーズ2参照）
- ターン制（プレイヤー人数分を順番に処理。フェーズ1はCPU 1体との対戦を想定）

---

## 手札・ドローシステム（フェーズ1で先行実装済み）
ターンループの土台として、ブック（デッキ）3本柱に先行してドロー・手札まわりを実装した。

- ブックは40枚固定（モンスター4属性×6枚＝24枚、武器防具8枚、スペル8枚）
- カード種類: モンスター（4属性: 火・水・風・地、カルドセプトの地形属性に準拠）、武器防具（戦闘用）、スペル
- ゲーム開始時、各プレイヤーはブックからランダムに4枚ドローして初期手札とする
- 手札上限は6枚。ターン開始時に1枚ドローし、これが7枚目になった場合はどれか1枚を選んで捨てる
- 自分の番のドロー時はカード画像を大きくアップ表示してから手札に加える（CPUのドローは非公開情報として画面には出さない）
- ブックを1周（山札を引き切る）したら、捨て札を山札にシャッフルして戻す
- 手札表示は2系統: (1) 自分の手札は画面左下に常時小さく表示、自分だけいつでもタップしてカード情報を確認できる。(2) ターンが来たプレイヤーの手札は画面中央上部（サイコロの下）に、左下の2倍サイズで表示し、サイコロを振った瞬間に消える。CPU（対人戦なら相手）のターンでも中身をそのまま表示する（伏せない）。ただし左下の常時パネルは常に自分の手札のみで、相手の手札はそこには出ない
- スペルはターン毎に1回、サイコロを振る前だけ中央の手札から使用可能。使用すると即座に手札から消費され、効果発動演出のあと自動的にサイコロを振れる状態に戻る（実際のスペル効果はフェーズ2以降のバトル設計で詳細化）
- サイコロは目押し方式: 1回目のクリックでスピン開始（1〜6の目が高速で切り替わる）、2回目のクリックで停止を予約し、そこから0.7秒後に表示されていた目で確定する（クリックの瞬間に即決まらないスキル要素）。CPUも同じ`rollDice(steps)`経路で乱数を振るが、目押し操作はプレイヤーのみ
- CPUの意思決定（サイコロを振るまでの間、土地購入判断、捨て札選択）には毎回一呼吸の待ち時間を入れ、テンポが速すぎないようにする

## フェーズ2: モンスターバトルシステム
- CPU戦の強さ調整（初心者向けの難易度からスタートし、対人戦へのステップとする）
- **土地の獲得は「購入」ではなく「召喚」**: 空き地にモンスターカードを召喚することで自分の土地にする。土地購入コストは廃止し、召喚コストのみを消費する
- **侵略**: 他プレイヤーが所有する土地にも、召喚と同じコスト・ルールでモンスターを送り込み、バトルによって奪う（＝侵略）。召喚と侵略は同一の仕組み（対象が空き地か、敵の土地かの違いのみ）

### 土地コマンド（先行実装済み）
止まったマスが土地の場合、以下のメニューが表示される（ラベルにA/B/C等の記号は付けない）。
- **召喚／侵略／入れ替え** — 手札の（コストが払える）モンスターカードを選択（選ばれたカードは2回点滅。スペル・アイテム使用時も同じ演出）→「◯◯しますか？ コスト◯G」ではい/いいえ確認 → 空き地なら召喚、敵地ならバトル発生（戦闘結果は下記バトルルール参照）、自陣なら配置モンスターを入れ替え。実行したら自動でターン終了
- **土地レベルアップ** — 自分の所有地（今のマスに限らず盤面上のどこでも、Lv5未満のもの）から対象を選択。コストは固定値テーブル（下記）。レベルが上がると地価・通行料も上がる。実行したら自動でターン終了（「このターン通過した全マス限定」ではなく所有地全体が対象に拡張済み）
- **土地属性変更** — 自分の所有地から対象を選択→現在の属性を除いた4色から変更先を選択→「属性を◯◯に変更しますか？ コスト◯G」ではい/いいえ確認。コスト＝現レベル×100G（対象が無色の場合は半額）。実行したら自動でターン終了
- **土地情報の確認** — クリックすると盤面を自由に見渡せるカメラワーク画面に遷移。画面四隅に点滅する矢印ボタンがあり、押した方向にカメラがパン移動（ズーム・角度は固定）。任意のマスをクリックするとそのマスの情報（属性・レベル・所有者・配置モンスター・地価・通行料）がポップアップし、「閉じる」（カメラワーク継続）と「土地コマンドに戻る」（カメラ位置を元に戻して終了）の2択。右下の「戻る」ボタンでもいつでも終了できる。ターンは終了しない（土地コマンドメニューに戻る）
- 敵の土地に止まっていても、それとは別の自分の所有地をレベルアップ・属性変更するだけなら、止まったマスの通行料はいつも通り自動徴収される（侵略しなかった場合と同じ）
- CPUも同じ召喚/侵略ロジックを自動実行（レベルアップ・属性変更・情報確認は行わない）

### バトルルール（先行実装済み・盤面の侵略コマンドから実際に呼び出される）
- 属性の弱点サイクル: 火→水→雷→森→火（各属性は次の属性に弱い＝1.2倍ダメージ）。無属性（無色とは別概念、モンスター専用）はこのサイクルの対象外で、弱点も特攻もない
- 1戦闘＝同時1回攻撃交換（両者が同時にダメージを与え合う）
- 侵略の勝敗判定: 防衛側が死亡＆侵略側が生存 → 侵略側が土地を奪取。両者死亡 → 土地は無人に戻り、色も属性色に戻る。防衛側が生存 → 防衛成功（土地はそのまま）
- 武器・防具（総称「アイテム」）は装備先モンスターのATK/HPに加算されるが、勝敗を問わず戦闘後に消滅する（一部復活するものを今後実装予定）
- モンスターにかけるスペルは「呪い」と呼び、時限的なもの以外は永続。加算ATK/加算HPとして基礎値に上乗せされ、モンスターが戦闘で死亡した時点で消滅する
- 初期実装モンスター: サラリーマンダー（火, ATK30/HP20, コスト50, 土地に生き残ったら残HP×4ゴールド獲得）、港区女子（水, ATK15/HP30, コスト45, 弱点属性からの被ダメージ1.2倍・それ以外は0.8倍, 与えたダメージと同額のゴールドを対戦相手から奪う）
- 初期実装アイテム: ナイフ（武器, コスト5, ATK+10）、なべのふた（防具, コスト5, HP+10）
- 初期実装スペル: マ〇ジャロ（対象モンスターに加算ATK+10・加算HP+20の呪いを付与、永続）
- 上記5種の実データカードはキャラメイクで選んだ初期ブック（下記フェーズ3参照）にそれぞれ4枚ずつ混入済み。それ以外の汎用プレースホルダーモンスターにも仮のATK/HP/コストを付与し、どのモンスターカードも召喚可能にしてある

### 土地の属性・レベル・資産価値（先行実装済み）
- 土地の属性は画面上の4象限（斜めカメラで実際に見える上下左右）で固定: 上＝火、右＝水、下＝森、左＝雷。周回コースの各辺がちょうど1属性に収まるよう、ワールド座標の対角線（x-z, x+z）で判定している
- **無色（薄いグレー）属性**も存在する。連鎖しない（連鎖倍率は常に×1.0）特殊な属性で、周回コースの角（4象限の境界にあたるマス、スタートマスを除く）に盤面生成時から配置される
- 土地コマンドの「土地属性変更」で、所有地の属性はいつでも変更可能（上記の4属性＋無色から、現在の属性以外を選択）。コスト＝現レベル×100G（対象が無色の場合は半額）
- 土地はレベルを持ち、初期Lv1・カルドセプト本家準拠でLv5上限
- **地価・通行料はカルドセプト準拠の計算式**（本家に「土地の売却」はなく、資産価値＝将来の通行料徴収ポテンシャルとして評価される）
  - 地価 = 基本地価（全マス共通150G固定） × レベル倍率（Lv1=×1〜Lv5=×5） × 連鎖倍率（同属性の所有地数によるボーナス、カルドセプト ビギンズ系の数値表: 連鎖1=×1.0／2=×1.5／3=×2.0／4=×2.5／5=×3.0）
  - 通行料 = 地価 × 通行料倍率（Lv1=0.3／Lv2=0.3／Lv3=0.4／Lv4=0.6／Lv5=0.8）
  - レベルアップのコストは地価に連動しない固定値: Lv1→2=50G／Lv2→3=200G／Lv3→4=400G／Lv4→5=600G
  - プレイヤーの総資産Gは「所持G＋所有地の地価合計」。レベルアップや連鎖（同属性の追加所有）で地価・通行料の両方が上がる
  - 現状の盤面は1周回コース＝属性ごとに1エリアなので、「エリア」は実質「属性」と同義（同じ属性の所有地＝1連鎖として数える）。将来、分岐や複数エリアを持つマップを追加する場合はエリアIDをマスに持たせて連鎖判定を分離する必要がある

### 進行方向（先行実装済み）
- 自分（人間プレイヤー）の最初のサイコロを振って出目が確定した直後、盤面中央に斜め矢印（↙反時計回り／↘時計回り）が2つ表示される。1回目のクリックでその矢印が点滅（アーム状態）、同じ矢印をもう一度クリックすると確定し、その出目のぶんだけ選んだ方向へ移動する（CPUは常に時計回り＝既定方向、矢印は出ない）
- 選択はそのプレイヤーの`direction`（+1=時計回り／-1=反時計回り）に反映され、以後（2ターン目以降）は毎回そのまま使われる（再度選ぶ画面は出ない）
- 特殊カードによる進行方向反転は今後実装予定（`direction`を反転させるだけで対応できる設計）

## プレイヤー情報UI（先行実装）
- 画面四隅にプレイヤー情報パネル（アイコン・名前・所持G・総資産G[所持金+所有土地の評価額合計]・召喚数[所有マス数]）を表示。各プレイヤーの色で縁取り
- 最大4人対戦を想定した配置ロジック: 同盟戦でない場合はターン順に左上→右上→左下（左上の下）→右下（右上の下）。同盟戦の場合は最初のターンのプレイヤーのチームが左列（リーダー左上、同盟者は左下）、もう一方のチームが右列という配置
- ターン表示（「〇〇のターン」）は左上ではなく、中央のサイコロの右に大きめの文字で表示

## フェーズ3: ブック編成（デッキ編成）（骨組み実装済み）
- プレイヤーは「カード図鑑」（全カード一覧）から選んで40枚の「ブック」を編集する
- 1種類のカードにつき最大4枚まで、かつ**所持枚数を超えて編成不可**。合計ちょうど40枚になるまで保存不可（`MAX_COPIES_PER_CARD`/`DECK_SIZE`, `main.js`）
- カード名をタップするとイメージ（カード自体の色付き表示を拡大したもの。個別のカードイラスト素材はまだ無い）と説明（種別・レア度・属性・ステータス）のポップアップが開く（既存の`card-detail-modal`を流用）
- 複数ブックの保存・切り替えは今後（現状は1キャラにつき1ブックのみ、`character.deckList`としてローカル保存）

### カード所持・レアリティ・ショップ（骨組み実装済み）
- **所持枚数の概念**: `character.ownedCards`（カード名→枚数）。キャラメイク時に選んだ初期ブックの40枚がそのまま初期所持カードになる。デッキ編集の「＋」は所持枚数と4枚のどちらか小さい方が上限
- **レアリティは4種**: N・S・R・EX（`cards.js`の`Rarity`）。カード左上に表示。現状の割り当ては、ネームドモンスター2種=R、ネームドアイテム2種=N、ネームドスペル1種=S、汎用プレースホルダー全部=N（EXは現状未使用、今後の強力な実装カード用に予約）
- **種類アイコン**をカード右上に表示（`TYPE_ICON`）: モンスター=👹、スペル=⚡、武器防具=⚔️
- **ショップ画面**（骨組み実装済み・売却のみ、パック購入は未実装）: 所持カードのうち「所持数－ブック編成数」の余りがあるものだけ売却可能。売却額はN=15G／S=30G／R=100G、EXは売却不可（`RARITY_SELL_PRICE`）。売却すると`character.currency`（永続所持G、キャラメイク時300スタート）に加算される。現状は初期ブック＝初期所持カードが1:1で一致するため、実際に売れる余りが生まれるのはガチャ等で追加入手したカードがある場合のみ（そのガチャ自体は未実装）

### 属性・カード規模（方針確定・コンテンツは今後拡充）
- 属性は5種: 火・水・雷・森・無属性（`cards.js`の`Element`）。弱点サイクルは火→水→雷→森→火（各属性は次の属性に1.2倍ダメージを受ける）。無属性のモンスターは弱点も特攻も無い代わりに、後述の「同属性土地HP加算」ボーナスも受けられない（**未実装・今後の課題**: 自分のモンスターが自分と同じ属性の土地に配置されている場合にHPが加算されるボーナス。土地属性とモンスター属性を同じEnumに統一したのはこのメカニクスのため）
- 想定カード規模: モンスターは各属性20種（無属性含む）、アイテム30種、スペル40種くらい。現状は名前付きの実データが5種（モンスター2・アイテム2・スペル1）＋汎用プレースホルダー（`buildCardPool`、既定で計40種）のみで、本格的なコンテンツ拡充は今後
- **初期ブック（スターターブック）は2種**（`STARTER_DECKS`, `battleCards.js`）: 火・森ブック（サラリーマンダー軸）／水・雷ブック（港区女子軸）。それぞれ該当2属性に寄せて汎用モンスターも生成される（`Deck`の`elements`オプション）ので、単に名前付きカードが違うだけでなく実際に構成が偏る
- `src/cardCatalog.js`の`getCardCatalog()`が図鑑の中身（名前付きカード＋汎用プレースホルダー）を返す。デッキ編集画面はカード名で名寄せ・枚数管理する（`cardKey` = `card.name`。理由: 図鑑の生カード定義には`catalogId`が無く、汎用カードの`id`は生成のたびに変わるため、両者を安定して突き合わせられるのは`name`だけ）

## フェーズ4: カード収集・ガチャ・通貨・ブリードモンスター
- CPU対戦・対人戦の勝利で通貨を獲得（対人戦は難易度が高い分、報酬を厚めに設定）
- 通貨でパックガチャを購入し、新規カードを入手（**未実装**。売却側の`RARITY_SELL_PRICE`は先行実装済み、購入・排出率側はまだ無い）
- レアリティ設計は先行実装済み（N/S/R/EX、上記フェーズ3参照）。排出率の設計はガチャ実装時に詳細化
- 通貨・ガチャ処理はクライアント改ざん防止のためサーバー側（Firebase Functions）で処理する
- **ブリードモンスター**: ガチャ入手カードとは別に、プレイヤーが自分で育てて（ブリードして）育成したオリジナルモンスターをブックに編成できる仕組みを新規導入。育成ロジック・パラメータ設計は今後詳細化する

## アセット（実素材の反映状況）
- ステージ背景: `public/images/stage/stage1.png`（海底イラスト）を盤面の地面（ground plane）テクスチャとして適用済み（`scene.js`）
- キャラアイコン: `public/images/player/icons6.png`（3×2グリッドの魚＋うさぎ6種シート）を`src/iconSheet.js`がブラウザ側でCanvas分割し、6枚の個別アイコンとして扱う（サーバー側の画像加工は不要）。キャラメイクのアイコン選択と、盤面上の自キャラ駒（ビルボードスプライト）の両方に使用
- **既知の問題**: `icons6.png`の各アイコンの「透明に見える」市松模様部分は、実際には透過（アルファチャンネル）ではなくピクセルとして描き込まれた市松模様そのもの。そのため盤面の駒やキャラメイクのアイコンにもこの市松模様がそのまま表示される。本当に背景を透過させたい場合は、元画像を実際にアルファ抜きした状態で差し替える必要がある

## フェーズ5: 対戦モード
- Firebaseを使ったリアルタイムマルチプレイ
- 盤面状態・ターン進行・プレイヤー状態の同期

---

## データ構造（叩き台）

```
users/{userId}/
  ├─ currency: number
  ├─ ownedCards: [カードID配列]
  ├─ decks/
  │   ├─ deck1: { name: string, cards: [カードID配列] }
  │   └─ deck2: { name: string, cards: [カードID配列] }
  └─ matchHistory: [対戦履歴配列]

cards/{cardId}/
  ├─ name: string
  ├─ rarity: string（コモン/レア/激レア等）
  ├─ imageUrl: string
  └─ stats: {...}（攻撃力・防御力等、フェーズ2で詳細設計）
```

## アセット・容量について
- カードイラストは1枚ずつ用意（WebP形式、圧縮推奨）
- 100〜300枚規模でもFirebase Storage無料枠（5GB）内に収まる想定
- ダウンロード量対策としてブラウザキャッシュを効かせる設計にする

## 参考にしている前作からの変更点
- プレイヤー同士の直接バトルを廃止し、モンスターバトルのみに一本化
- 最初にデッキを選ぶ／自由に編成する形式を新規導入
- カード収集・ガチャによる成長ループを新規導入
- 土地の獲得を「購入」から「モンスター召喚」に変更。購入コストは廃止し召喚コストのみ。他プレイヤーの土地への侵略も同じ仕組み
- ブック（デッキ）編集にカード収集の楽しみを持たせる。あわせて、自分で育てるブリードモンスターをブックに編成できる仕組みを新規導入
