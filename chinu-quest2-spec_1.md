# チヌクエスト2 開発仕様書

## 概要
カルドセプト風の3Dボードゲーム。プレイヤー同士の直接対決バトルは廃止し、モンスターとのバトルに一本化。カード収集・デッキ編成・ガチャによる成長要素を持つ。

## 技術スタック
- **描画**: Three.js（WebGL、3D表示）
- **データ同期・保存**: Firebase（Realtime Database or Firestore、Authentication、Storage、Functions）
- **配信形態**: PWA（Progressive Web App）
  - manifest.json + Service Worker
  - ホーム画面に追加してフルスクリーン起動、AppStore審査不要
- **対応画面**: スマホ横画面固定を想定

## 開発フェーズ（この順で実装）
0. ログイン／キャラメイク／モード選択ハブ
1. 盤面移動＋マス目イベント（シングルプレイ、CPU相手）
2. モンスターバトルシステム
3. デッキ編成画面
4. カード収集・ガチャ・通貨システム
5. 対戦モード（マルチプレイ）

現時点ではフェーズ0の骨組み（ログイン〜対戦(CPU)まで）とフェーズ1〜2を実装済み。動作確認しながら段階的に進める。

---

## 未実装まとめ（2026-08-10更新、実装指示待ち）
今後の実装指示のための棚卸し。詳細な仕様・背景は各項目が属するセクション本文を参照。

### 対人戦・マルチプレイ（2026-08-10実装、対戦本編まで完了）
- Firebase/Firestoreによる部屋ロビー（`src/pvp.js`）に加え、実際の対戦本編（ダイス・盤面移動・分岐選択・土地コマンド・召喚・カードドロー等）をホスト権威モデルでリレーする仕組みまで実装済み。ローカルのFirebaseエミュレータ+2ブラウザタブでロビー→デッキ選択→対戦開始→ターン交代を含む一連の流れを確認済み
- ホストだけが本物の`Game`シミュレーションを持ち、ゲストは`publicState`（盤面・プレイヤー状態、手札は含まない）と自分専用の手札ドキュメント（`private/{uid}`）を購読する薄いクライアント。ゲスト自身の盤面は`createBoard()`/`GameScene.buildBoard()`をホストと同じ引数で呼び直して再構築する（決定的なので一致する）
- `main.js`の`relayable(type, localPrompt, {broadcast})`が、game.jsの約20種の意思決定コールバック（`onLandCommand`等）をホスト自身の手番ならローカルUIへ、相手の手番ならFirestore経由でゲストへ、それぞれ振り分ける。`game.js`側のコールバックは`(payload, ..., player.id)`のように途中に追加引数を挟む型があるため、`relayable`は可変長引数を受けて「最後の引数=player.id」として分離する（`onLandCommand`/`onShopPurchase`がこれに該当）
- ゲスト発の操作（ダイスを振る／スペル使用）は逆方向の`guestAction`/`guestActionId`チャンネル（`GuestActionSender`/`HostActionListener`）で送る
- 相手の本当の手札は表示しない（vs CPUでは見せているCPUの手札と違い、対人戦では両者とも自分の手番以外は相手の手札を伏せる）
- Firestoreは値に`undefined`を許さないため、`sendGuestResponse`は`undefined`を`null`に丸めてから書き込む（`promptCardReveal`等、確認のみでresolve()に引数を渡さない型があるため）
- 本物のFirebaseプロジェクト設定（`.env`）はまだ未投入（開発中はローカルエミュレータにフォールバックしている）
- モンスター同士のバトル演出（`battle-scene-modal`）は実機で確認済み（侵略→アイテム選択→攻撃応酬→通行料決着まで一巡）。検証中に`relayable()`のバグを発見・修正: `onBattleSceneEnter`/`onBattleAttack`/`onBattleOutcome`はplayer.idを引数に持たないbroadcast型なのに、末尾引数を一律「player.id」とみなして切り落とす処理が唯一の引数（payload本体）を誤って消してしまい、ゲスト側で`undefined`を受け取って侵略した瞬間に無言で止まっていた。broadcast型は末尾切り落としをスキップするよう修正済み
- ショップマス（`onShopPurchase`）とSTART/EVENTマス（チェックポイント到着）は実機で確認済み。検証中に2件のバグを発見・修正:
  - `getTileSummary()`: 非LANDマス（START/EVENT/SHOP）はboard.js側でlevelがundefinedのまま置かれており、そのままFirestoreへ中継すると拒否されて対戦相手（チェックポイントに止まった側）の意思決定が無言で止まっていた。非LANDマスのlevel/landValue/tollをnullへ丸めて修正
  - `relayable()`: 追加引数を持つ型（`onLandCommand`/`onShopPurchase`）の中継payloadを配列でまとめていたが、引数自体が配列の場合（ショップの購入候補一覧など）Firestoreが拒否する「ネストした配列」になっていた。配列ではなく`{a0, a1, ...}`という個別プロパティのオブジェクトにまとめて修正
- 未検証のまま残っている個別フロー: スペル使用のリレー、破産・勝敗判定の対人戦での見え方 - アーキテクチャ上は他の意思決定と同じ経路を通るはずだが、実機での動作確認はまだ
- 既知のv1簡略化: ゲスト側の駒移動はステップアニメーションなし（publicState同期のたびに最終位置へスナップ）、切断検知なし（ホスト退出→ゲスト強制退出のパスのみ）、ゲストのダイス目はゲスト側で計算した値をそのまま信頼（サーバー側再検証なし）

### カード・収集まわり
- カード規模の本格拡充（想定: モンスター各属性20種・アイテム30種・スペル40種）。**火・水属性は2026-08-12実装で20種ずつ完了**（詳細は「火・水属性モンスター40種＋特殊能力システム拡張」セクション参照）。雷・森属性とアイテム・スペルの拡充は未着手
- アイテムの一部復活効果（現状は全アイテムが勝敗問わず戦闘後に必ず消滅）
- 通貨・ガチャ処理のサーバー側（Firebase Functions）移行（現状は全てクライアントローカルのlocalStorage、改ざん防止なし）

### 土地コマンド・盤面まわり
- **強制停止**（指定マスに強制的に相手を止まらせる効果）: 同盟仲間は巻き込まれない、というルールは確定済みだが効果自体が未実装
- **特殊能力（`ability.type`）は2026-08-12実装で5種類に拡張済み**: `damage`（火炎瓶男）、`warpToEmptyElementLand`（人魂）、`healAllOwnedAndCleanse`（料理の鉄人/海の家店長）、`drawCard`（あざらしさん）、`curseTransparency`（深海魚X）。詳細は「火・水属性モンスター40種＋特殊能力システム拡張」セクション参照

---

## 火・水属性モンスターの微調整＋後攻(lastStrike)特性の新設（2026-08-12実装）
ユーザーがカタログ一覧を見て指示した6件の修正。
- **後攻(lastStrike)特性を新設**（`battle.js`）: 先制(firstStrike)の対になる特性。`strikeOrderScore(unit)`ヘルパーで先制=+1／後攻=-1／どちらも無し=0のスコアを算出し、スコアが高い側が先に攻撃する形へ`resolveBattle`の順序判定を一般化（旧実装は`hasTrait(defender,'firstStrike') && !hasTrait(attacker,'firstStrike')`という先制専用の分岐だった）。同スコア同士（無特性同士・先制同士・後攻同士）は従来通り攻撃側が先。後攻側が防御側になった場合は元々防御側は2番手なので見た目上の変化はなく、後攻側が攻撃側になった場合だけ防御側に先を譲る。先制と後攻が同じ戦闘に同時に出ても矛盾なく解決できる
- 火付け役・炎のチャレンジャー・半魚人・ファイヤーマンに`traits:['firstStrike']`を追加
- アオリイカに`traits:['lastStrike']`を追加、ステータスを30/30→**HP40/ATK30**に変更
- 嵐を呼ぶ〇女のコストを50G→**100G**に変更
- 検証: `resolveBattle`を直接叩くNode単体テスト5件（後攻単独／後攻が防御側の場合は変化なし／先制と後攻が同時に出た場合／後攻同士のタイブレーク／既存の先制のみの回帰確認）で全パス、実機でも6件の変更値が正しく読み込まれることを確認

## 雷・森属性モンスター40種＋特殊能力（2026-08-12実装、火・水属性40種で確立した実装に合流）
火・水（20種ずつ、`src/fireMonsters.js`/`src/waterMonsters.js`）に続き、`src/thunderMonsters.js`/`src/forestMonsters.js`で雷・森属性を追加。これで4属性×20種＝**モンスターカタログ全80種が揃った**（`MONSTER_CATALOG`はfire/water/thunder/forestの4カタログをスプレッドするだけの構成に）。既存の効果体系（`effect.type`/`ability.type`ディスパッチ、`chainRequired`、`statsPerElementChain`、`atkBonusAgainstRarity`、`warpToEmptyElementLand`、`challengeOdds`等）を最大限再利用しつつ、雷・森ならではの新規効果を追加実装した。

### battle.js: 新規on-hit/撃破時/戦闘終了時エフェクト7種
- `atkDownOnHit`（静電気野郎）: 命中時、相手に永続ATKダウンの呪いを付与（既存の汎用addedAtk curseをそのまま流用、重ね掛け可）。**副作用としてdealDamageに`Math.max(0, ...)`のクランプを追加** - ATKダウンが重なって負値になっても、負のダメージ（＝相手を回復させてしまうバグ）にならないようにした
- `shockOnHit`（雷雲、感電）: 命中時、相手に「1/3の確率で攻撃が不発になる」永続の呪いを付与。目くらまし（1回消費）と違い入れ替え/死亡まで持続し、performStrikeの先頭で毎回抽選する
- `stealDamageMultiple`（テンホウ×5、山賊フクロウ×2）: 強盗特性（3倍固定）を効果として一般化し、倍率を自由に設定できるようにしたもの
- `instantKillOnHit`（樹海の怨霊=全属性対象、とある科学の...=水属性限定）: `targetElement`未指定なら全属性、指定時はその属性の相手にのみ発動する共通ハンドラ
- `chanceGoldAfterBattle`（トリュフ豚）: 戦闘終了時、生死問わず確率でG獲得
- `chanceSetHpOnHit`（山姥）: 命中時、相手が生存していれば確率でHPを固定値にする（現在HPがその値未満でも引き上げる＝回復もあり得る仕様通り）
- `goldOnKillElement`（きこりのおじさん）: 指定属性のモンスターを撃破した時だけGを得る（payOnKillと同じ撃破判定タイミング）

### game.js: 新規の召喚時/シナジー/経済/土地コマンド効果
- `itemOnSummon`（謎の科学者）: `_placeUnit`にフック。名前付きアイテム（ITEM_CATALOG）＋汎用武器防具プールを合わせたレアリティ別プールからN70%/S20%/R10%で1枚を手札に加える（所持数は変化しない）
- `synergyWithNamedAlly`（タケノコ派⇔きのこ派）: `_applyEffectBonus`で、盤面のどこかに指定catalogIdの味方が配置されていればATK/HPボーナスが乗る新規ヘルパー`_hasAllyOnBoard`を追加
- `atkDoubleIfRicher`（マウントゴリラ）: `_applyEffectBonus`で、戦闘開始時点の所持Gが相手を上回っていれば基礎ATK分を追加（＝2倍）
- **`summonFieldMonster`**（電柱を植える男）: 新設の土地コマンド。ランダムな空き地に、図鑑登録なしの専用駒「電柱」（`thunderMonsters.js`の`DENCHU_FIELD_MONSTER`、`MONSTER_CATALOG`には含まれない）を召喚する。盤面のどこかに電柱が1体でもいる間、**所有者を問わず**全ての雷属性モンスターがHP+10（`_battleBonus`に追加、`_hasFieldMonsterOnBoard`で判定）
- **`summonMonsterOnEmptyLand`**（サンダーバード→雷雲、ドリアード→自分自身）: 新設の土地コマンド。`ability.catalogId`で指定した実在のカタログモンスターの新規インスタンスをランダムな空き地に召喚する（電柱と違いこちらは図鑑登録されている実カード）
- **`cursePlayerHaste`**（ソニックムーヴ）: 新設の土地コマンド。選んだプレイヤー（同盟仲間は対象外）に「高速化の呪い」を2ターン分付与する。付与された側は`_beginTurn`が既存の「速度違反はご愛嬌」(`forcedDiceRemaining`)と全く同じ仕組みで自動的にサイコロ・スペルフェーズを飛ばし、6マス固定で強制移動する（`player.hasteTurnsRemaining`、ほこら効果は全体カウンタだったのに対しこちらはプレイヤー個人に効く点が違い）
- **避雷針侍の身代わり効果**: 新規ヘルパー`_maybeRedirectDeathToLightningRod`。`_runInvasion`/`_humanMoveFlow`の侵略分岐で`_runBattleScene`直後・死亡処理より前に呼び、味方が敗死する結果だった場合、盤面の別マスにいる避雷針侍自身を代わりに死なせてresultを書き換える（元のモンスターはノーダメージで生存に戻る）
- **メカニックマソの周回回復**: `_grantGoalBonus`（ゴール通過処理）の先頭で`_healMechanicMasoAllies`を呼び、配置していれば自分の雷属性モンスター全員の現在HPを最大HPの10%回復する。なお戦闘開始時に`prepareForBattle`が毎回HPを最大値へリセットする既存仕様上、この回復は「次の戦闘結果」には影響せず、専ら土地情報等アイドル時のHP表示にのみ反映される（氷柱の戦闘後自傷など既存の同種効果と共通の性質）

### 仕様上あいまいだった3点（ユーザー確認済み・2026-08-12）
1. **ソニックムーヴ**: コスト欄が「50G」のみで土地コマンド側のコストが別記載されていなかったため、当初は発動コスト0（無料）として実装したが、ユーザー確認により**土地コマンドのコストは80G**に修正（`commandCost: 80`）。召喚コスト自体は50Gのまま
2. **避雷針侍**: コスト欄に「通常（コマンド50G）」とあるが、対応する土地コマンドの説明文が本文に無かったため、氷柱（水属性）の時と同じ判断で土地コマンド自体は実装せず、身代わり効果（受動効果）のみ実装 → ユーザー確認によりこのままでOK
3. **電柱のHP+10効果**: 「配置されていると全ての雷属性モンスターのHP+10」に所有者の限定が無かったため、味方限定ではなく敵味方問わない全体効果として実装した（メカニックマソが明示的に「味方雷モンスター」と書いていたのとの対比から、この解釈にした） → ユーザー確認によりこのままでOK

### 検証
`resolveBattle`を直接叩くNode単体テスト13件、`Game`をVite SSRローダー経由でNode実行するテスト22件（chainRequired・新規エフェクト・電柱/雷雲召喚・電柱の全体HP+10オーラ・高速化の呪い・避雷針侍の身代わり・メカニックマソの回復を含む）の計35件を実施、全てパス。実機でも新規登録→水・雷ブックでCPU戦開始→カタログ80種読み込み確認→ダイス→着地→召喚まで一連の操作をコンソールエラーなしで確認済み

## ブリードパーツパック（2026-08-12実装、Codex担当分・仕様確認済み）
- `src/breedParts.js`の`drawBreedPartPack(random)`: 150Mで一括3個抽選（`BREED_PART_PACK = { cost: 150, count: 3 }`）。各枠はN65%/S25%/R10%で独立抽選し、3個全てNだった場合だけ最後の1個をSへ差し替える最低保証付き。ショップ画面（`main.js`の`renderShopScreen`内、`shopPartsList`）に「ブリードパーツパック」として配線済みで、購入すると`currentCharacter.ownedPartIds`へ追加・M減算・結果表示（`showPackResult`、通常のカードパックと同じUIを`onDetail=null`で流用）まで一通り機能する。`tests/breedParts.test.mjs`のNode単体テスト2件（全N時のS差し替え／通常時のレアリティ維持）を実行し両方パス

## ゴール/チェックポイント/ほこらマスの建物イラスト（2026-08-12実装）
- ユーザー提供の`image/goal.png`（城）・`image/checkpoint.png`（塔）・`image/hokora.png`（鳥居）から境界フラッド・フィルで背景除去。`hokora.png`は内部の暗い陰影（灯籠の下部・屋根の影）が背景の純黒と地続きになっており、既定の許容誤差（22）だと金色の装飾が黒い穴になって消えてしまったため、許容誤差を6まで下げて再抽出（他2枚は22のままで問題なし）
- `public/images/board-markers/`に`goal.webp`・`checkpoint.webp`・`shrine.webp`として保存
- `scene.js`の`buildBoard()`から`_createBoardMarker(tileType, tilePosition)`を新規呼び出し。START/EVENT/SHRINEマスそれぞれに、プレイヤー駒・NPC駒と全く同じ「常にカメラを向くbillboard `THREE.Sprite`」方式で建物を配置（`createPiece`と同じ仕組みを流用）。各画像固有のaspect比を保持したまま、共通の目標高さ（3.0ユニット）から幅を逆算するため、縦長の城・塔・鳥居がそれぞれ潰れずに表示される。プレイヤー駒がタイル表面よりわずかに沈み込んで接地して見える量（0.1）に合わせて底面を配置
- 検証: 実機でCPU戦を開始し、開始マス（START）に城のイラストが正しい向き・サイズで表示されることをcanvasキャプチャで確認。3画像とも`/images/board-markers/`から200 OKで読み込まれることを確認

## メニューボタン新設・カメラ角度調整・UI微修正（2026-08-12実装）
- **メニューボタン**: 旧`#exit-battle-button`（画面上部中央、退出）と`#mute-button`（同、BGM）は`#log`と重なって見づらかったため廃止。代わりに盤面右下の「目標G」表示のすぐ上に「メニュー」ボタンを新設し、押すとポップアップ（`#game-menu-modal`）が開く。中身は上から**土地情報／BGMのON・OFF／ヘルプ／退出**の4項目固定
  - **土地情報**（新機能）: 土地コマンドの土地サブメニューにあった「情報」ボタンをここに統合・一本化した（`game.js`の`_runLandBrowse`から`'info'`分岐ごと削除、`land-submenu-info`ボタンも撤去）。特定の1マスに紐づかず、いつでも自分の所有地を一覧から選んで見られる（`main.js`の`showLandInfoFromMenu` - 所有地が無ければ「所有している土地がありません」とログ表示するだけ）。一覧・選択UIは既存の`onPickAbilityTarget`用モーダルをそのまま再利用（`label`フィールドで表示文言だけ差し替え）、詳細表示も既存の`promptShowTileInfo`をそのまま再利用 - 見た限りを閉じると一覧に戻り、何度でも選び直せる。対人戦ゲスト側はローカルに`Game`インスタンスを持たないため今のところ非対応（「対人戦のゲスト側では現在利用できません」）
  - **BGMのON/OFF**: 押しても選択後にメニューが閉じない（何度でも切り替えられる）。表示は🔊/🔇+「BGM」
  - **ヘルプ**（新規）: サイコロ・土地コマンド・通行料・バトルボーナス・特殊能力の要点をまとめた静的テキストをモーダルで表示
  - **退出**: 中身（対戦をやめますか？＋M変換額の確認→はい/いいえ）は既存のまま、トリガーの位置だけメニュー内に移動
- **ログの文字サイズ**: `#log`のfont-sizeを13px→16pxに拡大（読みにくいという指摘のため）
- **サイコロの位置**: `#dice-button`に`transform: translateX(-38px)`（約1cm）を追加し、指摘通り左へ寄せた
- **カメラ角度**: `scene.js`の`CAMERA_OFFSET`のY成分を25→32に変更（仰角約50.6°→約58°、X/Zは据え置きなのでタイル が菱形に見える斜め視点自体は維持）。より上から見下ろす角度になるよう調整。**プレイヤーアイコン・盤面駒は元々`THREE.Sprite`（常にカメラの方を向く）で実装済みで、ゴール/チェックポイント/ほこらのマスも平らな`BoxGeometry`（回転無し）のみで構成されており、これらのオブジェクト自体が奥行き方向に傾いて見える要因は見当たらなかった（実機のcanvasキャプチャでも駒が正面向きの正方形/円として描画されていることを確認済み） - カメラ角度が浅かったことによる見え方の問題だった可能性が高く、今回の角度調整で併せて改善される想定
- **タイトル画面の並び順**: ロゴ→説明文→ログインの順だったのを、ロゴ→ログイン→説明文の順に変更（`index.html`の`#login-screen`内で`.pg-field`群を`#title-intro`より前に移動しただけ、CSS変更なし）
- **サラリーマンダーの専用カード絵**: ユーザー提供の`image/card/sarari-manda-.png`（チェッカーボード柄で「透過」を表現したAI生成画像、実際のアルファチャンネルは無し）から境界フラッド・フィル＋孤立した小さな市松模様の島を面積フィルタで除去して背景を除去、`public/images/card-art/salarymander.webp`として保存。`fireMonsters.js`の`salarymander`エントリに`imageDataUrl`を追加（`fireMonster()`ビルダーに`imageDataUrl`オプションを新設） - `renderCardEl`が`card.imageDataUrl`を最優先で使う既存の仕組みにそのまま乗るだけで、火属性共通画像から専用絵に自然に差し替わる
- 検証: 実機（開発サーバー）でメニューの4項目全て（土地情報の一覧→詳細→一覧に戻る往復、BGM切り替えでメニューが閉じないこと、ヘルプの表示、退出の確認ダイアログ）を実際に操作して確認。`#log`のfont-size・`#dice-button`のtransformも算出スタイルで確認。カード詳細ポップアップでサラリーマンダーの専用画像（`/images/card-art/salarymander.webp`、200 OK）が適用されていることを確認。カメラ角度は実機のcanvasキャプチャで見た目を確認したが、「もう少し」という主観的な指摘だったため、実際に十分かはユーザー確認待ち

## 目標G表示＋①ヒトデ戦の盤面オーバーレイ会話（2026-08-12実装）
- **目標G表示**: `STORY_STAGES`各エントリに`goalCurrency`フィールドを追加（①4000/②5000/③7000/④10000）。`startBattle`のstoryOptionsで受け取り、盤面右下（`#stage-goal-display`、`bottom`基準 - 既存の`corner-br`プレイヤーパネルは`top`基準なので重ならない）に「目標G: 4,000」のようにカンマ区切りで表示するだけの**表示専用**機能（勝敗判定には一切使わない）。CPU戦・対人戦などstoryOptions.goalCurrency未指定の対戦では非表示のまま
- **ヒトデの立ち絵/盤面駒**: ユーザー提供の`image/hitode.png`（単色黒背景のイラスト）から境界フラッド・フィルで背景除去→トリミングした立ち絵と、他NPCと同じ256×256正方形トークン正規化（88%高さ・下寄せ・水平中央）を生成し、`npcArt.js`の`NPC_PORTRAIT_URL`/`NPC_TOKEN_URL`に`'ヒトデ'`キーを追加（`public/images/npc-portraits/hitode.webp`・`public/images/npc-tokens/hitode.webp`）
- **①ヒトデ戦専用: 盤面を隠さない会話オーバーレイ**（`playOverlayDialogueLines`、`main.js`）。既存の`#story-dialogue-screen`（全画面・盤面を完全に隠す）とは別物の新規要素`#story-overlay-dialogue`で、`#app`の中に重ねる形にして背景をほぼ透明にし、盤面（3Dシーン）を見せたまま会話する。左下にヒトデの立ち絵、右下にプレイヤーがキャラメイクで選んだアイコン画像（`iconSheet.js`の`icons[iconIndex].dataUrl`をそのまま`<img>`のsrcに使う - 専用の立ち絵が無いキャラでもこれで代用できる）を表示し、吹き出し（`#story-overlay-bubble`）は発言者側（`side-left`/`side-right`クラス）に寄って表示、発言していない側の立ち絵は減光する。オーバーレイ全体が疑似クリック領域になっており、会話が終わるまで盤面操作（サイコロ等）は実質ブロックされる（`Game`自体はバックグラウンドで既に`init()`済みだが、操作が物理的に届かない）
  - **開幕（intro）**: `playStoryStage`が①ヒトデ戦の場合だけ、先にデッキ選択→`startStoryBattle`で盤面を表示→その直後に`playOverlayDialogueLines(stage.intro, {...})`を呼ぶ流れに変更（他ステージは今まで通りデッキ選択前に全画面`#story-dialogue-screen`で会話してから盤面を作る）
  - **決着後（outro）**: `handleStoryBattleEnd`に`useHitodeOverlay = stage.key==='hitode' && won`分岐を追加。勝利時だけ`#app`を隠す処理を後回しにし、決着直後の盤面が見えたままオーバーレイで会話→会話終了後に盤面を隠してステージ選択へ戻る（敗北時・他ステージは従来通り即座に盤面を隠してからの全画面会話のまま）
  - **outro文言の変更**: ①本編（`replay`ではなく通常クリア時）のoutroを、急流に飲まれる旧演出から「ヒトデ: 俺の・・・屍を・・・超えていけ・・・ガクっ」の1行のみに差し替え
- 検証: 実機（開発サーバー）で新規登録→①ヒトデ戦選択→（全画面イントロをスキップして）デッキ選択→盤面表示直後にオーバーレイ会話が始まることを確認。会話中は盤面（両プレイヤーパネル・手札・「目標G: 4,000」）が背景に見えていること、吹き出しが発言者（ヒトデ→左/主人公→右）に応じて`side-left`/`side-right`を正しく切り替えること、最終行を閉じるとオーバーレイが消えて通常のサイコロ操作に戻ることを確認済み。ヒトデの立ち絵・盤面駒（`hitode.webp`）のネットワーク取得も200 OKを確認。**決着後（勝利）のオーバーレイ演出自体は、CPU戦を実際に最後まで勝ち切るプレイテストまでは行っておらず、コードレビューでの確認に留まる**（introと全く同じ`playOverlayDialogueLines`を呼ぶだけの経路なので、動作自体はintro側の検証で担保されている想定）

## 火・水属性モンスター40種＋特殊能力システム拡張（2026-08-12実装）
ユーザー提供の最終仕様表（火属性20種はCodexが先行実装済み、水属性20種は今回追加）を元に、名前付きモンスターを20種→40種に拡張。特殊能力の大半は元々カード定義（`effect`/`ability`フィールド）だけ用意されていて実際のゲームロジックには繋がっていなかった（Codexの先行実装分含む）ため、今回全て実装して繋いだ。

- **`src/waterMonsters.js`（新規）**: `src/fireMonsters.js`と同じ`waterMonster(id, name, rarity, hp, atk, options)`ビルダーパターンで水属性20種（N10/S6/R4）を定義。`battleCards.js`の`MONSTER_CATALOG`に`FIRE_MONSTER_CATALOG`と並べてスプレッド
- **港区女子（`minatoJoshi`）の仕様統合**: 元々`battleCards.js`に直書きされていた仮ステータス（R・ATK15/HP30・battle.js側にハードコードされた耐性0.8倍+ダメージ連動ゴールド略奪）を、正式仕様（N・ATK30/HP30・攻撃成功時に固定30G略奪）に差し替え、`waterMonsters.js`のデータ駆動カタログに統合。battle.js側のハードコード分岐（`catalogIdOf(...) === 'minatoJoshi'`の特殊ケース2箇所）は削除し、後述の汎用`stealGoldOnHit`エフェクトに置き換えた。id自体は維持しているので`STARTER_DECKS.waterThunder.featuredMonster`やstory.js各キャラのテーマ定義は変更不要だった
- **`src/battle.js`に新設した効果ディスパッチ**: `performStrike()`という1回の攻撃解決を専用関数へ切り出し、以下を`effect.type`ベースで処理する（英字は`effect.type`の値）:
  - 命中時: `stealGoldOnHit`（港区女子=固定G略奪）、`poisonOnHit`（カエンタケ=毒付与、`unit.curses`に`poisonRatio`を持つエントリとして保存、入れ替え/死亡までunit.curses経由で持続）、`blindOnHit`（アオリイカ=`unit.blinded`フラグ、次の自分の攻撃を1回だけ無効化してすぐ消費 - 同じ戦闘内の反撃even含む）
  - 攻撃終了時（命中失敗含め常に）: `selfDamageAfterAttack`（ヘビースモーカー）、`chanceSelfDamageOnAttack`（へ〇ま竜/嵐を呼ぶ〇女）、`chanceSelfDestructAfterAttack`（着火まん）
  - 撃破時: `payOnKill`（炎上系ユーチューバー=倒した相手の所有者へ賠償金）、`deathRetaliation`（花火師=相手の攻撃で死亡した時だけ道連れダメージ）
  - ダメージ計算そのものへの介入: `chanceDamageReduction`（マグマン=確率で被ダメージ半減、既存の`halfDamage`特性とは別枠で同時判定）、`challengeOdds`（炎のチャレンジャー=自分の攻撃が確率失敗／相手の攻撃を確率無効化）、`elementDamageBonus`（ファイヤーマン=特定属性への追加ダメージ倍率）
  - 戦闘開始前（`prepareForBattle`より前、HP計算に影響するため）: `destroyItemBeforeAttack`（海賊S=相手の装備アイテムを破壊）
  - 戦闘終了時（生死問わず）: `randomGoldAfterBattle`（もぐもぐ風〇火山）
  - 戦闘終了時（生存時のみ）: `survivalGold`（サラリーマンダー、既存）、`selfHealAfterBattle`（おひ〇ねラッコ=HP回復＋G消費）、`selfDamageAfterBattle`（氷柱=無条件自傷、これで力尽きることもある）
  - 毒tick: 2回の攻撃応酬が終わった直後、その時点で生存している側が保持する毒で追加ダメージ（基礎HP=`def.hp`基準）を受ける。これでも力尽きうる。毒の状態そのもの（`unit.curses`のエントリ）は入れ替え/死亡まで戦闘をまたいで持続する
- **`src/game.js`に新設したカード固有の戦闘中ステータス補正**: `_battleBonus`（盤面状況依存の同属性/応援ボーナス）とは別枠の`_applyEffectBonus(unit, opponentUnit, bonus)`を`_runBattleScene`内で追加呼び出し。`statsPerElementChain`（炎神/水神=所有する同属性土地の連鎖数×n だけATK/HP上昇）と`atkBonusAgainstRarity`（ファイアキック/水風呂修行僧=相手が指定レアリティならATK+n%）を処理
- **召喚条件`chainRequired`**: `_affordableMonsterCards`が`_meetsChainRequirement`でフィルタするようになり、「〇属性の土地n連鎖以上」を満たさないと手札にあってもそのモンスターは召喚候補に出ない。連鎖の生カウント（土地の枚数）を返す`_chainCount(ownerId, element)`を新設し、既存の`_chainMultiplier`（地価計算用の1.0〜3.0倍率）はこれをラップする形に整理した
- **土地コマンド「特殊能力」を5タイプに拡張**（`_humanAbilityFlow`を全面書き換え）。全タイプ共通で、対象/種類の選択→コスト確認ダイアログ→確定した時だけ`commandCost`をG消費するように統一（以前は`damage`タイプ唯一の実装で、コスト確認自体が無くコストも一切消費されない未完成状態だった）:
  - `damage`（火炎瓶男、既存）: 射程内の敵からUIで選んで固定ダメージ
  - `warpToEmptyElementLand`（人魂）: 指定属性の空き地へワープ（対象選びは`damage`と同じ`onPickAbilityTarget`を再利用、`label`フィールドで表示文言だけ切り替え）
  - `healAllOwnedAndCleanse`（料理の鉄人/海の家店長）: 自分の所有モンスター全員を全回復（同属性ボーナス込みの最大HPまで）＋呪い全除去
  - `drawCard`（あざらしさん）: モンスター/武器防具/スペルから選んだ種類のカードを山札（無ければ捨札）からランダムに1枚引く。新設の`onPickCardType`コールバック＋`#card-type-picker-modal`（`element-picker-modal`と同じ見た目パターン）
  - `curseTransparency`（深海魚X）: 自分の土地に「透過の呪い」をかける（`tile.transparentCursed`）。かかっている間は他プレイヤーから侵略不能（`_humanSummonFlow`/`_humanMoveFlow`の候補からブロック）、通行料は常に0（`_tollOfTile`）。ユニットが入れ替わる/倒れる/移動する/売却されるあらゆる経路でフラグをリセットするよう`_placeUnit`他を修正
- **仕様上あいまいだった2点は保守的に解釈**（要ユーザー確認）: ①水属性「海賊S」は表の召喚コスト欄が「通常」なのに特殊能力説明文に「召喚コスト100G」と重複記載があったため、後者を採用してコスト100Gとして実装。②水属性「氷柱」は召喚コスト欄に「通常（コマンド50G）」とあるが対応する土地コマンドの説明文が無かったため、コマンド自体（`ability`/`commandCost`）は実装せず「戦闘終了時にHPマイナス10」という受動効果のみ実装した
- 検証: `src/battle.js`単体（`resolveBattle`を直接呼ぶNode一時スクリプト）で新規効果21件、`src/game.js`（Vite SSRローダー経由でNode実行、`import.meta.env`の解決に対応）で連鎖召喚条件・戦闘中ステータス補正・5種の土地コマンド・コスト不足時の未消費ガードの計29件をアサーションで確認（テストスクリプトは検証後削除）。加えて実機（開発サーバー）でキャラ作成→CPU戦開始→ダイス→分岐選択→着地→召喚確認ダイアログ→召喚まで一連の操作をコンソール誤りなしで確認し、カードカタログが40種（火20/水20）を正しく読み込むことも確認済み

## 複数デッキ（ブック）スロット（2026-08-10実装）
- `character.decks`: `{id, name, deckList}`の配列、最大`MAX_DECKS`(3)冊。旧`character.deckList`（単一）は`ensureBreedFields`が初回ログイン時に`decks[0]`へ自動移行する（フィールド自体は残るが以後どこからも読まない）
- デッキ編成画面にブック切り替えタブ（既存のブック名ボタン＋冊数未満なら「＋新規作成」）とデッキ名編集欄を追加。切り替えても未保存の編集内容はブックごとに独立（保存ボタンを押した冊だけが更新される）
- **対戦・ストーリーどちらも、盤面に入る直前に必ずデッキ選択画面を挟む**（`promptDeckSelection()`）。最大3冊を本の絵＋名前で並べて「デッキを選んでください」→ 押すと「「〇〇」にしますか？ はい/いいえ」＋カード内訳一覧（`#deck-select-breakdown`、`max-height`+`overflow-y:auto`の同一パターンでこの内訳リストだけがスクロールし、画面自体はスクロールしない）
- 対人戦（対戦本編、2026-08-10実装）でも同じ`promptDeckSelection()`をホスト・ゲスト双方の対戦開始フローに挟んで配線済み
- **副産物のバグ修正**: このセッション中盤までに保存されていたデッキの一部に、カタログに存在しないキー（壊れた保存データ）に紐づく「空オブジェクトのカード」が混入し続けていたことが判明（デッキ編成画面は所持数ベースで数えるため気づかず、今回新設した内訳表示で初めて「undefined」として露見した）。`showDeckScreen`のカウント初期化時にカタログに存在しないキーを弾くよう修正（保存し直すと自動的に取り除かれ、正しい枚数不足として表示される＝自己修復）

## ストーリーモード「再挑戦」（2026-08-11実装）
- クリア済みステージをストーリー画面でもう一度選ぶと、本編とは別のシナリオ（`STORY_STAGES`各ステージの`replay`フィールド）で再戦できる。`intro`/`outro`/`format`/`opponents`/`ally`/`extraAlly`/`heroAllianceId`/`enemyAllianceId`のうち指定したものだけ本編の値を上書きし、未指定のフィールドは本編の値をそのまま使う（`main.js`の`buildBattlePlayerConfigs`がstage本体とvariant=replayの両方を受け取りフォールバックする）
- 例: ①ヒトデ戦の再戦は農家のウサギが乱入し1vs1vs1に、②暴君マダイ＆ニュウドウカジカ戦の再戦は某不思議の国の少女が乱入し1vs1vs1vs1に、③の再戦はヒトデが加勢し3vs2に拡大（2人目の味方は`ally`1フィールドでは表現できないため`extraAlly`という専用フィールドを新設）。④ダンボール男戦（ラスボス）はintro/outroのみ差し替え、形式・登場人物は本編と同じ1vs1のまま
- 再戦の勝敗は`character.storyProgress`・クリア報酬に一切影響しない（`handleStoryReplayEnd`は勝敗に応じたセリフを表示してステージ選択に戻るだけ。本編初回クリア時の`handleStoryBattleEnd`とは別関数）。何度でも遊べるおまけ戦闘という位置づけ
- ストーリー画面のステージ行は、クリア済みの場合ラベルが「クリア済み（もう一度挑戦できます）」に変わり、クリック先が本編`playStoryStage`から再戦`playStoryReplay`に切り替わる

## カードエディタのCSV一括登録（2026-08-11実装）
- カードエディタ画面に単体登録フォームとは別に「CSVで一括登録」セクションを追加（`index.html`の`#card-editor-bulk`）。約200種規模のカードデータを1件ずつ手入力するのは非現実的という判断から、Excelではなく依存ライブラリ不要のCSVを採用
- CSVはヘッダー名ベースの列マッピング（列の並び順に依存しない）。「種類」「レアリティ」「属性」「効果」列は日本語ラベルで入力し、内部でenum値に逆変換する（`BULK_TYPE_LABELS`等、main.js）。テンプレートCSVはボタン1つでダウンロード可能
- **画像はファイル名一致で一括紐付け**: CSVの「画像ファイル名」列と、複数選択した画像`<input type=file multiple>`のファイル名が一致した場合のみ自動で紐付く（一致しなければ画像なしで登録される）。1枚ずつのアップロード操作は不要
- 行ごとに独立してバリデーション（`validateCustomCard`を流用）するため、1行の入力ミスが他の正常な行の登録をブロックしない。結果は行単位で成功/エラーを一覧表示（`renderBulkCsvResult`、エラー内容も行ごとに表示）
- パーサーは`parseCsv`（RFC4180ライクな手書き実装、ダブルクォート内カンマ・改行に対応、BOM除去込み）。CSV/画像読み込みからカタログ保存まで全てクライアント側で完結し、サーバー通信は発生しない（`saveCustomCardsBulk`が1回のlocalStorage書き込みで全件保存）

## 複数マップ + 対人戦のマップ選択（2026-08-11実装）
- `board.js`の盤面生成を単一固定レイアウトから複数マップ対応へ拡張。`MAPS`（`{id, name, rows}`の配列）で①〜④のストーリー各ステージに対応する4つの盤面レイアウトを定義し、`createBoard(mapId)`がidで引く（省略/不明なidはMAPS[0]＝①ヒトデの縄張りにフォールバックするので、CPU戦など従来どおりmapId無指定の呼び出しは今までと完全に同じ盤面になる）
- ストーリーモードは各ステージの`key`（`hitode`/`madai`/`budou`/`danball`）をそのまま`mapId`として`createBoard()`へ渡すだけで対応マップになる（`story.js`側にmapIdフィールドを別途持たせる必要はない）。本編・再挑戦（replay）どちらも同じステージの同じマップを使う
- 4マップの形状: ①外周＋十字（7×7、既存レイアウトそのまま）／②外周＋「H」字の内部通路2本（9×7）／③外周＋中央3×3の広場（7×7）／④外周＋十字を拡大（9×9、ラスボス戦向けの一番広いマップ)。全マップ、開始マス起点のBFSで全タイル到達可能なことを確認済み（孤立マスなし）。実素材の背景画像はまだ1種類（`stage1.png`）しか無いため、当面は全マップ共通のプレースホルダーとして使う
- **対人戦のマップ選択**（`main.js`）: ホストが「部屋を作る」を押すとまず`#pvp-map-select-screen`（4マップのサムネイル一覧）が出る。サムネイルは`board.js`の`createMapThumbnailCanvas()`が盤面レイアウトの配色（属性色/開始・チェックポイント・ショップ色）だけから毎回その場でcanvas描画する（実素材の画像は使わない）。マップをタップすると縮小プレビュー付きの確認ポップアップ「『（マップ名）』で遊びますか？ はい/いいえ」（`#pvp-map-confirm-modal`）が出て、はいで初めて`createPvpRoom({..., mapId})`を呼ぶ（部屋の`mapId`フィールドとしてFirestoreに保存、ゲスト側もこれを読んで同じ`createBoard(mapId)`を呼ぶので同一盤面になる）
- **対人戦ロビーのデッキ選択タイミングも変更**: 参加者（ゲスト）は部屋コードを入力して「入室する」を押した直後にその場でデッキを選び、選んだデッキリストは入室と同時に`guestDeckList`としてFirestoreへ書き込む（`joinPvpRoom`のuid/status更新と同じ1回の書き込みにまとめている - firestore.rulesの更新権限がゲストは「入室の瞬間」以外は`guestResponse`系フィールドしか書けない制約のため、デッキも入室と同時に書く必要がある）。そのためロビー画面に入った時点でゲストはもう準備完了で、あとはホストの「対戦開始」を待つだけになる。ホストは対戦開始を押した時点で`guestDeckList`を読むだけで済み、以前あった`relay.ask('pickDeck', {})`（対戦開始のたびにゲストへ質問を飛ばす往復）は不要になったので削除した
- ホストが「対戦開始」を押すと`beginPvpMatch()`が部屋の`status`を`'battling'`に更新し、ゲスト側は購読中のロビーリスナーがこの遷移を検知して`startPvpGuestBattle()`を呼ぶ（以前はpickDeck質問への回答自体が対戦開始の合図を兼ねていたが、デッキ選択を入室時点に前倒ししたため、対戦開始専用の一方向シグナルとして`status`遷移を新設した）
- **未検証のまま残っている部分**: ローカルのFirebaseエミュレータ（Firestore/Auth）がこのセッションでは起動できなかった（Java未インストール、`winget install Microsoft.OpenJDK.21`が別セッションで34時間ハングしていたのを発見しkillした経緯あり）ため、マップ選択→部屋作成→ゲスト入室→対戦開始という一連の実際のFirestore経由の往復はホスト・ゲスト2ブラウザでの実機確認ができていない。マップ選択画面・確認ポップアップ・サムネイル描画・「部屋を作成できませんでした」への失敗時フォールバックまでは実機確認済み。ストーリーモード4マップは全て実機で対戦開始・ダイス・移動まで確認済み（CPU戦のデフォルト盤面も回帰なし）

## ②マダイの岩礁マップ差し替え + ほこら/全チェックポイント制ゴール（2026-08-11実装）
- ユーザー指定のレイアウトに差し替え（`board.js`のMADAI_ROWS、15×5）。左右2つの縦長ループを、中央のG（開始マス）を通る横一列の通路が橋渡しする「めがね」型。四隅がH（ほこら）とC（チェックポイント）の対角配置（左上H・右下H、右上C・左下C）。ショップマスは無し（ユーザー指定に無いため）
- **このマップだけ「全チェックポイントを通過しないとゴールにならない」ルール**が有効（`MAPS`の`madai`エントリに`requireAllCheckpoints: true`。他の3マップはfalseのまま＝従来通り無条件でゴールボーナス）。`Game`はプレイヤーごとに`passedCheckpoints`（通過済みチェックポイントtile idの集合、`_movePlayer`のループで通過・着地どちらでも記録）を持ち、`_grantGoalBonus`が全チェックポイント通過済みの時だけ+100Gを渡してから記録をクリアする（未達成ならログだけ出してボーナス無し。次ラップでまた全部通り直す必要がある）
- **ほこらマス「マダイの福音書」**: 新タイル種別`TileType.SHRINE`。ちょうど止まると4種類の効果からランダムに1つ発生（`Game._resolveShrineTile`）:
  - 混沌を愛せ: 手札を全て捨て、デッキ（山札+捨札）を丸ごと再シャッフルしてから5枚引き直す（`Deck.resetShuffle()`を新設）
  - 力こそパワー: 盤上の配置済みモンスターから1体をランダムに選び、「倍化」という名の永続呪い（既存の呪いインフラ`applyCurse`をそのまま流用 - 呪いの`addedAtk`にその時点の基礎ATKと同じ値を入れるので実質倍になる）をかける。盤上にモンスターが1体も無ければ不発
  - 速度違反はご愛嬌: 次のプレイヤー人数分の手番だけ、サイコロフェーズ自体を見せずに強制的に10マス進ませる（`Game.forcedDiceRemaining`、`_beginTurn`が消化）。フェーズが無いのでその間はスペルも使えない
  - 右の頬をシバかれたら、左の頬をシバきなさい: 盤上の配置済みモンスターの土地全てに「強制停止」の呪い（`tile.forcedStopCursed`）をかける。呪われた土地は所有者本人と**同盟仲間**（ユーザー指定の追加ルール）以外、通過中でも足を止められ通行料を払うか戦闘するしかなくなる（`_movePlayer`のループ内で判定・強制的に移動を打ち切る）。呪いは対象の土地で実際に戦闘が起きると消える（`_runInvasion`/`_humanMoveFlow`が戦闘解決直後にクリア）
  - `getTileSummary`の`unitAtk`/`unitHp`は呪い込みの実数値を返すよう修正（以前はdef値そのままで「倍化」が数値に反映されなかった）。土地の情報表示にも「呪い: 強制停止中」の注記を追加

## ③決闘の浜辺マップ差し替え + ワープマス（2026-08-11実装）
- ユーザー指定のレイアウトに差し替え（`board.js`のBUDOU_ROWS、11×12）。メインの盤面（外周ループのみ、11×6）と、行と行の間を丸ごと空白にして物理的に隔絶したもう一つの島（4×4のシンプルな外周ループ）の2領域から成る。2領域はtileの隣接関係としては一切繋がっていない（Node上のBFSで確認済み）
- **ワープマス**: 新タイル種別`TileType.WARP`。V（メイン盤面に1マスだけ）とP（別の島の四隅、4マス）の2種類のコードがどちらもこの型になる。ちょうど止まった時だけ発動（通過しただけでは何も起きない - ほこらと同じ挙動）。VはPのうち「先頭の1つ」（`createBoard`がtile生成順=盤面走査順で最初に見つかるP、実質的に一番上・一番左の角）へ、4つのPはどれに着地してもVへ、瞬間移動する。着地時点でリンク先を計算するのではなく`createBoard`の末尾で`tile.warpTargetId`として盤面生成時に一度だけリンク付けしておく方式（`Game._resolveWarpTile`はそれを読むだけ）
- ワープは一歩ずつ歩くtween演出をせず、その場で座標をスナップする（「瞬間移動」の意図的な演出）。ワープ後は移動元の概念が無くなるので`previousTileId`をリセットする（次に分岐に差し掛かった時、全方向が候補になる）
- **検証方法についての注記**: このセッションのBrowserペインは`document.hidden === true`（実際にはコンポジット/描画されていない）状態で固定されており、`requestAnimationFrame`に依存するアニメーション（`utils.js`の`tween()` - 盤面の1マスごとの移動、`GameScene.panTo`のカメラパン）が一切発火せず、ダイスを振った後の実際の移動が画面上では永久に完了しない（`Game`側のロジック自体は正しく動いていても、視覚的な確認ができない）。そのため②③の新マップ・新メカニクス（チェックポイント制ゴール・ほこら4効果・強制停止＋同盟免除・ワープ）はNode.js上で`requestAnimationFrame`をポリフィルした上で`Game`クラスを直接動かすテストスクリプトで検証した（盤面の連結性・ワープの紐付け・各効果の実際の状態変化を全てアサーションで確認、全て成功）。ブラウザでの確認は「戦闘開始時点の初期状態が正しく組まれる」ところまで（プレイヤー構成・盤面のタイル種別・コンソールエラー無し）に留まる - ダイスを振ってからの実際の移動アニメーションは次回、Browserペインが正常にコンポジットされるセッションで改めて実機確認するのが望ましい

## ④暗転した世界マップ差し替え＋全マップ共通「全チェックポイント制ゴール」＋パネルの通過状況表示（2026-08-11実装）
- ④ダンボール男戦のマップをユーザー指定のレイアウトに差し替え（`board.js`のDANBALL_ROWS、11×11）。外周ループ＋「田」字型の十字通路、チェックポイント4つ（四隅寄り3つ＋盤面中央1つ）、右辺中央に3連続のほこら（H×3、縦一列）。開始マス起点のBFSで全85マス到達可能を確認済み
- **「全チェックポイントを通過しないとゴールにならない」ルールを全4マップ共通に変更**（以前は②マダイの岩礁だけの専用ルールだったが、ユーザー指定で全ステージに拡大）。`MAPS`の全エントリに`requireAllCheckpoints: true`を設定。ロジック自体（`Game._grantGoalBonus`/`_hasPassedAllCheckpoints`）は②実装時のまま変更なし、②以外の3マップにも同じ判定が及ぶようになっただけ
- **チェックポイント番号付け**: `createBoard()`がEVENT(チェックポイント)タイルに盤面走査順（生成順）で1から通し番号(`tile.checkpointNumber`)を振る。マップごとの実在番号一覧は`Game.checkpointNumbers`としてコンストラクタで一度だけ計算し、`_notifyState`/PvPの`_pvpSnapshot`が毎回そのまま送る
- **プレイヤーパネルに通過状況を表示**: 各プレイヤー名の下に、そのマップのチェックポイント番号を暗い丸バッジで並べる（`renderPlayerPanels`が`checkpointRowHtml()`で描画、CSSは`.checkpoint-num`/`.checkpoint-num.lit`）。該当プレイヤーがそのチェックポイントを通過済みなら明るい黄色に点灯（`player.passedCheckpointNumbers`、Gameが`player.passedCheckpoints`のtile idを`checkpointNumber`に変換して送る）。ゴールでボーナスを獲得すると全て消灯し次のラップの通過待ちに戻る。チェックポイントの無いマップは現状無い（全マップ最低2個以上）が、無い場合は何も表示しない設計にしてある
- 実機確認: ④の戦闘開始直後、両プレイヤーのパネルに「①②③④」の4バッジが全て消灯状態で正しく表示されることを確認（CSSの色・サイズも意図通り）。点灯側の動作（実際にチェックポイントを通過してからの点灯遷移）はダイス移動アニメーション自体がこのセッションのBrowserペイン制約で確認できないため、Node上のGameクラス直接テストで検証済み（`passedCheckpointNumbers`が通過したチェックポイントの番号だけを正しく含み、ゴールでクリアされることをアサーションで確認）

## 土地レベルの縁取り表示（2026-08-10実装）
- Lv2以上の土地マスに黒い縁取り（枠）を重ねて表示する。Lv2=細い一重、Lv3・Lv4と段階的に太くなり、Lv5だけ太さを戻して二重の細い枠にする（Lv1は枠なし）。`scene.js`の`GameScene.updateTileLevelBorder(tile)` - `THREE.ExtrudeGeometry`で作った平たい額縁状ジオメトリをtile.meshの子として重ねる方式（GL線の太さはブラウザ依存で制御できないため）
- 呼び出し箇所は土地Lvアップ（`_humanLevelUpFlowForTile`）と土地売却（`tile.level`を1にリセットする`_humanSellLandFlow`）の2箇所のみ - 属性・所有者・配置モンスターの変化ではtile.levelは変わらないので、それらの操作では呼ばれない

## 土地コマンド「特殊能力」（2026-08-10実装）
- 配置されたモンスターが持つ固有能力を行使する土地サブメニューの新項目。カード定義に`ability`フィールド（例: `{ type: 'damage', range: 3, power: 10 }`）を持たせるだけで有効化される汎用仕組み（`game.js`の`_humanAbilityFlow`/`_tileDistance`）。射程はマス目のグラフ距離（隣接関係でのホップ数、移動で使うのと同じ`tile.neighbors`）で判定する
- 第一号として`battleCards.js`に「火炎瓶男」（火・R・ATK20/HP25・コスト40）を追加。射程3マス以内の敵モンスター1体に10ダメージを与える
- 対象は自分と別陣営（同盟仲間は対象外）で、射程内にいる全ての敵から土地サブメニュー内の一覧UI（新モーダル`#ability-target-modal`）で選ぶ。ダメージで対象のHPが0以下になったら即座に倒れる（土地は無人化、`_handleUnitDeath`経由で不死鳥特性も通常の戦闘死と同様に効く）
- 特殊能力の使用は移動・売却と同様、実行すると自動でそのターンが終了する
- 土地サブメニューの「特殊能力」ボタンは、配置モンスターが`ability`を持つ土地でのみ表示される（`getTileSummary`の`hasAbility`フィールド）

## ストーリーモード（`chinu-quest2-story-spec.md`、2026-08-09実装）
- `src/story.js`: `STORY_STAGES`（全4ステージのタイトル・形式・登場人物・intro/outro会話・報酬定義）。立ち絵グラフィックは未着手のため`speaker`名だけのプレースホルダー表示（画像パスを持たせる構造ではなく、後日`portrait`等のフィールドを足すだけで差し替えられるようテキストのみで組んである）
- ハブの「ストーリー」タイル → `#story-screen`（ステージ選択・ロック/クリア済み表示） → `#story-dialogue-screen`（1行ずつクリックで進める会話）→ バトル本編 → outro会話 → ステージ選択に戻る、という一連の流れをmain.jsに実装
- **バトル本編は通常の対戦と同じ`Game`クラスをそのまま使う**（後述のN人対戦エンジン汎用化により、`playerConfigs`と`storyMode:true`を渡すだけで1vs1・1vs1vs1・2vs2同盟戦のいずれも成立する。ストーリー専用のバトルエンジンは別途作っていない）
- 進行状況は`character.storyProgress`（クリア済みステージ数）としてキャラクターデータに保存。既存アカウントには`ensureBreedFields`でデフォルト値0を補完
- ステージ③クリア報酬「ペーの杖」は`battleCards.js`の`ITEM_CATALOG.peeStaff`（EXレア・武器・ATK+25/HP+10・先制付与特性）として実装、クリア時に`ownedCards`へ自動付与
- 敗北時はM変換・進行度変更なしでステージ選択に戻るのみ（再挑戦前提、ペナルティなし）
- **NPCデッキサンプル（2026-08-09実装）**: 各キャラは`battleCards.js`の`buildThemedDeckList({elements, featuredMonster, featuredItem})`で組んだ固有の40枚デッキを持つ（`story.js`の各キャラ`theme`フィールド）。属性2種縛り＋任意で名前付きモンスター/アイテムを4枚ずつ混ぜることでキャラごとに個性を出している（例: ヒトデ=水/森+港区女子、暴君マダイ=火/雷+サラリーマンダー）。ラスボスのダンボール男だけは`elements: [neutral]`の1属性縛りで組んでおり、`buildCardPool`のランク上昇（同属性内での連番ランクに応じてステータスが伸びる仕様）がそのまま働くことで自然とATK/HPが他キャラより大幅に高くなる（無属性なので属性有利/不利も一切受けない）。カード規模が小さい現段階での簡易差別化であり、本格的なキャラ専用カードが増えたら置き換わる想定

## N人対戦エンジンの汎用化（`src/game.js`、2026-08-09実装）
ストーリーモードの1vs1vs1・2vs2同盟戦を成立させるため、`Game`のプレイヤー構築を汎用化した。
- コンストラクタが`playerConfigs`（任意人数のプレイヤー設定配列）を受け取れるようになった。省略時は従来通りhumanPlayer+固定CPUの2人構成にフォールバックするため、通常の対戦モードは無変更で動作する
- ターン進行・CPU行動ロジック・同盟（`allianceId`）の通行料免除／連鎖合算／総資産合算は元々プレイヤー人数に依存しない実装だったため、変更不要でそのまま3人・4人構成に対応した
- `storyMode`フラグを追加。通常モードでは従来通り「破産＝500Gを渡されゴール地点から再スタート」だが、`storyMode:true`では破産＝脱落（盤上の土地・ユニットを手放し、駒を非表示化）として扱い、`_checkStoryWinCondition`が生存陣営数を判定する（`allianceId`の集計だけで1vs1vs1のFFAも2vs2の同盟戦も同じロジックで勝敗が付く）。勝敗確定時は`onStoryBattleEnd({won, alivePlayerIds})`コールバックでUI側に通知
- プレイヤーパネル（`renderPlayerPanels`）に脱落時のグレーアウト表示（「（脱落）」ラベル＋`opacity`低下）を追加

---

## フェーズ0: ログイン・キャラメイク・モード選択ハブ（骨組み実装済み・ローカル仮認証）

### 初回起動フロー
1. **ID・パスワードで新規登録 or ログイン**（Firebase Authenticationを想定）
2. 初回ログイン時のみ**キャラメイク**: 好きなキャラ画像を選ぶ（盤面上の自キャラアイコンになる。現状の丸いプレースホルダー色アイコンに代わるもの）→ 名前を決定 → 初期デッキを2種類から選択
   - キャラ画像はプリセットのチヌ・アンコウ・イカから選択するか、任意の画像をアップロードできる。アップロード画像は中央を正方形に切り出し、512px WebPへ縮小してキャラクターデータに保存する。選択画像は盤面駒と主人公の会話立ち絵に共通利用し、旧`iconIndex`形式のセーブデータも互換表示する

### 2回目以降のログイン
- ログイン後はいきなり対局に入らず、**モード選択画面**（ハブ画面）を経由する
  - **ストーリー** — 1人プレイでストーリーを進める（新規モード。シナリオ設計は今後詳細化）
  - **対戦** — CPU戦または対人戦（対人戦はフェーズ5のオンライン対戦に相当）
  - **デッキ** — カード図鑑から選んで40枚のデッキを編集する（フェーズ3のブック編成に相当。骨組み実装済み、詳細はフェーズ3参照）
  - **ショップ** — 所持Gでパックを購入。パックは複数種類あり、レアリティごとに排出率が異なる（フェーズ4のガチャに相当）
  - **ブリード** — 所持Gを使ってブリードモンスターのステータス・特殊能力をセットする（フェーズ4のブリードモンスターに相当、今回「お金を使ってステータス構成を組む」という具体的な操作方法が確定）

### 既存フェーズとの対応関係
このハブ画面は独立した新規要件ではなく、フェーズ3〜5の入口を1つの画面にまとめたもの。「対戦」の中の“CPU戦”が現在実装済みの盤面ゲームそのものにあたる。実装順としては、まずログイン〜モード選択〜対戦(CPU)の骨組みを通してから、デッキ／ショップ／ブリード／ストーリーを順次埋めていく想定。

### 実装状況（骨組みのみ）
- `src/auth.js`: ローカル仮認証（localStorageにID/パスワード/キャラ情報を保存するだけ、Firebase未接続）。`loginOrRegister(id, password)`で新規登録とログインを兼ねる、`saveCharacter(id, character)`でキャラ情報を保存
- キャラメイク画面: アイコンは実素材がまだ無いため色スウォッチ6色から選ぶプレースホルダー（盤面のコマ色になる）。名前入力（10文字まで）。初期デッキは`battleCards.js`の`STARTER_DECKS`（火・森ブック／水・雷ブックの2種、下記フェーズ3参照）から選択
- モード選択ハブ: 「対戦」「デッキ」「カード図鑑」「ショップ」「ブリード」が実際に機能する。CPU戦を選ぶと`main.js`の`startBattle(character)`が呼ばれて既存の盤面ゲームが開始する（対人戦ボタンは無効化表示）。**ストーリーのみ**「準備中です」のスタブ画面（戻るボタンのみ）が残っている
- `game.js`の`Game`コンストラクタに`humanPlayer: { name, color, deckVariant, deckList }`を渡せるようにし、盤面ゲーム側の自キャラ名・コマ色・デッキがキャラメイク（またはデッキ編集）の選択を反映するようにした

---

## フェーズ1: 盤面・移動システム

### カメラ・ビュー
- カルドセプト風の見下ろし視点（斜め上からのアングル）
- コマが1マス移動するごとに、カメラもなめらかに追従（線形補間でスライド）
- コマが停止した瞬間、カメラも静止。イベント発生時はズームイン等の演出を想定（フェーズ1では簡易でよい）

### 盤面（ステージ1・分岐あり、実装済み）
- 周回コースから分岐ありのグラフ構造に変更済み。`board.js`の`STAGE1_ROWS`にASCIIアートでマス配置を直書きし、各マスは隣接マスのid配列`neighbors`を持つ（`generateLoopGridCoords`による単純な周回生成は廃止）
- ステージ1のレイアウト: 外周ループ＋中央を貫く十字（外周4辺の中点と中央がそれぞれ3〜4方向に分岐する交差点になる）
  ```
  G火火火火火C
  水　　火　　雷
  水　　火　　雷
  水水水無雷雷雷
  水　　森　　雷
  水　　森　　雷
  C森森森森森S
  ```
  G=スタート、C=チェックポイント、S=ショップ、火/水/雷/森/無=空き地属性
- マス種類: `TileType`に`START`/`LAND`/`EVENT`/`SHOP`の4種（ショップマスを新規追加）

### ゲームループ
以下の①②③の流れで実装済み（フェーズ2の土地コマンドを先行実装）。
- ① サイコロを振る → コマが1マスずつ進むアニメーション
- ② 特殊マス（ゴール＝スタートマス／チェックポイント＝イベントマス）ならその処理。敵の土地に止まった場合はここで通行料を自動精算
- ③ 止まったマスが土地なら「土地コマンド」メニュー（フェーズ2参照）
- ターン制（プレイヤー人数分を順番に処理。フェーズ1はCPU 1体との対戦を想定）

---

## 手札・ドローシステム（フェーズ1で先行実装済み）
ターンループの土台として、ブック（デッキ）3本柱に先行してドロー・手札まわりを実装した。

- ブックは40枚固定（モンスター4属性×6枚＝24枚、武器防具8枚、スペル8枚）
- カード種類: モンスター（4属性: 火・水・風・地、カルドセプトの地形属性に準拠）、武器防具（戦闘用）、スペル
- ゲーム開始時、各プレイヤーはブックからランダムに4枚ドローして初期手札とする
- 手札上限は6枚。ターン開始時に1枚ドローし、これが7枚目になった場合はどれか1枚を選んで捨てる
- 自分の番のドロー時はカード画像を大きくアップ表示してから手札に加える（CPUのドローは非公開情報として画面には出さない）
- ブックを1周（山札を引き切る）したら、捨て札を山札にシャッフルして戻す
- 手札表示は2系統: (1) 自分の手札は画面左下に常時小さく表示、自分だけいつでもタップしてカード情報を確認できる。(2) ターンが来たプレイヤーの手札は画面中央上部（サイコロの下）に、左下の2倍サイズで表示し、サイコロを振った瞬間に消える。CPU（対人戦なら相手）のターンでも中身をそのまま表示する（伏せない）。ただし左下の常時パネルは常に自分の手札のみで、相手の手札はそこには出ない
- スペルはターン毎に1回、サイコロを振る前だけ中央の手札から使用可能。使用すると即座に手札から消費され、効果発動演出のあと自動的にサイコロを振れる状態に戻る（実際のスペル効果はフェーズ2以降のバトル設計で詳細化）
- サイコロは目押し方式: 1回目のクリックでスピン開始（1〜6の目が高速で切り替わる）、2回目のクリックで停止を予約し、そこから0.7秒後に表示されていた目で確定する（クリックの瞬間に即決まらないスキル要素）。CPUも同じ`rollDice(steps)`経路で乱数を振るが、目押し操作はプレイヤーのみ
- CPUの意思決定（サイコロを振るまでの間、土地購入判断、捨て札選択）には毎回一呼吸の待ち時間を入れ、テンポが速すぎないようにする

## フェーズ2: モンスターバトルシステム
- CPU戦の強さ調整（初心者向けの難易度からスタートし、対人戦へのステップとする）
- **土地の獲得は「購入」ではなく「召喚」**: 空き地にモンスターカードを召喚することで自分の土地にする。土地購入コストは廃止し、召喚コストのみを消費する
- **侵略**: 他プレイヤーが所有する土地にも、召喚と同じコスト・ルールでモンスターを送り込み、バトルによって奪う（＝侵略）。召喚と侵略は同一の仕組み（対象が空き地か、敵の土地かの違いのみ）

### 土地コマンド（全面刷新済み）
止まったマスが土地・スタート・チェックポイントのいずれかの場合、以下の3択メニューが表示される（ラベルにA/B/C等の記号は付けない）。
- **召喚（敵地なら侵略）** — 今止まっているマスが土地の場合のみ有効。手札の（コストが払える）モンスターカードを選択（選ばれたカードは2回点滅）→「◯◯しますか？ コスト◯G」ではい/いいえ確認 → 空き地なら召喚、敵地ならバトル発生（戦闘結果は下記バトルルール参照）、自陣（既に自分のモンスターがいる）なら入れ替え（下記参照）。実行したら自動でターン終了
- **土地** — カメラワーク画面に遷移し、**このターンの移動で通過したマス**（サイコロの出目の数だけ、着地マス含む）だけがゆっくり点滅・淡く発光する。ただしスタートまたはチェックポイントにぴったり止まった場合は例外的に**自分の所有地すべて**が対象になる（本拠地的な扱い）。発光しているマスをタップすると：
  - 自分のモンスターが配置されている土地 → 縦並びのサブメニューが表示: **入れ替え**（新しいカードの召喚コストが必要。元々配置されていたモンスターのカードは手札に戻る）／**土地Lvアップ**／**属性変更**／**移動**（下記参照）／**情報**／**もどる**
  - それ以外（空き地・敵地・自分のモンスターがいない自分の土地は存在しない — 下記の所有権モデル参照）→ タップしてもそのマスの情報がポップアップするだけで、サブメニューは出ない
  - サブメニューの「土地Lvアップ」「属性変更」「入れ替え」「移動」はいずれも実行したら自動でターン終了。「情報」はポップアップを閉じると同じサブメニューに戻る。「もどる」でこの土地ブラウズを終了しトップの3択メニューに戻る（ターンは終了しない）
- **終了** — 何もせずターン終了
- CPUは召喚/侵略ロジックのみ自動実行（土地ブラウズ相当の操作は行わない）

**移動（新規）**: サブメニューの「移動」を選ぶと、配置されているモンスターに隣接する土地（`neighbors`、空き地または敵地のみ・自分の他の所有地には移動不可）へ向けて矢印が表示される（分岐選択と同じ画面斜め方向の矢印UI）。1つ選ぶと「移動しますか？」のはい/いいえ確認。空き地なら戦闘なしでそのまま移動。敵地なら侵略と同じバトルが発生し、以下の3通りに分岐する：
  - 侵略側が生存・防衛側が死亡 → 移動先の土地を奪取（そこへ配置）。元いた土地は所有権を失う（下記モデル参照）
  - 両者生存（防衛成功） → モンスターは元の土地にとどまる（実質何も変化しない）
  - 侵略側が死亡（防衛側の生死問わず） → モンスターは消滅。元いた土地は所有権を失う。防衛側も同時に死亡していれば移動先の土地も所有権を失う
  移動（および移動先での戦闘）が発生したら、結果を問わず自動でターン終了

**土地の所有権とレベルのモデル（確定仕様）**:
- **所有権はモンスターが配置されている間だけ成立する** — 「所有しているがモンスターがいない」という中間状態は存在しない。モンスターが死亡・移動で離れた瞬間、そのマスの `owner` は必ず `null` に戻る
- **土地のレベルは所有権と無関係に保持される** — 所有権を失っても（土地を売る、という未実装の将来機能を除き）レベルは下がらない。無人になった土地を誰か（元の持ち主自身を含む）が改めて召喚/侵略/移動で獲得すると、そのマスに残っていたレベルがそのまま引き継がれる（＝「捨てられた高レベル地」を拾うのが得になる）

**通行料（先行実装から仕様変更・確定仕様）**: 敵の土地に着地した瞬間の即時徴収はやめ、その土地コマンドが終わる瞬間に一度だけ確定する。
- 侵略を試みて**勝ち、土地を奪った場合は通行料は一切かからない**（自分が奪った土地に自分で通行料を払うのはおかしいため）
- 侵略に**負けた/そもそも侵略しなかった**（別の所有地をレベルアップするだけ等）場合は、今まで通り通行料を徴収する
- 「移動」コマンドでの侵略はもともとプレイヤーの駒がその土地に立っていないため、勝敗に関わらず通行料の対象外（変更なし）

### ショップマス（先行実装済み）
- 止まると図鑑からランダムに3枚（コストのあるモンスター・アイテムのみ。スペルはコスト概念が無いため対象外）を提示。選んだカードは2回点滅→「「◯◯」を購入しますか？ コスト◯G」ではい/いいえ確認→はいで即座に手札に加わる（いいえの場合は3枚の選択画面に戻る）
- **ここで買ったカードはその対戦中だけ手札に存在する一時的なもので、`character.ownedCards`（永続所持枚数、フェーズ3のデッキ編集で使うもの）には一切影響しない**。対戦が終われば（退出しても）自然に消える。ハブの「ショップ」画面（永続所持カードの売却）とはコード上も完全に別系統
- CPUはショップマスに止まっても何もしない

### バトルルール（全面刷新済み・盤面の侵略コマンド／移動コマンドの侵略から実際に呼び出される）
- 属性の弱点サイクル: 火→水→雷→森→火（各属性は次の属性に弱い＝1.2倍ダメージ）。無属性（無色とは別概念、モンスター専用）はこのサイクルの対象外で、弱点も特攻もない
- **戦闘は同時ではなく順番制**: 先制・後攻などの特殊能力が無い限り、侵略側（攻撃側）から先に攻撃する。防御側は攻撃側の一撃で死亡した場合、反撃する機会がないまま戦闘終了（＝攻撃側は無傷）。防御側が生き残った場合のみ、続けて反撃する。この結果、通常戦闘では「相打ち（両者死亡）」は起こり得ない（将来、反撃を待たずにダメージを与えるような特殊能力が実装されれば別）
- 侵略の勝敗判定: 防衛側が死亡＆侵略側が生存 → 侵略側が土地を奪取（「〇〇の□□は土地を奪った」と表示）。それ以外（防御側が生存、または両者死亡）→ 防御側が土地を守った扱い（「〇〇の□□は土地を守った」と表示）
- 武器・防具（総称「アイテム」）は装備先モンスターのATK/HPに加算されるが、勝敗を問わず戦闘後に消滅する（一部復活するものを今後実装予定）
- モンスターにかけるスペルは「呪い」と呼び、時限的なもの以外は永続。加算ATK/加算HPとして基礎値に上乗せされ、モンスターが戦闘で死亡した時点で消滅する
- **同属性ボーナス（先行実装済み）**: 自分の土地に配置されているモンスターは、その土地の属性が自分の属性と一致していれば、土地レベル×10（最大50）のHPが加算される。所有権がある（＝配置されている）モンスターにのみ適用され、手札から召喚したばかりの侵略側にはこのボーナスは無い（移動コマンドで元の土地から侵略する場合は元の土地基準で判定される）
- **応援ボーナス（先行実装済み）**: 戦闘が発生した土地に隣接するマスに、その戦闘に参加しているモンスターと同じ所有者の別モンスターが配置されていれば、ATK+10。攻撃側・防御側どちらにも同じ判定を独立に適用する（自分自身は対象外）
- 初期実装モンスター: サラリーマンダー（火, ATK30/HP30, コスト50, 土地に生き残ったら残HP×2ゴールド獲得）、港区女子（水, ATK30/HP30, コスト50, 攻撃成功時に相手の手持ちGから30G奪う）。**両者とも2026-08-12実装で`src/fireMonsters.js`/`src/waterMonsters.js`のデータ駆動カタログに移行済み**（旧仕様の港区女子固有の耐性0.8倍・ダメージ連動ゴールド略奪はbattle.jsのハードコード特殊分岐ごと廃止し、汎用`effect.type: 'stealGoldOnHit'`に置き換えた）
- 初期実装アイテム: ナイフ（武器, コスト5, ATK+10）、なべのふた（防具, コスト5, HP+10）
- 初期実装スペル: マ〇ジャロ（対象モンスターに加算ATK+10・加算HP+20の呪いを付与、永続）
- **特殊効果（先行実装済み・`unit.def.traits`配列で保持、現状はブリードモンスターのパーツのみが付与元）**:
  - **先制**: この特性を持つ側が防御側の場合、通常と逆に防御側から先に攻撃する（攻撃側も先制を持っていれば通常通り攻撃側が先）
  - **被ダメージ半減**: この特性を持つ側への被ダメージが0.5倍になる（攻撃側が貫通を持つ場合は無効化される）
  - **貫通**: 攻撃側がこの特性を持つ場合、防御側の同属性ボーナス（土地加算HP）を0として扱い、防御側の被ダメージ半減も無効化する。アイテムによるHP加算は無効化できない
  - **不死鳥**: この特性を持つユニットが死亡すると、カードが（新しいidで）持ち主の手札に戻る。これにより手札が7枚になった場合は通常の手札上限処理と同じ「捨てるカードを選ぶ」画面に移行し、捨てたカードはもう戻ってこない
  - **強盗**: この特性を持つ側の攻撃が成功（ダメージ>0）すると、与えたダメージ×3のゴールドを相手の手持ちから奪い自分（使用者）に還元する（港区女子の同種能力は×1）

### バトルフェーズの演出（全面刷新済み）
侵略が発生すると盤面画面から専用のバトルフェーズ画面へ遷移する。
1. **暗転して移行**: 画面が暗転し、攻撃側のカードを画面左、防御側のカードを画面右に配置する
2. 各カードの左上・右上に所有者名、HP、ATKを表示。ラベルは単に「HP」「ATK」（「基礎HP」とは表記しない）。応援ボーナス（ATK）・同属性ボーナス（HP）が乗っている場合のみ、基礎数値の横に「＋◯◯」と表示（両ボーナスとも、この時点で確定しているため先に見せられる。アイテムの加算分はまだここには出ない）。**カード下部には有利/不利属性のラベルも表示**（弱点サイクル上、自分が相手に1.2倍ダメージを与えられる側なら「有利！攻撃1.2倍」、逆に相手から1.2倍を受ける側なら「不利…被ダメージ1.2倍」、どちらでもなければ非表示）
3. **アイテム選択（攻撃側→防御側の順、非公開）**: まず攻撃側の手札（アイテムのみ）が表示され、1枚選ぶか「使わない」を選択。選んだ瞬間の視覚的な演出（点滅等）は無く、防御側の画面には攻撃側が何を選んだか一切表示されない。攻撃側の選択が終わってから防御側の手札が同様に表示され、選び終えたら戦闘開始。選んだアイテムは戦闘後、勝敗を問わず手札から消費される（永続所持カードではなく、その場で使い切り）
4. **攻撃実行**: 特殊能力がある場合はダメージ計算前に発動することがある（今後の拡張用）。先制/後攻の特殊能力が無ければ攻撃側から攻撃: 使ったアイテムがあれば攻撃側カードの左上に1/4サイズで重なって表示され、ダメージ計算メッセージ（「◯◯→△△に31ダメージ（倍率1.2）」形式）を表示。**メッセージは1.5秒間表示したままにする**。この一撃で防御側が死亡した場合、防御側のカードが下から崩れ落ちるエフェクトを出す（このメッセージの1.5秒の間に発生）。防御側が生存していれば、続けて防御側の攻撃フェーズが同様に発生（アイテム重畳・メッセージ表示・1.5秒待機・特殊効果は同様、攻撃側が死亡すれば同じ崩れ落ちエフェクト）。**両者とも生存した場合（引き分け）は、決着メッセージの前に両陣営のカードをそれぞれ自分の側の画面外へ退避させる**演出を挟む
5. **決着メッセージ（1.5秒表示）**: 攻撃側が勝った（防御側死亡・攻撃側生存）場合「〇〇の□□は土地を奪った」と表示し、フェードアウトして盤面へ戻る。奪った土地は**レベル・属性をそのまま引き継ぎ**、攻撃側モンスターがそこに残る。ただし戦闘で受けたダメージ分そのまま（基礎HPが全快せず減ったまま）配置される。それ以外（防御側が勝った/引き分けた）場合は「〇〇の□□は土地を守った」と表示。通行料の扱いは上記「通行料」節を参照
- 上記5種の実データカードはキャラメイクで選んだ初期ブック（下記フェーズ3参照）にそれぞれ4枚ずつ混入済み。それ以外の汎用プレースホルダーモンスターにも仮のATK/HP/コストを付与し、どのモンスターカードも召喚可能にしてある

### 土地の属性・レベル・資産価値（先行実装済み）
- 土地の属性は画面上の4象限（斜めカメラで実際に見える上下左右）で固定: 上＝火、右＝水、下＝森、左＝雷。周回コースの各辺がちょうど1属性に収まるよう、ワールド座標の対角線（x-z, x+z）で判定している
- **無色（薄いグレー）属性**も存在する。連鎖しない（連鎖倍率は常に×1.0）特殊な属性で、周回コースの角（4象限の境界にあたるマス、スタートマスを除く）に盤面生成時から配置される
- 土地コマンドの「土地属性変更」で、所有地の属性はいつでも変更可能（上記の4属性＋無色から、現在の属性以外を選択）。コスト＝現レベル×100G（対象が無色の場合は半額）
- 土地はレベルを持ち、初期Lv1・カルドセプト本家準拠でLv5上限
- **地価・通行料はカルドセプト準拠の計算式**（本家に「土地の売却」はなく、資産価値＝将来の通行料徴収ポテンシャルとして評価される）
  - 地価 = 基本地価（全マス共通150G固定） × レベル倍率（Lv1=×1〜Lv5=×5） × 連鎖倍率（同属性の所有地数によるボーナス、カルドセプト ビギンズ系の数値表: 連鎖1=×1.0／2=×1.5／3=×2.0／4=×2.5／5=×3.0）
  - 通行料 = 地価 × 通行料倍率（Lv1=0.3／Lv2=0.3／Lv3=0.4／Lv4=0.6／Lv5=0.8）
  - レベルアップのコストは地価に連動しない固定値: Lv1→2=50G／Lv2→3=200G／Lv3→4=400G／Lv4→5=600G
  - プレイヤーの総資産Gは「所持G＋所有地の地価合計」。レベルアップや連鎖（同属性の追加所有）で地価・通行料の両方が上がる
  - 現状の盤面は1周回コース＝属性ごとに1エリアなので、「エリア」は実質「属性」と同義（同じ属性の所有地＝1連鎖として数える）。将来、分岐や複数エリアを持つマップを追加する場合はエリアIDをマスに持たせて連鎖判定を分離する必要がある

### 進行方向・分岐選択（先行実装済み・分岐盤面に合わせて全面刷新）
- 盤面がグラフ構造になったため、「ゲーム開始時に1回だけ方向を決める」旧方式は廃止。**移動中に分岐マス（隣接候補が2つ以上）へ差し掛かるたびに毎回選択を求める**方式に変更（`Game._movePlayer`/`_chooseNextTile`）。1回のサイコロ移動の途中で複数回選択が発生することもある
- 選択UIは画面中央に斜め矢印（↖↗↙↘、最大4方向）が候補の分だけ表示され、1回タップで即決定（頻繁に発生するため、以前の「2回タップで確定」方式は廃止）。同時に土地情報と同じカメラワーク（上下左右パン、戻るボタンは非表示）も使えるので、分岐先を見てから選べる
- どの分岐先がどの矢印に対応するかは、盤面のグリッド座標の差分（世界座標の±X/±Z）を、斜めカメラでの実際の画面方向に変換して決めている（+X→↘／-X→↖／+Z→↙／-Z→↗）
- CPUは分岐でランダムに1つを選ぶ
- 分岐でない（隣接候補が1つだけの）マスは従来通り自動で通過する

## プレイヤー情報UI（先行実装）
- 画面四隅にプレイヤー情報パネル（アイコン・名前・所持G・総資産G[所持金+所有土地の評価額合計]・召喚数[所有マス数]）を表示。各プレイヤーの色で縁取り
- 最大4人対戦を想定した配置ロジック: 同盟戦でない場合はターン順に左上→右上→左下（左上の下）→右下（右上の下）。同盟戦の場合は最初のターンのプレイヤーのチームが左列（リーダー左上、同盟者は左下）、もう一方のチームが右列という配置
- ターン表示（「〇〇のターン」）は左上ではなく、中央のサイコロの右に大きめの文字で表示

## 同盟戦（ルール確定・ロジックは実装済み、対戦モード自体は未実装）
`player.allianceId`が同じ2人が組む2vs2相当のチーム戦。**4人対戦を開始する画面・フローがまだ無い**ため現状は常に`allianceId: null`同士（＝実質何も変わらない）だが、`Game`側のロジックは`allianceId`を見て以下のように自動で切り替わるよう実装済みなので、同盟戦の開始フローが実装され次第そのまま機能する。
- **仲間の土地では通行料を取られない**（`_settleLandingToll`が`tile`の所有者と着地したプレイヤーの`allianceId`が一致する場合は徴収をスキップ）
- **同属性の連鎖ボーナスは仲間の所有地も合算**して数える（`_chainMultiplier`。例: 自分が火の土地を1つ、仲間が火の土地を1つ持っていれば連鎖2として計算される）。**土地の所有権自体はあくまで個人**のまま（`tile.owner`・`_ownedTiles`は仲間分を含めない）で、連鎖カウントの合算だけが仲間戦の特典
- **総資産（画面表示）は2人の合計**になる（`_totalAssetsOf`。所持G＋地価の合計を両プレイヤーの「総資産」欄に同じ値で表示）。ただし「所持G」欄自体は個人のまま別々に表示される
- **強制停止（未実装の将来効果 - 指定マスに強制的に相手を止まらせる）にも仲間は巻き込まれない**想定。効果自体がまだ無いため、実装時にこのルールを反映すること
- **手持ちがマイナスになった際、パートナーの土地を代わりに売ることはできない**想定（「土地を売る」自体が未実装のため、実装時にこの制約を反映すること。破産処理は下記参照）

## 破産処置（先行実装済み）
所持Gがマイナスになったプレイヤー（自分のターンの行動で通行料や略奪特性等により発生しうる）は、そのターンの行動が全て終わった時点（`rollDice`の最後、次のプレイヤーに手番が移る直前）で自動的に**500Gを渡され、ゴール地点（スタートマス）へ強制的に再スタート**する（`Game._checkBankruptcy`）。**デッキ・手札の中身はそのまま**（何も失わない）。現在の操作プレイヤーだけでなく、そのターン中に（侵略の略奪効果などで）マイナスになった可能性がある全プレイヤーを毎ターン終わりにチェックする。土地の所有権はこの処理だけでは失われない。

## フェーズ3: ブック編成（デッキ編成）（骨組み実装済み）
- プレイヤーは「カード図鑑」（全カード一覧）から選んで40枚の「ブック」を編集する
- 1種類のカードにつき最大4枚まで、かつ**所持枚数を超えて編成不可**。合計ちょうど40枚になるまで保存不可（`MAX_COPIES_PER_CARD`/`DECK_SIZE`, `main.js`）
- カード名をタップするとイメージ（カード自体の色付き表示を拡大したもの。個別のカードイラスト素材はまだ無い）と説明（種別・レア度・属性・ステータス）のポップアップが開く（既存の`card-detail-modal`を流用）
- 複数ブックの保存・切り替え（実装済み・2026-08-10）: 最大3冊、詳細は後述の「複数デッキ（ブック）スロット」セクション参照

### カード所持・レアリティ・ショップ（骨組み実装済み）
- **所持枚数の概念**: `character.ownedCards`（カード名→枚数）。キャラメイク時に選んだ初期ブックの40枚がそのまま初期所持カードになる。デッキ編集の「＋」は所持枚数と4枚のどちらか小さい方が上限
- **レアリティは4種**: N・S・R・EX（`cards.js`の`Rarity`）。カード左上に表示。現状の割り当ては、ネームドモンスター2種=R、ネームドアイテム2種=N、ネームドスペル1種=S、汎用プレースホルダー全部=N（EXは現状未使用、今後の強力な実装カード用に予約）
- **種類アイコン**をカード右上に表示（`TYPE_ICON`）: モンスター=👹、スペル=⚡、武器防具=⚔️
- **ショップ画面**（実装済み・Codexが担当）: 所持カードのうち「所持数－ブック編成数」の余りがあるものだけ売却可能。売却額はN=15G／S=30G／R=100G、EXは売却不可（`RARITY_SELL_PRICE`）。売却すると`character.m`（永続所持M）に加算される。**パック購入も実装済み**（`src/shopPacks.js`、属性別モンスターパック×4＋アイテム／スペル／無属性パック、各100M、最低1枚S以上を保証する排出ロジック）

### 属性・カード規模（方針確定・コンテンツは今後拡充）
- 属性は5種: 火・水・雷・森・無属性（`cards.js`の`Element`）。弱点サイクルは火→水→雷→森→火（各属性は次の属性に1.2倍ダメージを受ける）。無属性のモンスターは弱点も特攻も無い代わりに、同属性ボーナス（土地属性とモンスター属性が一致すればHP加算、先行実装済み・詳細は上記バトルルール参照）も受けられない。土地属性とモンスター属性を同じEnumに統一したのはこのメカニクスのため
- 想定カード規模: モンスターは各属性20種（無属性含む）、アイテム30種、スペル40種くらい。現状は名前付きの実データが5種（モンスター2・アイテム2・スペル1）＋汎用プレースホルダー（`buildCardPool`、既定で計40種）のみで、本格的なコンテンツ拡充は今後
- **初期ブック（スターターブック）は2種**（`STARTER_DECKS`, `battleCards.js`）: 火・森ブック（サラリーマンダー軸）／水・雷ブック（港区女子軸）。それぞれ該当2属性に寄せて汎用モンスターも生成される（`Deck`の`elements`オプション）ので、単に名前付きカードが違うだけでなく実際に構成が偏る
- `src/cardCatalog.js`の`getCardCatalog()`が図鑑の中身（名前付きカード＋汎用プレースホルダー）を返す。デッキ編集画面はカード名で名寄せ・枚数管理する（`cardKey` = `card.name`。理由: 図鑑の生カード定義には`catalogId`が無く、汎用カードの`id`は生成のたびに変わるため、両者を安定して突き合わせられるのは`name`だけ）。唯一の例外がブリードモンスターで、いつでも改名できるため`catalogId==='breedMonster'`を優先的にキーとして使う（名前が変わってもデッキ内所持カウントが迷子にならないようにするため）。デッキ編集画面は`getCardCatalog()`固定のキャッシュとは別に、キャラクターごとの最新ブリードモンスター定義（`buildBreedCardDef`）をその場で合成した一覧を見る

## フェーズ4: カード収集・ガチャ・通貨・ブリードモンスター
- CPU対戦・対人戦の勝利で通貨を獲得（対人戦は難易度が高い分、報酬を厚めに設定）
- 通貨でパックガチャを購入し、新規カードを入手（**実装済み・Codexが担当**、`src/shopPacks.js`。詳細は上記フェーズ3のショップ画面の項を参照）
- レアリティ設計は先行実装済み（N/S/R/EX、上記フェーズ3参照）
- 通貨・ガチャ処理はクライアント改ざん防止のためサーバー側（Firebase Functions）で処理する
- **ブリードモンスター（実装済み・`src/breedParts.js`）**: ガチャ入手カードとは別に、プレイヤーが自分で育てた（ブリードした）オリジナルモンスター1体を`character.breedMonster`として持つ。初期値は名前「ブリモン」・無属性・ATK15・HP15・召喚コスト30G。「ブリード」画面でいつでも**改名**でき、**画像も登録**できる（カードエディタの画像アップロードと同じ仕組み: `FileReader`でdata URL化し`breedMonster.imageDataUrl`に保存、`buildBreedCardDef`経由でカード表示に反映されるので、図鑑・デッキ編集・対戦中の手札などどこでも自作画像で表示される）。名前・画像どちらも純粋に見た目の要素で、ステータス（ATK/HP/コスト/属性/特殊効果）はパーツ構成のみで決まる
  - **レアリティEX・初期デッキに1枚**: キャラメイク時に`buildBreedCardDef()`でその時点のブリードモンスターをレアリティEXの実在カードとして生成し、汎用モンスターを1枚外して初期40枚デッキに差し替える（`character.ownedCards`にも1枚として計上）。デッキ編集画面にも常に1行として表示され（`getCardCatalog()`の静的キャッシュとは別に、その場でブリードモンスターの最新ステータスを合成した一覧を見る）、0枚か1枚かの出し入れができる
  - **パーツの入手（実装済み）**: ショップ画面「ブリードパーツガチャ」で1回150Mを消費し、`BREED_PARTS`から3個入手する。各枠の基本排出率はN=65%／S=25%／R=10%。3個ともNになった場合は最後の1個をSへ差し替え、最低1個S以上を保証する。重複入手可能で、`character.ownedPartIds`に個数分のIDを保持する。「ブリード」画面では所持数の範囲で同じパーツを複数装着でき、1個ずつ取り外せる（装着・取り外しは無料・何度でも可逆）
  - **初期実装パーツ11種**（`BREED_PARTS`。costDeltaはブリードモンスター自身の召喚コスト(G)への加算）:
    - 属性パッチ（S, コスト+30G, 無色を除く4属性から装着時に選択・上書き）
    - ATKアップ（N, コスト+20G, ATK+10）／HPアップ（N, コスト+20G, HP+10）
    - ダブルアップ（R, コスト+35G, ATK+10・HP+10）
    - 攻撃特化（N, コスト+20G, ATK+20・HP-10）／防御特化（N, コスト+20G, HP+20・ATK-10）
    - 先制付与（S, コスト+30G, 特性「先制」）／被ダメージ半減（R, コスト+60G, 特性「被ダメージ半減」）／貫通（R, コスト+70G, 特性「貫通」）／不死鳥（S, コスト+40G, 特性「不死鳥」）／強盗（S, コスト+30G, 特性「強盗」）
    - 特性の実際の戦闘での挙動は上記バトルルール節の「特殊効果」を参照
    - 「ブリード」画面のパーツ一覧は質素なアイコンバッジで表示（⚔️=ATK、🛡️=HP、💰=コスト(G)、🎨=属性選択、特殊効果ごとに1文字絵文字（⚡先制／💠被ダメージ半減／🏹貫通／🔥不死鳥／🥷強盗）+ラベルテキスト）。`breedPartBadges()`が{icon, text}の配列を返す
  - **装着上限**: ATK・HP・召喚コストそれぞれ独立に上限（ATK70/HP70/コスト200）を判定し、いずれか1つが上限を超える場合はそのパーツを装着できない（他のステータスがまだ余裕があっても、その特定のパーツだけがブロックされる。例: HPが上限でもコストのみ変動するパーツは装着可）。装着した結果HPまたはATKが0以下になる場合もエラーメッセージを表示して装着を拒否（`canEquipPart`が理由付きで判定、装着ボタン自体も事前に無効化される）

## アセット（実素材の反映状況）
- ステージ背景: `public/images/stage/stage1.png`（海底イラスト）を盤面の地面（ground plane）テクスチャとして適用済み（`scene.js`）
- キャラアイコン: `public/images/player/icons6.png`（3×2グリッドの魚＋うさぎ6種シート）を`src/iconSheet.js`がブラウザ側でCanvas分割し、6枚の個別アイコンとして扱う（サーバー側の画像加工は不要）。キャラメイクのアイコン選択と、盤面上の自キャラ駒（ビルボードスプライト）の両方に使用
- **既知の問題**: `icons6.png`の各アイコンの「透明に見える」市松模様部分は、実際には透過（アルファチャンネル）ではなくピクセルとして描き込まれた市松模様そのもの。そのため盤面の駒やキャラメイクのアイコンにもこの市松模様がそのまま表示される。本当に背景を透過させたい場合は、元画像を実際にアルファ抜きした状態で差し替える必要がある
- **BGM（2026-08-11〜12実装）**: `src/audio.js`は以前Web Audio APIで音声ファイル無しにプロシージャル生成していたが、そのコードは全て削除し、`HTMLAudioElement`でユーザー提供のMP3実音源（`public/audio/`）を再生する方式にした。盤面BGMはマップごとに専用曲を持てる設計（`playMapTheme(mapId)`が`board.js`の`MAPS`とは別にaudio.js内の`MAP_TRACK`テーブルでmapId→トラックを引き、無いマップは共通の`board-theme.mp3`にフォールバック）:
  - `board-theme.mp3`: ①ヒトデの縄張り・専用曲の無いマップの既定
  - `battle-theme.mp3`: バトルシーン用（全マップ共通、専用のマップ別戦闘曲は無い）
  - `stage2-theme.mp3`: ②マダイの岩礁専用
  - `stage3-theme.mp3`: ③決闘の浜辺専用
  - `boss-theme.mp3`: ④暗転した世界（ラスボス）専用
  - `main.js`の`currentMapId`（対戦開始時に設定するモジュール変数）を`playMapTheme(currentMapId)`にそのまま渡すだけで正しい曲が選ばれる。PvPで各マップを選んだ場合もゲスト側含め同じ仕組みが効く（`pvpLastRoom.mapId`から`currentMapId`を設定）
- **ストーリー名前付きNPCの立ち絵・盤面駒（2026-08-12実装）**: `src/npcArt.js`が名前文字列（story.jsの`speaker`/opponent`name`と完全一致）から実素材のURLを引く小さなルックアップ表。対応済み6キャラ: ダンボール男・暴君マダイ・ニュウドウカジカ（お肉）・紫の魔女ホフク・少女A（旧称「某不思議の国の少女」）・ウサギン（旧称「農家のウサギ」）。ヒトデ等は未対応でプレースホルダーのまま
  - **キャラ名変更（2026-08-12）**: ユーザー指定で「某不思議の国の少女」→「少女A」、「農家のウサギ」→「ウサギン」に改名。`story.js`内の該当する`speaker`/`name`/ステージタイトル文字列を全て置換済み（表示名がそのままnpcArt.jsの検索キーを兼ねるため、名前を変える時は両方を必ず揃える）
  - **立ち絵**（`public/images/npc-portraits/`）: 全身立ち絵、背景透過PNG。`#story-dialogue-screen`に追加した`#story-dialogue-portrait`が、`playDialogueLines`で1行進むたびに`speaker`名からルックアップして表示/非表示を切り替える（該当が無ければ非表示のまま、今まで通りテキストのみの表示）
  - **盤面駒**（`public/images/npc-tokens/`）: 256×256の正方形、キャラごとの縦横比を保ったまま底辺揃えで配置済み（`GameScene.createPieceFromImage`が前提とする1.6×1.6の正方形スプライトに合わせた設計）。`Game`の駒生成を汎用化 - 以前は人間プレイヤーの`humanIconImage`だけが特別扱いだったが、`player.iconImage`があれば（CPU/人間問わず）`createPieceFromImage`を使うよう統一した。`main.js`の`buildBattlePlayerConfigs`が非同期になり（`npcArt.js`の`loadNpcTokenImage`がpromise）、対応キャラのally/opponent configにだけ`iconImage`を設定する
  - 立ち絵・盤面駒とも元はAI生成の「フィギュア＋カードのモックアップ」6枚1組の合成画像から、背景（白/薄灰色）をflood-fillで透過にし、隣のキャラの絵が数px混入していた箇所を手作業で追加トリミングして作成した（`rembg`のような汎用背景除去だと枠やカード台座まで一体化していて使えなかったため、単色背景の別バージョンをユーザーに作り直してもらう方針にした）
- **カード共通プレースホルダー画像（2026-08-12実装）**: `src/cardArt.js`の`defaultCardArtUrl(card)`が、カードに`imageDataUrl`（カードエディタで個別に設定した画像）が無い場合のフォールバック画像を種別・属性から返す（`public/images/card-art/`、モンスターは属性ごとに5種、武器防具・スペルはそれぞれ1種の計7枚）。1枚ずつ個別に画像を用意するのは非現実的という判断で、当面は全カード共通のこのフォールバックを使う（`renderCardEl`一箇所に集約してあるので、カード一覧・デッキ編成・手札・カード詳細ポップアップ・バトルシーン等どの画面にも自動で反映される）。将来カードごとの専用素材ができたら、そのカードに`imageDataUrl`を持たせるだけで自然に上書きされる
- **マップごとの背景画像を切り替え可能に（2026-08-12実装）**: `board.js`の`MAPS`各エントリに`background`フィールドを追加（②マダイの岩礁だけ実素材の`stage2.jpg`、他3マップは共通の`stage1.png`のまま）。以前は`#app`のCSS背景が静的な1枚固定だったが、`main.js`の`applyMapBackground(mapId)`が対戦開始のたびに`getMapBackground()`を引いてインラインstyleで上書きするようにした（`currentMapId`を設定する2箇所＝`startBattle`/`startPvpGuestBattle`と同じタイミングで呼ぶ）
- **タイトル画面の刷新（2026-08-12実装）**: ログイン画面に実素材のロゴ（`public/images/title-logo.webp`、背景の黒をflood-fillで透過化）と背景イラスト（`public/images/title-background.jpg`、暗めのグラデーションを重ねて可読性を確保）を反映。文字だけの`<h1>`タイトルは廃止（ロゴ画像に文字が入っているため）。ゲーム紹介文を4項目（盤面パート・ストーリー＆対人戦・キャラメイク・カード収集）追加し、盤面パートには実際の対戦画面のスクリーンショット（`public/images/title-screenshot.jpg`）も添えた。ログイン画面はBGM無し（意図的）
- **ホーム画面への追加（2026-08-12実装）**: タイトル画面に追加推奨メッセージと「ホーム画面に追加」ボタンを表示。`beforeinstallprompt`対応ブラウザではインストール確認を直接表示し、iOS Safariなど非対応環境ではブラウザメニューから追加する手順を案内する。`manifest.webmanifest`とService WorkerによりPWAとしてstandalone起動し、ホーム画面アイコンには「チヌクエ」文字入りの専用画像を使用する
  - **スクリーンショットの取得方法についての注記**: このセッションはBrowserペインが終始`document.hidden=true`で`requestAnimationFrame`が発火しない状態だったため、通常の方法では画面を撮影できなかった。`window.requestAnimationFrame`をブラウザのコンソールから`setTimeout`ベースの実装に一時的に上書き（Node側のポリフィルと同じ発想）することで、WebGLキャンバス自体は`document.hidden`に関係なく描画できることを利用し、`canvas.toDataURL()`で実際の盤面描画を直接キャプチャした。それをCSSの盤面背景画像とその場でCanvas 2D合成し、実際のゲーム画面に近いスクリーンショットを作成した（HUD等のDOM/CSS部分は同じ手法が使えず含まれていない）

## 無属性モンスター20種＋特殊能力（2026-08-12実装）
`src/neutralMonsters.js`（N10種／S6種／R4種）を追加し、`MONSTER_CATALOG`は火/水/雷/森/無属の5カタログのスプレッドになった。これで**モンスターカタログ全100種**（4属性×20種＋無属性20種）が揃った。無属性バッチは今までで最も新規メカニクスが多く、既存の`effect.type`/`ability.type`ディスパッチで再利用できるものはそのまま流用しつつ（透明人間の回避は`challengeOdds`の`negateIncomingChance`を流用、特攻兵は既存の`selfDamageAfterAttack`をそのまま流用）、7種の完全新規メカニクスを実装した。

### battle.js: 新規3種
- `statOverrideInBattle`（ネット弁慶）: `statTotals`で素のdef.atk/hpの代わりに固定値（20/20）を基準にする。呪い・アイテム・状況ボーナスはその固定値の上にそのまま乗る（＝弱体化するのはあくまで「素の数値」だけ）
- `doubleItemEffect`（Ninja）: `statTotals`で装備アイテムのatk/hpボーナスだけを2倍にする（プラスもマイナスも対象、との仕様通り実装）
- `reflectDamage`（くねくね）: `dealDamage`で、防御側がこの効果を持ち被ダメージが0より大きい場合、防御側は無傷のまま攻撃側にそのダメージをそのまま転嫁する。攻撃が「届かなかった」扱いとして処理するため、命中時オンヒット効果（毒付与等）はこの一撃では発動しない

### game.js: 新規7種
- **合体召喚**（古代のギアA・B・C→ガシャーン）: `_placeUnit`に`fusionSummon`エフェクトのフックを追加し、新規ヘルパー`_maybeFuseGear`が「残り2種のギアが自分の盤面に揃っているか」を`catalogIdOf`で判定。揃っていればその2枚を盤面から消し、召喚したマスをEXレア・図鑑登録なしの専用駒「ガシャーン」（`neutralMonsters.js`の`GASHAAN_FIELD_MONSTER`、`DENCHU_FIELD_MONSTER`と同じ「盤面限定カード」方式）に差し替え、先に払ったギアの召喚コスト（10G）を払い戻す（＝合体時は実質コスト0）。効果テキストは仕様通り条件をぼかして「ほかのギアが2種類配置された状態で召喚すると…」とだけ表示。CPUの土地コマンド召喚（`_cpuLandCommand`）も同じ`_placeUnit`を通るので自動的に対応
- **死亡復活**（ゾンビ、`deathRespawnChance`）: `_handleUnitDeath`の先頭に追加。1/2の確率で、盤面のランダムな空き地に同じ定義のカードを再召喚する（`_placeUnit`をそのまま再利用）。実装中に判明した既存のバグを合わせて修正: 死亡時にカードを作り直す処理（ゾンビの再召喚・不死鳥の手札復帰）が新しいidだけ振って`catalogId`を引き継いでいなかったため、`catalogIdOf`ベースの盤面検索（シナジー判定等）が壊れる可能性があった。両方とも`catalogId: catalogIdOf(unit.def)`を明示するよう修正
- **コピー変身**（メタ〇ン、`copyOnSummon`）: `_humanSummonFlow`の召喚/入れ替え直後に新規ヘルパー`_maybeCopyOnSummon`を呼ぶ。盤面に存在するモンスター（自分自身のマスを除く全員、敵味方問わず）から1体選んで、そのdefを丸ごとコピーする（basedステータスのみ＝バフ・デバフは引き継がない。currentHpも変身後の素のHPにリセット）。**CPU（対戦相手用の簡易AI）は変身させず素の姿のまま運用**（スコープ限定、簡易AIに選択ロジックを追加するコストに見合わないと判断）
- **土地属性の自由変更**（古の魔法使い、`changeOwnLandElement`）: 新設の土地コマンド。既存の「属性変更」土地サブメニュー（レベル×100Gの動的コスト）とは別に、モンスターの特殊能力として自分の所有地から1つ選んで属性を変更できる（コマンドコストは固定30G）
- **自爆型ダメージ攻撃**（センチネル、`damageAndSelfDestruct`）: 新設の土地コマンド。射程無制限で敵モンスター1体を選び基礎HPに30ダメージを与え、発動と同時に自分自身も盤面から消滅する（`_handleUnitDeath`を経由するので不死鳥特性があればここでも効く）。既存の`damage`土地コマンドをベースに自爆処理を追加した形
- **周回ボーナス倍率**（フリーランサー、`lapBonusMultiplier`）: `_grantGoalBonus`で、自分の盤面のどこかにこの効果を持つモンスターがいればゴール通過ボーナス（通常100G）に倍率（1.3）をかける
- **総連鎖数ボーナス**（混沌の頭、`statsPerTotalChain`）: `_applyEffectBonus`に追加。既存の`statsPerElementChain`（単一属性の連鎖数×n）と違い、火・水・雷・森4属性の連鎖数を合計してから×n（5）する
- **属性ボーナスの属性不問化**（レインボーカメレオン、`elementHpBonusIgnoreElement`）: `_elementHpBonus`に判定を追加。通常の同属性ボーナスはモンスターの属性と土地の属性が一致しないと発動しないが、この効果を持つ場合はその判定をスキップし、無条件で土地レベル×10（上限50）を受け取る
- **戦闘終了時ランダムスペル入手**（怪しい老人、`randomSpellAfterBattle`）: `_runBattleScene`の末尾（onBattleOutcome後）で新規ヘルパー`_maybeGrantRandomSpell`を呼ぶ。生死を問わず、`SPELL_CATALOG`から完全ランダムに1枚を手札に加える

### 仕様上あいまいだった3点（実装方針を明示、要ユーザー確認）
1. **ガシャーンのステータス不一致**: 元データはギアA・Bの説明文が「HP/ATK70/70」、ギアCの説明文だけ「80/70」と食い違っていた。2箇所で一致している**70/70を採用**した。80/70が正しい場合は`neutralMonsters.js`の`GASHAAN_FIELD_MONSTER.hp`を80に直すだけで良い
2. **怪しい老人の対象カードプール**: 元の説明文は「スペルカードを手札に加える」としながら、直後の括弧で「電柱やガシャーンのようなEXカードも対象」と書かれており、電柱・ガシャーンは実際にはモンスター種別の盤面限定カードでスペルではないため文面が矛盾していた。**スペルカード限定**（`SPELL_CATALOG`のみ、現状`マ〇ジャロ`1種）で実装し、電柱・ガシャーンはプールに含めていない（EXレアも含めた「レア度を問わない完全ランダム」という原則は将来スペルカードが増えた時のために活かしてある）
3. **刀鍛冶の「オサフネ」**: 元データに「オサフネは別途実装予定」と明記されていたため、`katanakaji`エントリはability/commandCostを持たない素のステータスカードとして登録し、土地コマンドの配線は保留にした。オサフネのアイテムデータが来次第、`ITEM_CATALOG`への追加と`commandCost`/`ability`の配線が必要

### 検証
`resolveBattle`/`Game`を直接叩くNode単体テスト30件（合体召喚の成立/不成立/コスト払い戻し、ゾンビの死亡復活の確率成立/不成立、くねくねの反射、ネット弁慶のステータス固定、Ninjaのアイテム効果倍化、透明人間の回避、フリーランサーの周回ボーナス、混沌の頭の総連鎖ボーナス、狂戦士の無条件ATK倍率、レインボーカメレオンの属性不問ボーナス、古の魔法使いの土地属性変更、センチネルの自爆攻撃、怪しい老人のスペル入手、メタ〇ンのコピー変身）を実施、全てパス。実機でも`MONSTER_CATALOG`が5属性×20種＝計100種正しく読み込まれること（コンソールエラーなし）を確認済み。

## アイテム30種の実装＋刀鍛冶のオサフネ配線（2026-08-12実装）
`src/battleCards.js`の`ITEM_CATALOG`を、ナイフ/なべのふた/ペーの杖の3種のみだった状態から`chinu-quest2-items-final-v3.md`に沿ったN13/S9/R7/EX1の**全30種**に拡張した（`item()`ビルダー関数を新設し、モンスターカタログ各ファイルと同じ「オプションが有る時だけフィールドを足す」パターンに統一）。既存の`atkBonus`/`hpBonus`/`traits`だけの単純アイテム（こん棒・鉄パイプ・鉄の鎧・防弾チョッキ・平家の鎧・モバイルスーツ・にょ〇棒・ウニトロの服・諸刃の剣、ペガサスソードの先制特性）はデータ追加のみ。それ以外は新規メカニクスが必要だった。

### equipItemの重要な修正（副産物のバグ修正）
アイテムを装備する`equipItem(unit, itemDef)`が、従来は`ITEM_CATALOG`の**共有オブジェクトへの参照をそのまま**`unit.items`に積んでいた。今回、ナンカのお守り/ライフジャケットのように「1戦闘1回だけ発動、以後は自分自身に`consumed`フラグを立てて再発動を防ぐ」効果を追加するにあたり、この共有参照がそのまま残っていると**一度誰かが使うと未来永劫（他のプレイヤー・他の戦闘でも）そのアイテムの効果が壊れる**重大なバグになることが判明したため、`equipItem`は常に`{ ...itemDef }`でコピーを積むよう修正した（`atkBonusRange`を持つペーの杖はさらにその場でATKボーナスを抽選したコピーを積む）。Node単体テストにこの回帰確認（1戦闘目で消費→2戦闘目の別ユニットでも正常に効くこと）を追加済み。

### battle.js: アイテム専用の新規エフェクト8種＋既存エフェクトのアイテム対応
- 新設ヘルパー`getEffect(unit, type)`: カード自身の効果、または装備中アイテムの効果のうち、指定typeのものを1つ返す。目出し帽(`stealDamageMultiple`)・斬〇剣(`instantKillOnHit`)・ステゴロ(`destroyItemBeforeAttack`)・雷神剣等(`elementDamageBonus`)は、既存のモンスター効果と全く同じtypeをアイテム側にも持たせるだけで動くようにした（各チェック箇所を`unit.def.effect`直接参照から`getEffect()`経由に変更）
- `forceZeroAtk`（ダンボールの鎧）: `statTotals`で、装備中は他の加算要素も含めATKを完全に0へ上書き
- `negateNextDamage`（ナンカのお守り）: `dealDamage`の最初でチェック、1戦闘1回だけダメージを完全無効化
- `reflectHalfDamage`（ハリネズミの服）: `dealDamage`で通常通り被弾した上で、その半分を追加で攻撃側にも反射する（くねくねの`reflectDamage`＝自分ノーダメージで全反射、とは別の新効果）
- `surviveLethalDamage`（ライフジャケット）: `dealDamage`で致死ダメージ判定後、1戦闘1回だけHPを1に補正して踏みとどまる
- `chanceBlindOnHit`（電流ムチ）: `performStrike`の命中時ブロックに追加、確率で相手を目くらまし状態（`blinded`）にする（アオリイカの無条件版の確率版）
- `stealItemBeforeAttack`（真剣白刃取り）: `resolveBattle`の攻撃開始前フェーズに追加、相手のアイテムを破壊する代わりに奪って自分の装備に加える（既存のアイテム破壊＝ステゴロ/海賊Sと同じタイミング）
- `doubleStrike`（ツインハンマー）: `resolveBattle`の交戦ループを「固定2枠（先制側1回・後攻側1回）」から「各側が`strikeCount()`回連続で攻撃するループ」に一般化。装備側は自分の番に2連続攻撃し、1発目で相手が力尽きれば2発目は撃たない。単発の場合は`strikeCount=1`で従来と全く同じ挙動になる。`dmgToDefender`/`dmgToAttacker`も、単発の時だけ有効だった三項演算子方式から`exchanges`を集計する方式に変更（複数回攻撃でも正しく合算される）
- `equipItem`の`atkBonusRange`対応（ペーの杖）: 装備した瞬間に範囲内（25〜50）でATKボーナスを1回だけ抽選する

### game.js: アイテム発動にboard/player状態が要るもの3種＋刀鍛冶の配線
- `atkFromLastDiceRoll`（イカサマのサイコロ）: `_applyEffectBonus`に、カード自身の効果とは別枠で装備アイテムをチェックする分岲を追加。`Game.rollDice(steps)`で新設した`player.lastDiceSteps`（直近の実際のサイコロの目、強制サイコロも含む）を参照し、×11のATKボーナスを付与
- `returnsToHandIfUsed`（不死鳥の剣）: `_runBattleScene`の末尾（怪しい老人のスペル付与と同じタイミング）で新設`_maybeReturnItemToHand`を呼び、戦闘で実際に使用されたアイテムだけ新しいidで持ち主の手札に戻す（手札上限で未使用のまま捨てられた場合はこの経路を通らないので通常通り消滅する、という仕様通りの挙動）
- **刀鍛冶のオサフネ配線**: 前回の無属性バッチで「オサフネ未実装のため保留」としていた`katanakaji`に、新設した汎用の`grantItem`アビリティ（`ability.itemId`で指定した`ITEM_CATALOG`のカードをコマンドコストで入手する）を配線。今後同種の「土地コマンドで特定のアイテムを入手する」モンスターが出てもそのまま再利用できる

### 検証
`resolveBattle`/`Game`を直接叩くNode単体テスト20件（ダンボールの鎧のATK0化、ナンカのお守りの1回無効化＋equipItemコピー化の回帰確認、ハリネズミの服の半分反射、ツインハンマーの2連続攻撃とダメージ集計、真剣白刃取りのアイテム強奪、ステゴロのアイテム破壊、ライフジャケットのHP1耐え、雷神剣の属性特効、電流ムチの確率行動不能、斬〇剣の即死効果、ペーの杖のランダム範囲、イカサマのサイコロの出目参照、不死鳥の剣の手札復帰、刀鍛冶のオサフネ入手）を実施、全てパス。実機でも`ITEM_CATALOG`がN13/S9/R7/EX1の計30種正しく読み込まれること（コンソールエラーなし）を確認済み。

## にょ〇棒/イカサマのサイコロ/斬〇剣に貫通付与（2026-08-12実装）
ユーザー指示: 「にょ〇棒は+10 +10にして貫通の効果つけて。反射や無効化を無視して土地レベルボーナスも無視する。イカサマのサイコロも貫通、斬〇剣も貫通で。」
- にょ〇棒のステータスを+15/+15→**+10/+10**に変更、3アイテムとも`traits:['pierce']`を追加（斬〇剣は既存の`lastStrike`と併存）
- 貫通(pierce)は元々「同属性ボーナス（土地レベルのHP加算）を無視する」「被ダメージ半減traitを無効化する」の2つだけを対象にしていたが、今回**反射・無効化系（くねくねの`reflectDamage`／ハリネズミの服の`reflectHalfDamage`／ナンカのお守りの`negateNextDamage`）も無視する**よう`battle.js`の`dealDamage`を拡張した（`chanceDamageReduction`＝マグマンの確率半減は従来通り対象外のまま、変更していない）
- 副産物として発見・修正したバグ: `game.js`の`_runBattleScene`にある「貫通は相手の同属性ボーナスを無視する」判定が、`attackerUnit.def.traits`（モンスター自身の特性）しか見ておらず、**アイテムが持つtraits（今回の3種はまさにこれ）を無視していた**。`battle.js`の`hasTrait()`相当のチェック（def.traits + 装備アイテムのtraits）に直して修正
- 検証: Node単体テスト7件（にょ〇棒のステータス変更確認、貫通がくねくね/ナンカのお守り/ハリネズミの服それぞれを無視すること、貫通が土地レベルボーナスを無視して本来なら耐えるはずの一撃で倒れること）で全パス、実機で3アイテムのtraits反映を確認済み

## CPU AIの大幅強化（2026-08-12実装）
それまでのCPUは実質「意思決定ゼロ」だった: 土地コマンドは手札の先頭の召喚可能カードで無条件に召喚/侵略するだけ（自分の土地では何もしない＝レベルアップ等は一切しない）、分岐点の移動方向は完全ランダム、バトルアイテムも手札の先頭のGEARを機械的に装備するだけ、という3つの一行ロジックが全てだった。これをキャラごとの「性格」パラメータに基づく確率的な意思決定に置き換えた。

### src/aiProfiles.js（新規）
`DEFAULT_AI_PROFILE`と、story.jsに登場する7キャラ（ヒトデ/ウサギン/暴君マダイ/ニュウドウカジカ（お肉）/少女A/紫の魔女ホフク/ダンボール男）ごとの上書き差分`AI_PROFILES`を定義。パラメータは全て確率・閾値であり固定分岐ではない:
- `offElementSummonChance`: 土地属性と違うモンスターをあえて選ぶ確率（同属性の選択肢があるのに、という条件下で判定）
- `levelUpReserve`: 土地レベルアップ後もこの額以上Gを残せる時だけ実行（キャラごとに300〜700）
- `minWinProbabilityToInvade`: 侵略を決断する勝率の基本しきい値（キャラごとに0.15〜0.9）
- `itemGambleChance`: 「アイテムを使えば勝てるが素では勝てない」場面でそれでも侵略に踏み切る確率
- `highValueAvoidance`: 相手の高額マス（Lv3以上）をどれだけ避けたがるか（侵略しきい値の上乗せ・分岐点の経路選択の両方に影響）
- `preferredElements`: 実行時にキャラのデッキテーマ（story.jsの`theme.elements`）から差し込む。テーマの無い汎用CPU（フリー対戦の既定の「CPU」）はnull＝どの属性でもマッチ扱い

`resolveAiProfile(name, preferredElements)`が実行時プロファイルを組み立て、`Game`コンストラクタが各CPUプレイヤーに`player.aiProfile`として持たせる（`main.js`の`buildBattlePlayerConfigs`が`opponent.theme.elements`/`allyDef.theme.elements`を`elements`として`playerConfigs`に追加）。

### game.js: 侵略前の勝率シミュレーション
`_estimateWinProbability(card, ownerId, hand, defenderTile, useItem, trials=20)`が、実際に戦闘を起こさずモンテカルロ法（既定20試行）で勝率を見積もる。**相手はアイテムを使わない前提**（ユーザー指示通りの簡略化）。実際の対戦へは一切影響しないよう設計: `this._goldAdapter()`（本物のプレイヤーGを直接動かす）ではなく試行ごとに使い捨ての`GoldLedger`を使い、`onLog`も一時的に無効化し、防御側ユニットも`items`/`curses`だけ独立コピーした複製（`_cloneFieldUnitForSim`）を使う（本物の装備・呪いを破壊しない）。土地の同属性ボーナスや貫通判定など、実戦と全く同じ`_battleBonus`/`_applyEffectBonus`ロジックを再利用しているため、見積もりと実際の挙動が乖離しない。

### game.js: 土地コマンドの判断ロジック刷新
- `_cpuMaybeLevelUp`: 自分の土地限定。土地属性がキャラの得意属性に合っていて、かつ`levelUpReserve`を満たす時だけレベルアップ（Lv5上限は既存の`LEVEL_CAP`のまま）
- `_cpuChooseSummonCard`: 空き地への召喚。基本は土地属性と同じカードから選び、`offElementSummonChance`の確率であえて属性違いを選ぶ（選択肢が同属性に無ければ必然的に属性違い）。同プール内ではATK+HP合計が高いカードを優先
- `_cpuDecideInvasion`: 敵地への侵略。手持ちの召喚可能カード全てについてアイテム無し/有りの勝率を`_estimateWinProbability`で見積もり、最有力の組み合わせを選ぶ。相手が高額マス（Lv3以上）なら`highValueAvoidance`に応じてしきい値を引き上げる。アイテム無しでしきい値超えならそのまま侵略、アイテムがあれば超える場合は`itemGambleChance`の確率でだけ踏み切る。どちらも満たさなければG消費なしで見送り（ログに「侵略を見送った」と表示）

### game.js: アイテム選択・分岐点の経路選択
- `_itemPowerScore`/`_bestBattleItemFromHand`: GEARカードを「ATK+HPボーナス＋特殊効果+15点＋先制/貫通+10点ずつ－後攻-5点」という大まかな基準で採点し最強の1枚を選ぶ。この関数を`_cpuPickBattleItem`（実際の戦闘）と`_estimateWinProbability`（事前シミュレーション）の両方が共有するため、「シミュレーションで想定した通りに実際も動く」が保証される
- `_cpuChooseNextTile`: 分岐点での移動方向を完全ランダムから重み付き抽選に変更。次に目指すべきマス（`_nearestGoalTileId`: 未通過チェックポイントがあればBFS距離で最も近いもの、無ければゴール）までの距離が近い選択肢ほど選ばれやすく、かつ相手の高額マス（Lv3以上・同盟仲間以外の所有地）につながる選択肢は`highValueAvoidance`に応じて重みを下げる（0にはしない＝「勝てる可能性がある限り攻める」キャラも成立する）。重み付き抽選そのものは新設の`_weightedRandomPick`ヘルパーが担う

### 検証
Node単体テスト18件（プロファイル解決、属性一致優先/offElementSummonChanceの両極端、レベルアップの属性・reserve判定、勝率シミュレーションが極端なケースで0/1になること、シミュレーションが実際のプレイヤーGを一切変更しないこと、侵略判断の決断/見送り、分岐選択が目標への近さ・高額マス回避それぞれで統計的に有意な偏りを示すこと、アイテムスコアリング）を実施、全てパス。実機でも新規アカウント作成→CPU戦を実際に4ターン分自動進行させ、CPUが召喚判断を繰り返し行い、エラーなく進行することを確認（このセッションはBrowserペインが終始`document.hidden=true`の状態だったため、`window.requestAnimationFrame`をコンソールから一時的にポリフィルしてアニメーション主体のUIフローを動かした）。

## 直接ダメージ系の演出: 火の玉+ダメージ数値ポップ（2026-08-12実装）
土地コマンドの直接ダメージ系アビリティ（`ability.type`が`damage`＝火炎瓶男等、`damageAndSelfDestruct`＝センチネル）が発動した時、対象マスへ火の玉が降ってきて、着弾後にダメージ数値がぴょんと跳ねて約1秒静止してから消える演出を追加した。

- **`scene.js`**: `playFireballImpact(tilePosition)`（新規）が、なべのふた等と同じ「canvasに手描きしたテクスチャ」方式（放射状グラデーションの炎プレースホルダー、実素材無し）で火の玉スプライトを生成し、対象マスの真上（y=8）から着地位置まで`tween`でease-in（`t*t`、自由落下っぽい加速）落下させ、着弾の瞬間に一度膨らませてフェードアウトさせて消す（`scene.remove`+`material.dispose()`）。`worldToScreen(x,y,z)`（新規）は3Dワールド座標を`camera.project()`＋キャンバスの`getBoundingClientRect()`で画面ピクセル座標に変換するヘルパーで、ダメージ数値をキャンバスの上にDOMで重ねて表示するために使う
- **`game.js`**: 新設のオプショナルコールバック`onDamageEffect({tileId, damage})`を、`_humanAbilityFlow`の`damage`/`damageAndSelfDestruct`両分岐でHP減算・ログ出力の直後に`await`する（未指定でもクラッシュしない）。ペイロードは`tileId`のみ（本物の`tile`オブジェクトは`mesh`等のTHREE.Meshを持っておりFirestore中継に乗せられないため）
- **`index.html`/`style.css`/`main.js`**: `#game-canvas`の上に透明な`#fx-layer`オーバーレイを新設。`promptDamageEffect({tileId, damage})`が`tileId`からローカルの`tiles`配列で実物のtileを引き直し、`scene.playFireballImpact`→ダメージ数値のDOM要素生成（`.fx-damage-number`、`scene.worldToScreen`で位置決め）の順で演出する。CSSの`fx-damage-pop`キーフレーム（1.8秒）が「跳ねる（0〜28%）→静止（28〜83%、約1秒）→フェードアウト（83〜100%）」を担当し、`animationend`でDOM要素を削除してPromiseをresolveする。対人戦（PvP）でも両者に見えるよう`relayable('damageEffect', promptDamageEffect, {broadcast:true})`で中継し、ゲスト側ハンドラ一覧にも登録した
- 検証: Node単体テスト5件（`damage`/`damageAndSelfDestruct`双方で`onDamageEffect`が正しいtileId/damageで呼ばれること、コールバック未指定でもクラッシュしないこと）は全てパス。実機では、このセッション特有の`document.hidden=true`環境下だとJSの`requestAnimationFrame`だけでなくCSSの`animation`もブラウザ側で一時停止される（実際のユーザー環境では起きない、タブが可視である限り無関係の制約）ため、コンソールから`requestAnimationFrame`を一時ポリフィルした上で実際に`_humanAbilityFlow`を発火させ、火の玉の落下（`playFireballImpact`のPromiseが正しく解決）とダメージ数値のDOM要素（テキスト・位置・`fx-damage-pop`アニメーションの適用）が意図通り生成されること、対象のHPが正しく減ること（30→20）を確認。CSSアニメーションの静止フェーズで止まっていたため`animationend`を手動発火して後続処理（コールバックのresolve・要素の削除）が正しく完了することも確認した

## 配置モンスターの周回HP回復を全属性・全プレイヤー共通ルールに（2026-08-12実装）
ユーザー指示: 「配置されたモンスターのHPは周回で最大基礎HPの10%回復するようにして」。

以前はメカニックマソ（雷属性モンスター）が盤面にいる時だけ、そのプレイヤーが所有する**雷属性モンスターに限り**周回ごとに10%回復する専用効果だった（`_healMechanicMasoAllies`）。これを汎用ルールへ一般化: `_healOwnedUnitsOnLap(player)`が、そのプレイヤーが所有する配置済みモンスター**全員**（属性・メカニックマソの有無を問わない）を対象に、周回ごと（ゴール通過時、チェックポイント未達成でボーナス無しの周回でも回復自体は適用）に最大基礎HPの10%（端数四捨五入、上限クランプ）回復するようにした。「最大基礎HP」は素のdef.hp＋永続呪いの加算に加え、同属性ボーナス（土地レベル×10、既存の`_elementHpBonus`）も含む＝土地情報のアイドル時HP上限と同じ基準。`_grantGoalBonus`からの呼び出し名を`_healMechanicMasoAllies`→`_healOwnedUnitsOnLap`に変更した。

**追記（同日）**: ユーザー指示「メカニックマソの効果は周回回復に上乗せされる」を受け、`_healOwnedUnitsOnLap`に分岐を追加。自分の盤面のどこかにメカニックマソがいる場合、自分が所有する**雷属性モンスターだけ**汎用10%にさらに+10%上乗せされる（合計20%）。雷以外・メカニックマソ不在時は引き続き通常の10%のまま。これでメカニックマソの固有能力が意味を保つ形に収まった。

検証: Node単体テスト7件（同属性ボーナスを含めた回復量の正しさ、属性不一致でも回復すること、上限クランプ、他プレイヤーの所有ユニットは対象外であること、`_grantGoalBonus`から正しく呼ばれること、メカニックマソ在席時に雷属性だけ+20%になり雷以外は+10%のままであること）で全パス。

## スペル40種の実装（2026-08-12実装）
それまで「マ〇ジャロ」1種しか実在せず、しかも`useSpell`自体が完全に空のプレースホルダー（`onSpellUse`のカード名フラッシュ表示以外は何もしない）だった。今回、スペルの発動基盤そのものを新設した上で40種全てに実際の効果を実装した。

### 発動フロー刷新（`game.js`の`useSpell`）
- スペルに**初めてコスト（`cost`）を持たせた**（以前は「引いたら無料で使える」仕様のまま放置されていた）。手持ちGが足りなければ発動できない
- 対象選択（あれば）を**カード消費より先**に行うよう順序を入れ替えた: `_resolveSpellCast`が`card.target`（`enemyMonster`/`anyMonster`/`ownMonster`/`enemyPlayer`/`anyPlayer`/`anyTile`/`ownTile`/`twoOwnMonsters`/`self`/`none`）に応じた対象選択UIを出し、キャンセルすれば手札からもGからも一切消費されない（土地コマンドの`confirmAndSpend`と同じ考え方）。モンスター/土地/プレイヤーいずれの対象選びも既存の`onPickAbilityTarget`を使い回す
- 対象選択と発動が両方成功した後にまとめて手札除去・G消費・`spellUsedThisTurn`確定を行い、`_applySpellEffect`が`card.effect.type`をディスパッチして実際の効果を適用する

### 新規メカニクス（27種、効果ごとに再利用できるものは既存の仕組みを流用）
- **ダイス呪い**（1のダイス/3のダイス/6のダイス/アイキャンフライ/バックファイア）: `player.diceCurse`という1回限りの呪いを新設し、`rollDice`が次の1回だけ出目を固定/2倍/後退に上書きする。後退（バックファイア）は新設の`player.tileHistory`（着地履歴）を遡る`_movePlayerBackward`で実現
- **アリジゴク**: 既存の強制停止（`tile.forcedStopCursed`、ほこら効果由来）を汎用化。以前は`true`固定（免除対象＝土地の所有者）だったが、免除対象をプレイヤーidで指定できるようにした。**この過程で実在するバグを発見・修正**: id=0のプレイヤー（1人目）が絡む判定が`!tile.forcedStopCursed`等のtruthy判定のせいで`!0`=trueとなり「呪いなし」に誤判定されていた（`getTileSummary`の`cursed`表示も同様）。null/false明示判定に修正済み
- **ブルーオーシャン/帰巣本能**: 即時ワープ＋その場で土地コマンドを済ませてターンを終える新パターン（帰巣本能はワープ先を固定でスタート地点にし、周回ボーナスの2倍を渡す）
- **副業収入**: 新設の`player.lapsCompleted`（通算周回数）を参照
- **増税通知/追徴課税/脱税**: `tile.tollReductionRatio`（恒久）・`tile.tollBonusOnceMultiplier`（1回限り）・`player.tollWaiverCharges`（1回限り）を新設し、`_tollOfTile`/`_settleLandingToll`に組み込んだ
- **山分け/財布チューチュー**: プレイヤー間の直接G操作（既存のGoldLedgerは戦闘専用のため今回は使わず、player.currencyを直接操作）
- **千本桜/ファイヤーボール/小隕石/洪水系4種**: 新設`_spellDamageUnit`/`_spellDamageAllUnits`が、戦闘外でのHP減少・0以下になった場合の死亡処理（土地を空ける・`_handleUnitDeath`）をまとめて担当。単体対象は火の玉演出（`onDamageEffect`、直前のセッションで実装済み）付き、範囲攻撃系は演出を省略しログのみ
- **毒霧**: 対象マス＋隣接マス（グラフの`neighbors`＝盤面の「前後左右1マス」相当）に既存の`applyPoison`（今回`battle.js`からexportして流用）
- **絶対攻撃/お前も〇ぬんだ**: `player.pierceNextInvasion`/`player.guaranteedNextInvasionWin`を新設し、`_runInvasion`の冒頭で消費する。後者は戦闘を一切起こさず即座に土地を奪う（避雷針侍の身代わり等の介入も挟まない）
- **不死鳥の呪い**: 直前のアイテムバッチで実装した`surviveLethalDamage`（ライフジャケット等と同じ効果type）を、対象モンスターの`items`配列に戦闘外から直接注入することで再利用（次の戦闘で消費され、1回限り発動という仕様と自然に一致する）
- **不動産鑑〇士**: `player.allTilesAccessTurnsRemaining`を新設。`_runLandCommand`の`isAdmin`判定（本来はゴール/チェックポイントに立っている時だけ全所有地にアクセスできる）にこのカウンタを合流させ、効果中はどこにいても全所有地の土地コマンドを使えるようにした
- **最適化/放火系5種/シャッフル/サイコキネシス/聖域**: 土地属性の一括変更・モンスター入れ替え・強制隣接移動（既存の`_humanMoveFlow`侵略分岐と同じ決着ロジックを再利用）・透過の呪いの対象拡張（今まで自分専用だった`curseTransparency`と違い、任意の対象にかけられる）

### デッキへの実配布
以前は`buildStarterExtraCards`/`buildThemedDeckList`が「マ〇ジャロ2枚＋完全に無機能なプレースホルダー『スペル1』〜『スペル6』」でデッキのスペル枠を埋めていた（`cards.js`の`buildCardPool`が生成する仮データ、効果も所持もされないダミー）。これを`buildRandomSpellSelection`に置き換え、N70%/S20%/R10%の重み（`_randomItemCardForSummon`と同じ配分）で実在する40種からランダムに選んでデッキ枠を埋めるようにした。実機でも新規デッキの手札に実在スペル（副業収入）が入り、実際に発動できることを確認済み。**アイテムは今回のスコープ外のため、featured 1種以外は依然としてプレースホルダーのままという既存の非対称が残っている**（気になるようであれば別途対応可能）

### 仕様上あいまいだった点・スコープ限定（要ユーザー確認）
1. **「6のダイス」**: 元データは名前が「6のダイス」なのに効果文が「次のサイコロを1にする」となっていた（1のダイスとの記述重複ミスと判断）。名前に合わせて**出目を6に固定**として実装
2. **マ〇ジャロのレアリティ**: 新データはN指定だが、既存の実装・starter deckの構成等で既にS運用されていたため、**Sのまま**とした（数値のATK+10/HP+20はそのまま）
3. **絶対攻撃/お前も〇ぬんだの適用範囲**: 「侵略」全般ではなく、**ダイスで着地して侵略する`_runInvasion`経路のみ**に実装（土地コマンドの「移動」から侵略するケースは対象外、スコープ限定）
4. **呪い解除**: 「自分の呪いを解除する」を、プレイヤーにかけられた悪性の状態（`diceCurse`）のみ解除する意味に限定した（宝くじ・絶対攻撃等の自分にとって有利な状態は解除対象に含めていない）

### 検証
Node単体テスト46件（コスト消費・キャンセル時の無消費、ダイス呪い3種、アリジゴクの免除対象とid=0バグ修正、副業収入、通行料まわり3種、山分け・財布チューチュー、盤面全体/属性別ダメージと戦闘外死亡処理、毒霧の範囲、絶対攻撃・お前も〇ぬんだの`_runInvasion`連携、マ〇ジャロ、ヒール・博愛精神・呪い解除・不死鳥の呪い、不動産鑑〇士、最適化・放火・シャッフル・サイコキネシス・聖域）で全パス。実機でも新規アカウントでCPU戦を開始し、実際にデッキから引いた副業収入を発動（周回数×50+50Gが正しく加算）、ファイヤーボールを対象選択込みで発動（コスト消費・ダメージ・火の玉演出とも正常）してエラーが出ないことを確認した。

## 呪いシステムの仕様確定＋海賊Sのカード絵（2026-08-12実装）
ユーザー指示: 「マ〇ジャロはNでいい。呪い解除は有益な呪いも対象。海賊Sの画像をカードフォルダに入れたから実装して。呪いはプレイヤー呪いは移動してもついてくる、モンスター呪いはモンスターの移動で消滅。どちらの呪いも新しい呪いをかけられたら上書きされる仕様。つまり呪い状態は１つしか保持されない。」

- **マ〇ジャロ**: レアリティをS→**N**に変更（元データ通り）
- **呪い解除（curseCleanse）**: 前回は`player.diceCurse`のみ解除していたが、**有益な呪いも含めて**自分にかかっている状態を全て解除するよう拡張（`tollWaiverCharges`＝脱税／`lotteryOnNextGoal`＝宝くじ／`pierceNextInvasion`＝絶対攻撃／`guaranteedNextInvasionWin`＝お前も〇ぬんだ／`allTilesAccessTurnsRemaining`＝不動産鑑〇士）。対象モンスターの呪いも従来通り解除する
- **呪い状態は1つしか保持されない（単一スロット・上書き方式）**: `battle.js`の`applyCurse`/`applyPoison`/`applyShock`/`applyAtkDown`を、`unit.curses`へ`push`（追加）する方式から、新設の`setCurse`ヘルパーで`unit.curses`を**丸ごと置き換える**方式に統一した。これにより、例えば毒状態のモンスターに新たにATKダウンの呪いがかかると、毒は上書きされて消える（以前はATKダウンだけ複数回重ね掛けできる仕様だったが、今回の指示でそれも廃止）。プレイヤー側のダイス呪い（`player.diceCurse`）は元々単一フィールドなので、この仕様に既に合致していた（変更不要）
- **モンスターの呪いは移動で消滅、プレイヤーの呪いは移動してもついてくる**: `_humanMoveFlow`（土地コマンドの「移動」）と`_spellForceRelocateOneStep`（サイコキネシス）それぞれの「空き地への移動」「侵略成功による移動」の2分岐に`unit.curses = []`を追加。防衛を受けて元の土地に戻るケース（実質移動していない）は呪いを保持したままにした（**この挙動は次回の指示で「一瞬でも移動したら消える」に修正 - 詳細は後述の「移動関連の仕上げ」節を参照**）。プレイヤー呪いは`player`オブジェクトのフィールドなので元々座標と無関係で、この指示通りの挙動になっている（変更不要、Node単体テストで再確認のみ）
- **海賊S（waterMonsters.js）**: ユーザー提供の`image/card/kaizokuS.png`（白背景の全身イラスト）から境界フラッド・フィルで背景除去し`public/images/card-art/kaizokuS.webp`として保存、`imageDataUrl`をエントリに追加（`waterMonster()`ビルダーに`imageDataUrl`オプションを新設、fireMonster/waterMonsterで共通のパターン）

### 検証
Node単体テスト16件（マ〇ジャロのレアリティ、呪い解除が有益な呪いも含め全て消えること、毒→ATKダウンのように新しい呪いで上書きされ1つしか保持されないこと、空き地への移動でモンスターの呪いが消えること、プレイヤーの呪いは移動後も保持されること、海賊Sの画像パス）で全パス。実機でも海賊Sの画像が200 OKで読み込まれることを確認済み。

## 移動関連の仕上げ＋初期デッキ構成の実装（2026-08-12実装）

### モンスターの呪い: 防衛されて元の土地に戻った場合も消滅させる
前回の実装では「侵略が成功して実際に土地を移った」場合だけ`unit.curses = []`していたため、防衛を受けて元の土地に戻る（実質的には移動していない）ケースだけ呪いが残っていた。「一瞬でも移動したら消える」という指示を受け、`_humanMoveFlow`/`_spellForceRelocateOneStep`それぞれで`unit.curses = []`を**移動が確定した直後（目的地選択の確認が取れた時点）に無条件で1回だけ**実行するよう再構成し、成功・防衛・相打ちのどの分岐でも呪いが消えるようにした。

### 配置モンスターの盤上アイコン（新規）＋移動ホップ演出
これまで盤上には土地の色（所有者）としか配置モンスターが表現されておらず、モンスター専用のアイコンや駒は存在しなかった。プレイヤー駒（`createPiece`/`createPieceFromImage`、billboardスプライト）と同じ仕組みで、配置モンスター用の小さめの常駐アイコン（`scene.js`の`createUnitIcon`/`removeUnitIcon`、属性色のトークン、`UNIT_ICON_REST_Y=0.5`・スケール1.0でプレイヤー駒より低く小さく表示し見分けられるようにした）を追加した。
- `game.js`の`_syncUnitIcons()`が`tile.unit`の有無と`tile.unitMesh`の有無を突き合わせて自動で作成/削除する（`_notifyState`から毎回呼ぶ）。これにより召喚・侵略・死亡・売却・融合・シャッフルスペル等、`tile.unit`を書き換えるあらゆる既存箇所を個別に触らずに対応できた。
- 「移動」（土地コマンド／サイコキネシス）だけは`_hopUnitIcon`（`_tweenStep`のプレイヤー駒ホップと同じ`Math.sin(Math.PI*t)*0.5`の放物線）で明示的にアニメーションさせる: 空き地への移動は始点→終点、侵略成功も同様（奪われた側の古いアイコンは移動前に破棄）、防衛されたケースは始点→終点→始点の往復ホップで「一瞬移動して戻ってきた」ことを視覚的に表現する。

### 初期デッキ構成（`chinu-quest2-starter-decks-v3.md`）
2つの初期ブック（火・森／水・雷）を、ユーザー指定の完全固定40枚構成に置き換えた。従来は「看板モンスター/アイテム4枚＋残りは汎用プレースホルダー＋ランダム実在スペル」で埋めていたが、今回は**汎用枠を一切使わない**完全指定リストに変更:
- モンスター20枚（6種、内訳4/4/4/4/3/1、Sレアリティは各ブック1種のみ）
- アイテム7枚（ナイフ・なべのふた各1枚必須＋こん棒4枚＋防弾チョッキ1枚）
- スペル13枚（移動系4種×1枚＋ファイヤーボール4枚＋ヒール4枚＋各ブック固有のS スペル1枚: 火森は毒霧、水雷は追徴課税）

`battleCards.js`の`STARTER_DECKS`に`composition`（`{def, count}`の配列×モンスター/アイテム/スペル）を追加し、新設の`buildStarterCardList(bookId)`が`duplicateForDeck`でこのリストからちょうど40枚を組み立てる（乱数要素なし）。`Deck`の通常コンストラクタ（`new Deck(extra, {elements})`）は汎用枠を自動で埋め足してしまう（`DEFAULT_MONSTER_COUNT=24`等に足りない分を補充する仕様のため）ので使えず、`buildStarterDeckList`/`game.js`（`deckList`未保存時のフォールバック）とも`Deck.fromCardList()`経由に変更し、汎用枠が一切混ざらないようにした。

### 検証
Node単体テスト（初期デッキの40/20/7/13枚の内訳と各カードの正確な枚数×2ブック分、`Deck.fromCardList`後もcatalogIdが保たれること、汎用プレースホルダーが1枚も混ざらないこと）と、モンスター移動・アイコン同期・ホップ演出のNode単体テスト（空き地への移動/侵略成功/防衛による往復の3パターン、呪いが全パターンで消えること、盤上アイコンのmesh参照が使い回されること）で全パス。実機（Vite dev server）でも新規アカウント作成→火・森ブック選択→デッキ編成画面で40枚構成が仕様通り表示されることを確認し、さらにCPU戦の実ゲーム状態に対して`game._humanMoveFlow`を直接叩き、実際のThree.jsシーン上でアイコンの生成・移動ホップ・防衛往復・呪い消去がすべて正しく動作することを確認した。

## 盤面ヘルプ拡充＋盤面表示強化＋キャラ専用固定デッキ第1弾（2026-08-12実装）

### 盤面ヘルプに4項目追加
`main.js`の`HELP_TEXT`に【周回ボーナス】【土地レベルと連鎖】【土地コマンド】【呪い】の4節を追加。既存の勝敗目標/サイコロ移動/バトルボーナス等と合わせて、盤面の主要ルールが一通り読めるようになった。

### 周回ボーナスのカルドセプト準拠性について（ユーザーへの回答）
現状の実装はSTARTマス通過で**固定100G**（フリーランサー効果や「帰巣本能」「宝くじ」等のカード効果で変動はするが、周回数そのものでは増えない）。調べた限り、本家カルドセプト（ビギンズ）の周回ボーナスは「(周回数+1)×100G」のように**周を重ねるほど基本額が増えていく**方式で、単純な固定額ではない。つまり現状は完全には準拠していない。周回数に応じて増額する仕様に変更するかはゲームバランスに関わる判断のため、実装はせず今回は現状の確認・報告のみとした（変更する場合は`_grantGoalBonus`の`bonus`計算に`player.lapsCompleted`を掛ける形になる想定）。

Sources:
- [【カルドセプトビギンズ】での周回ボーナス・護符について調べてみる会｜くにみち](https://note.com/odo17891/n/ne61c90003292)
- [【カルドセプト】ルールと遊び方【ビギンズ】 - 神ゲー攻略](https://kamigame.jp/culdcept-begins/page/429540651093234368.html)

### 盤面表示の強化: ミニカード＋通行料バッジ＋HPゲージ＋所有者名
これまで配置モンスターは属性色の丸トークンのみだったのを、ミニカード風の見た目に刷新した。
- `scene.js`の`createUnitIcon(unit, tilePosition)`: 1枚のcanvasテクスチャに「上部の通行料バッジ」「カード本体（属性色の背景＋名前、`imageDataUrl`があれば実イラストを上部に描画）」「下部のHPゲージ（残量に応じて緑→黄→赤、数値表示付き）」をまとめて描画するミニカードのbillboardスプライト。実イラストは`Image`＋URLキャッシュ（`unitCardArtCache`）で非同期ロードし、届き次第再描画する。
- `updateUnitIcon(sprite, {hp, maxHp, toll})`: HP・通行料など変わりやすい値だけを差分チェックして再描画する（変化がなければ何もしない）。
- `createOwnerLabel(name, tilePosition)`/`removeOwnerLabel`: マスの手前下端に所有者名を表示する小さなテキストラベル。
- `game.js`の`_syncUnitIcons`は`tile.unit`の有無に加えて、既存アイコンへも毎回`updateUnitIcon`でHP・通行料（`_tollOfTile`）を渡すよう拡張。所有者名ラベルも`tile.unit`/`tile.owner`のライフサイクルに合わせて自動生成・削除する。移動時のホップ演出（既存の`_hopUnitIcon`）は変更なし - スプライト自体を使い回すので位置トゥイーンだけで済む。

### ヒトデ専用固定デッキ（`chinu-quest2-deck-hitode_1.md`）
キャラクター別の完全固定40枚デッキの第1弾。`battleCards.js`に`CHARACTER_DECKS`（`STARTER_DECKS.composition`と同じ形）と`buildCharacterCardList`/`buildCharacterDeckList`を新設し、`story.js`のヒトデ関連opponents（本編/再戦/ステージ③後の隠し再戦の3箇所）に`deckKey: 'hitode'`を追加。`main.js`の`buildBattlePlayerConfigs`は`deckKey`があれば`buildCharacterDeckList`、なければ従来の`buildThemedDeckList`（テーマ属性のランダム構成）を使う分岐にし、他のキャラクターは仕様書が来るまで現状のランダム構成のまま個別に差し替えていく方針。ヒトデの構成: モンスター20枚（水N16+水S1+無色N2+無色R1）／アイテム8枚（こん棒4+防弾チョッキ3+ハリネズミの服1）／スペル12枚（サイコロ系中心+放水2）。

### 検証
Node単体テスト（ヒトデデッキの40/20/8/12枚の内訳と各カード枚数、story.jsの3箇所全てにdeckKeyが設定されていること、盤面表示の作成/更新/削除のライフサイクルとHP・通行料のペイロード）で全パス。実機（Vite dev server）でストーリーモード①ヒトデ戦を実際に開始し、ヒトデが本当に固定デッキ（こん棒/きのこ派/山賊フクロウ/防弾チョッキ等）で対戦を始めることを確認。さらに実ゲーム状態にモンスターを配置してcanvasのピクセルデータを直接検査し、ミニカード（110×168、非透過ピクセル約89%）・HPゲージ・通行料バッジ・所有者名ラベル（160×40、非透過ピクセル約55%）が実際に描画されていること、通行料の値が`_tollOfTile`と一致することを確認した。ヘルプモーダルの新規4項目もDOM上に存在することを確認済み。

## 周回ボーナスをカルドセプト準拠の計算式に変更（2026-08-12実装）
これまで周回ボーナスは常に固定100Gだったが、本家カルドセプト（ビギンズ）の以下の仕様に合わせて再設計した。

- **基本ボーナス**: `(周回数+1)×100G`。周を重ねるほど増えていく（1周目200G、2周目300G…）。フリーランサーが盤上にいれば従来通りこの部分だけ倍率補正される
- **領地ボーナス**: `所持土地数×60G`（2人戦）／`所持土地数×80G`（3人以上）。**連鎖数や土地レベルは一切影響しない**（本家も同様の仕様）。土地の価値そのものへの連鎖ボーナス（地価=基本地価×レベル倍率×連鎖倍率）は既存の`_landValueOfTile`/`_chainMultiplier`で以前から実装済みで、これとは別枠

`game.js`に共通計算メソッド`_computeLapBonus(player)`を新設し（`{base, land, total}`を返す）、`_grantGoalBonus`（通常のゴール通過）と`_spellReturnToStartDoubleBonus`（帰巣本能=この2倍）の両方から使う形に統一。ログにも`+380G（基本200G＋領地180G）`のように内訳を表示するようにした。`main.js`のヘルプ【周回ボーナス】節も新しい計算式に合わせて更新。

### 検証
Node単体テスト14件（周回数に応じた基本ボーナスの増加、2人/3人以上での領地ボーナス単価の違い、フリーランサー倍率が基本ボーナスのみに掛かること、`_grantGoalBonus`/帰巣本能それぞれが正しい額を加算・周回数を進めること）で全パス。実機でも土地3枚所有・チェックポイント通過済みの状態で実際に`_grantGoalBonus`を2回叩き、1周目+280G（基本100+領地180）→2周目+380G（基本200+領地180）と正しく増加すること、ログの内訳表示が実際の画面に出ることを確認した。

## フェーズ5: 対戦モード
- Firebaseを使ったリアルタイムマルチプレイ
- 盤面状態・ターン進行・プレイヤー状態の同期

---

## データ構造（叩き台）

```
users/{userId}/
  ├─ currency: number
  ├─ ownedCards: [カードID配列]
  ├─ decks/
  │   ├─ deck1: { name: string, cards: [カードID配列] }
  │   └─ deck2: { name: string, cards: [カードID配列] }
  └─ matchHistory: [対戦履歴配列]

cards/{cardId}/
  ├─ name: string
  ├─ rarity: string（コモン/レア/激レア等）
  ├─ imageUrl: string
  └─ stats: {...}（攻撃力・防御力等、フェーズ2で詳細設計）
```

## アセット・容量について
- カードイラストは1枚ずつ用意（WebP形式、圧縮推奨）
- 100〜300枚規模でもFirebase Storage無料枠（5GB）内に収まる想定
- ダウンロード量対策としてブラウザキャッシュを効かせる設計にする

## 参考にしている前作からの変更点
- プレイヤー同士の直接バトルを廃止し、モンスターバトルのみに一本化
- 最初にデッキを選ぶ／自由に編成する形式を新規導入
- カード収集・ガチャによる成長ループを新規導入
- 土地の獲得を「購入」から「モンスター召喚」に変更。購入コストは廃止し召喚コストのみ。他プレイヤーの土地への侵略も同じ仕組み
- ブック（デッキ）編集にカード収集の楽しみを持たせる。あわせて、自分で育てるブリードモンスターをブックに編成できる仕組みを新規導入
